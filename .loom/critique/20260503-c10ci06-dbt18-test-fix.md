---
id: critique:c10ci06-dbt18-test-fix
kind: critique
status: final
created_at: 2026-05-03T23:03:34Z
updated_at: 2026-05-03T23:03:34Z
scope:
  kind: repository
  repositories:
    - repo:root
review_target: "ticket:c10ci06 dbt 1.8 ColumnInfo.config test compatibility patch"
links:
  tickets:
    - ticket:c10ci06
  evidence:
    - evidence:c10ci06-main-ci-dbt18-test-fix
external_refs:
  main_tests_run: https://github.com/z3z1ma/dbt-osmosis/actions/runs/25293085888
---

# Summary

Follow-up critique of the dbt 1.8 test compatibility patch after main CI exposed tests that assumed `ColumnInfo.config` exists in every supported dbt runtime.

# Review Target

Reviewed the uncommitted diff after `ff2b7da`, especially `tests/core/test_transforms.py`, `tests/core/test_inheritance_behavior.py`, `ticket:c10ci06`, and `evidence:c10ci06-main-ci-dbt18-test-fix`.

# Verdict

`pass`.

The dbt 1.8 test fix is a fair version-aware adjustment, not an improper weakening of dbt 1.10/1.11 coverage.

# Findings

None - no open findings.

# Evidence Reviewed

- Current uncommitted diff after `ff2b7da`.
- `tests/core/test_inheritance_behavior.py`, including the `_ensure_column_config` skip when the runtime column object has no `config` field and the active config inheritance assertions when it does.
- `tests/core/test_transforms.py`, including the conditional assertion that `config` is non-null when present and the unconditional YAML sync output assertions.
- `ticket:c10ci06` evidence, critique, claim matrix, and journal updates.
- `evidence:c10ci06-main-ci-dbt18-test-fix`, including local dbt 1.8 targeted `2 passed, 1 skipped` and dbt 1.11 targeted `3 passed` observations.
- `.github/workflows/tests.yml` matrix shape.
- `git diff --check` was clean.

# Residual Risks

- Full main CI after this exact fix is still pending.
- dbt 1.10 post-fix local targeted evidence is not separately recorded; final matrix rerun should confirm it.
- The feature-detection skip would also skip if a future supported dbt unexpectedly lacks `ColumnInfo.config`; inspect CI skip summaries for dbt 1.10/1.11.

# Required Follow-up

- Commit the test, ticket, evidence, and critique updates together.
- Rerun main CI after push.
- Record the resulting CI evidence, including whether dbt 1.10/1.11 rows keep config-specific coverage active.
- Keep `ticket:c10ci06` acceptance-pending until CI evidence and retrospective disposition are reconciled.

# Acceptance Recommendation

`ticket-acceptance-review-needed`.

This fix is acceptable to commit and send through CI. Do not close the ticket until the post-fix main CI run is recorded and passes.
