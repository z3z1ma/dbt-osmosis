---
id: packet:critique-ticket-c10loom04-20260504T211528Z
kind: packet
packet_kind: critique
status: consumed
target: ticket:c10loom04
review_target:
  kind: code_change
  summary: "c10loom04 dbt-loom parser and manifest mutation compatibility fix"
  ref: ticket:c10loom04
  diff: "working tree diff from a33563f558b432a59f3d656fdfddb7ec365ac241"
  paths:
    - src/dbt_osmosis/core/config.py
    - tests/core/test_config.py
mode: review
change_class: code-behavior
risk_class: medium
style: reference-first
created_at: 2026-05-04T21:15:28Z
updated_at: 2026-05-04T21:20:35Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - reviewer returns output only"
  paths:
    - "None - reviewer returns output only"
parent_merge_scope:
  records:
    - critique:c10loom04-dbt-loom-parser-review
    - ticket:c10loom04
    - packet:critique-ticket-c10loom04-20260504T211528Z
  paths:
    - .loom/critique/20260504-c10loom04-dbt-loom-parser-review.md
    - .loom/tickets/20260503-c10loom04-replace-dbt-loom-parser-hack.md
    - .loom/packets/critique/20260504T211528Z-ticket-c10loom04.md
source_fingerprint:
  git_commit: a33563f558b432a59f3d656fdfddb7ec365ac241
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: a33563f558b432a59f3d656fdfddb7ec365ac241
  git_status_summary: dirty_mixed
  git_status_detail: "tracked c10loom04 source/test/ticket edits plus untracked Ralph packet, evidence record, and this critique packet"
  compiled_from:
    - ticket:c10loom04
    - packet:ralph-ticket-c10loom04-20260504T210814Z
    - evidence:c10loom04-dbt-loom-parser-validation
    - research:dbt-110-111-api-surfaces
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 80
  avoid_full_file_reads: true
sources:
  target_records:
    - ticket:c10loom04
  evidence:
    - evidence:c10loom04-dbt-loom-parser-validation
  diffs_or_paths:
    - src/dbt_osmosis/core/config.py
    - tests/core/test_config.py
    - "git diff -- src/dbt_osmosis/core/config.py tests/core/test_config.py"
  prior_packets:
    - packet:ralph-ticket-c10loom04-20260504T210814Z
links:
  ticket:
    - ticket:c10loom04
  evidence:
    - evidence:c10loom04-dbt-loom-parser-validation
  packet:
    - packet:ralph-ticket-c10loom04-20260504T210814Z
---

# Mission

Review the c10loom04 implementation for code correctness, dbt 1.10/1.11 compatibility, scope discipline, and evidence sufficiency before ticket acceptance.

# Governing Context

`ticket:c10loom04` acceptance criteria:

- ACC-001: Cross-project model nodes are parsed through a valid dbt 1.10/1.11-compatible path.
- ACC-002: The code no longer calls `ModelParser.parse_from_dict(None, ...)`.
- ACC-003: Manifest mutation is done through a clear supported boundary or a narrowly documented compatibility shim.
- ACC-004: A test mocks or fixtures dbt-loom manifests and verifies eligible nodes are added to the manifest.
- ACC-005: dbt-loom load failure remains non-fatal with clear warnings.

Ralph iteration 01 returned `stop` and changed only `src/dbt_osmosis/core/config.py` and `tests/core/test_config.py`.

# Review Lens

Named critique profiles to apply:

- code-change
- dbt-compatibility

Focus especially on:

- Whether `ModelNode.from_dict(node)` is a valid and sufficiently safe replacement for the old parser hack.
- Whether `_set_project_manifest()` is narrow, safe, and correctly used.
- Whether public/private/protected/non-model filtering still matches ticket scope.
- Whether failure paths remain non-fatal and warnings remain useful.
- Whether the test checks meaningful behavior rather than merely implementation trivia.

# Change Class

Declared above as `code-behavior`, medium risk.

# Evidence Expectations

Review `evidence:c10loom04-dbt-loom-parser-validation` and decide whether it is enough for ticket review. Do not treat evidence as acceptance; critique should state residual risks and recommendations.

# Source Snapshot

Key implementation claims from parent diff review:

- `src/dbt_osmosis/core/config.py` now imports `ModelNode` instead of `ModelParser`.
- `_add_cross_project_references()` now appends `ModelNode.from_dict(node)` for exposed model nodes.
- `_set_project_manifest()` isolates the private `_DbtProject__manifest` write and uses a public property setter if one becomes available.
- `create_dbt_project_context()` uses `_set_project_manifest()` after optional dbt-loom manifest augmentation.
- `tests/core/test_config.py` adds `test_add_cross_project_references_imports_exposed_models_without_parser_hack()`.

Parent validation:

- `uv run pytest tests/core/test_config.py -q` -> `21 passed`.
- `uv run ruff check src/dbt_osmosis/core/config.py tests/core/test_config.py` -> passed.
- `uv run basedpyright src/dbt_osmosis/core/config.py` -> `0 errors, 29 warnings`.

# Diff Under Review

Run or inspect:

```bash
git diff -- src/dbt_osmosis/core/config.py tests/core/test_config.py
```

Changed paths under review:

- `src/dbt_osmosis/core/config.py`
- `tests/core/test_config.py`

# Required Questions

- Did the actual diff satisfy ticket:c10loom04 ACC-001 through ACC-005 and declared write boundary?
- Did the change add unrequested behavior, scope creep, or a new owner-layer claim in the wrong place?
- Does the evidence support the implementation and ticket review claims, or is it stale, partial, missing, or overclaimed?
- Did the review inspect the actual files, records, tests, and diff rather than trusting the child or parent report?
- Are tests checking real behavior rather than only mock behavior or implementation trivia?
- Are there unresolved risks that should block acceptance, become accepted risk, or turn into linked follow-up work?

# Stop Conditions

Return `blocked` if the working tree no longer contains the c10loom04 implementation diff or the target records conflict materially with this packet.

# Output Contract

Return:

- verdict
- findings with severity/confidence
- evidence reviewed
- file and line references for code findings when practical
- residual risks
- follow-up recommendation

# Working Notes

Reviewer must not edit files, records, commits, or git config.

# Reviewer Output

Verdict: `pass`.

Findings: none.

Evidence reviewed:

- Critique packet `.loom/packets/critique/20260504T211528Z-ticket-c10loom04.md`.
- Ticket acceptance criteria in `.loom/tickets/20260503-c10loom04-replace-dbt-loom-parser-hack.md`.
- Evidence record `.loom/evidence/20260504-c10loom04-dbt-loom-parser-validation.md`.
- Working-tree diff for `src/dbt_osmosis/core/config.py` and `tests/core/test_config.py`.
- Implementation refs: `_set_project_manifest()` shim, context manifest setter, dbt-loom node import path, dbt-loom non-fatal wrapper, and regression test.

Read-only checks:

- `git diff --check -- src/dbt_osmosis/core/config.py tests/core/test_config.py` -> no output.
- Grep confirmed no production `ModelParser.parse_from_dict(None, ...)` remains.
- Installed dbt-core introspection found `ModelNode.from_dict(d, *, dialect=None)` under dbt-core `1.10.20`.

Residual risks:

- No full dbt 1.11 runtime matrix or real dbt-loom package integration was run; evidence is focused/local plus source-level compatibility.
- `_set_project_manifest()` fallback remains a private compatibility shim if no public setter exists, but it is narrow and documented.

Follow-up recommendation: proceed to ticket acceptance review. Broader dbt 1.10/1.11 matrix or real dbt-loom integration coverage may be follow-up, not a blocker for this ticket.

Acceptance recommendation label: `no-critique-blockers`.

# Parent Merge Notes

Parent recorded final critique `critique:c10loom04-dbt-loom-parser-review` and updated `ticket:c10loom04` critique disposition to completed on 2026-05-04T21:20:35Z. Ticket moved to `complete_pending_acceptance` pending final validation, commit, push, and remote CI evidence.
