---
id: "packet:critique-ticket-c10diff31-20260505T004715Z"
kind: packet
packet_kind: critique
status: consumed
target: "ticket:c10diff31"
review_target:
  kind: "code_change"
  summary: "schema diff type normalization and one-to-one rename matching"
  ref: "ticket:c10diff31"
  diff: "dirty worktree against 4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4"
  paths:
    - "src/dbt_osmosis/core/diff.py"
    - "tests/core/test_diff.py"
    - ".loom/tickets/20260503-c10diff31-fix-schema-diff-rename-type-noise.md"
    - ".loom/evidence/20260505-c10diff31-schema-diff-validation.md"
    - ".loom/packets/ralph/20260505T003927Z-ticket-c10diff31-iter-01.md"
mode: review
change_class: "code-behavior"
risk_class: "medium"
style: reference-first
created_at: "2026-05-05T00:47:16Z"
updated_at: "2026-05-05T00:52:20Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - reviewer returns output only"
  paths:
    - "None - reviewer returns output only"
parent_merge_scope:
  records:
    - "critique:c10diff31-schema-diff-review"
    - "ticket:c10diff31"
  paths:
    - ".loom/critique/20260505-c10diff31-schema-diff-review.md"
    - ".loom/tickets/20260503-c10diff31-fix-schema-diff-rename-type-noise.md"
    - ".loom/packets/critique/20260505T004715Z-ticket-c10diff31.md"
source_fingerprint:
  git_commit: "4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4"
  git_status_summary: "dirty_mixed"
  git_status_detail: "Tracked c10diff31 ticket/source/test changes plus untracked c10diff31 evidence and Ralph packet; review target is this dirty diff."
  compiled_from:
    - "ticket:c10diff31"
    - "initiative:dbt-110-111-hardening"
    - "evidence:c10diff31-schema-diff-validation"
    - "packet:ralph-ticket-c10diff31-20260505T003927Z"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "forbidden - reviewer only needs local diff, records, and validation evidence"
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  target_records:
    - "ticket:c10diff31"
    - "packet:ralph-ticket-c10diff31-20260505T003927Z"
  evidence:
    - "evidence:c10diff31-schema-diff-validation"
  diffs_or_paths:
    - "git diff -- src/dbt_osmosis/core/diff.py tests/core/test_diff.py .loom/tickets/20260503-c10diff31-fix-schema-diff-rename-type-noise.md"
    - ".loom/evidence/20260505-c10diff31-schema-diff-validation.md"
links:
  ticket:
    - ticket:c10diff31
  evidence:
    - evidence:c10diff31-schema-diff-validation
---

# Mission

Review the `ticket:c10diff31` implementation for correctness, scope discipline, evidence sufficiency, and operator clarity before the ticket can move toward acceptance.

# Governing Context

The ticket requires conservative type normalization, original type preservation for real changes, one-to-one rename matching, deterministic ambiguous rename output, and focused tests.

Acceptance targets:

- `ticket:c10diff31#ACC-001`
- `ticket:c10diff31#ACC-002`
- `ticket:c10diff31#ACC-003`
- `ticket:c10diff31#ACC-004`
- `ticket:c10diff31#ACC-005`
- `initiative:dbt-110-111-hardening#OBJ-006`

# Review Lens

Named critique profiles to apply:

- code-change
- test-coverage
- operator-clarity

# Change Class

`code-behavior`, medium risk. The reviewed behavior is user-facing schema diff output, so prioritize false positives/false negatives, deterministic behavior, and whether the tests prove the ticket claims without broadening adapter type alias policy.

# Evidence Expectations

Review the local red/green evidence in `evidence:c10diff31-schema-diff-validation`, the child output in `packet:ralph-ticket-c10diff31-20260505T003927Z`, and the actual dirty diff. Do not treat the child output as proof without checking the code and tests.

# Source Snapshot

Parent-observed implementation summary:

- `src/dbt_osmosis/core/diff.py` now uses `_normalize_comparable_type()` before deciding whether to emit `ColumnTypeChanged`.
- `_normalize_comparable_type()` lowercases and removes all whitespace via `"".join(data_type.lower().split())`.
- Real `ColumnTypeChanged` records still carry the original YAML/database type strings.
- `_detect_column_renames()` sorts removed and added candidates and removes each matched added candidate from `available_added`.
- `tests/core/test_diff.py` adds focused tests for type case/whitespace equality, original type preservation, one-to-one rename matching, and deterministic ambiguous rename output.

# Diff Under Review

Use the local dirty worktree diff in `/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening` against `4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4` for:

- `src/dbt_osmosis/core/diff.py`
- `tests/core/test_diff.py`
- c10diff31 Loom ticket/evidence/packet records

# Required Questions

- Does the implementation satisfy all five c10diff31 acceptance criteria without widening scope?
- Could the type normalization suppress a meaningful real type change that should still be reported?
- Does greedy sorted rename matching introduce a correctness issue or merely the accepted deterministic minimal behavior?
- Are the tests strong enough, or do they overfit private helper behavior and miss `compare_node()` behavior where it matters?
- Is the evidence fresh and sufficient for local acceptance review, with limitations stated honestly?
- Are there findings that should block acceptance, become accepted risk, or be converted to follow-up?

# Stop Conditions

Stop or return `blocked` if the review target, source fingerprint, governing records, or diff under review appears materially stale or inconsistent with this packet.

# Output Contract

Return:

- verdict (`pass`, `pass_with_findings`, `changes_required`, or `inconclusive`)
- findings with severity/confidence and file/line references when practical
- evidence reviewed
- residual risks
- follow-up recommendation

# Working Notes

Reviewer is read-only. Do not edit files or records.

# Reviewer Output

Verdict: `pass_with_findings`

Findings:

- Low / medium confidence: type normalization can theoretically hide semantic whitespace in exotic/custom type strings. Normal adapter type strings are covered; accept risk unless the project wants to support those exotic strings.
- Low / high confidence: rename tests partly overfit `_detect_column_renames()` directly. Existing `compare_node()` rename coverage exists and the integration path looks correct; optional future follow-up could add a competing-rename `compare_node()` regression.

Evidence reviewed:

- Critique packet, ticket, Ralph packet, evidence record.
- Dirty diff for `src/dbt_osmosis/core/diff.py`, `tests/core/test_diff.py`, ticket/evidence records.
- Fresh local validation: `uv run pytest tests/core/test_diff.py -q -p no:cacheprovider` -> `20 passed, 2 warnings`.
- Fresh static validation: `uv run ruff check ... && uv run ruff format --check ... && git diff --check` -> passed.

Residual risks:

- Rare semantic-whitespace type strings.
- Greedy rename assignment may choose a deterministic but non-optimal match.
- Remote CI not observed.

Required follow-up: None before acceptance. Optional follow-up: add a `compare_node()` competing-rename regression test.

Acceptance recommendation: `risk-disposition-needed`.

# Parent Merge Notes

Parent recorded final critique as `critique:c10diff31-schema-diff-review`. The ticket still owns disposition and acceptance; critique recommends risk disposition but no required code follow-up before acceptance.
