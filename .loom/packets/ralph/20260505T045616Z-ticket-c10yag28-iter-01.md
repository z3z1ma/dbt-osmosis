---
id: "packet:ralph-ticket-c10yag28-20260505T045616Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10yag28"
mode: execution
change_class: code-behavior
risk_class: low
style: reference-first
verification_posture: test-first
iteration: "1"
created_at: "2026-05-05T04:56:16Z"
updated_at: "2026-05-05T05:03:18Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns Loom output only; parent reconciles ticket, evidence, and packet status"
  paths:
    - "src/dbt_osmosis/core/settings.py"
    - "src/dbt_osmosis/core/introspection.py"
    - "pyproject.toml"
    - "tests/core/test_settings.py"
    - "tests/core/test_config_resolution.py"
    - "tests/core/test_introspection.py"
parent_merge_scope:
  records:
    - "ticket:c10yag28"
    - "packet:ralph-ticket-c10yag28-20260505T045616Z"
  paths:
    - ".loom/tickets/20260503-c10yag28-lean-code-cleanup.md"
    - ".loom/packets/ralph/20260505T045616Z-ticket-c10yag28-iter-01.md"
    - ".loom/evidence/20260505-c10yag28-lean-cleanup-validation.md"
source_fingerprint:
  git_commit: "d812dd42c519fc732005b5615beb460bc7456b24"
  integration_remote: "origin"
  integration_ref: "refs/remotes/origin/main"
  integration_commit: "d812dd42c519fc732005b5615beb460bc7456b24"
  git_status_summary: "dirty_mixed"
  git_status_detail: "Expected dirty state at launch is the parent ticket update plus this newly compiled Ralph packet; no code/config/test files are dirty."
  compiled_from:
    - "ticket:c10yag28"
    - "ticket:c10res14"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed - local package/test commands may resolve existing uv-managed dependencies if needed; no remote Git mutation is allowed"
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research: []
  spec: []
  plan: []
  ticket:
    - "ticket:c10yag28"
    - "ticket:c10res14"
links:
  github_issue:
    - "https://github.com/z3z1ma/dbt-osmosis/issues/379"
---

# Mission

Perform the narrow low-risk cleanup for `ticket:c10yag28`: remove the private `ThreadPoolExecutor._max_workers` mutation, make unsupported database property access unmistakably unsupported instead of silently falling back to manifest data, verify resolver helper/source classes are not dead after `ticket:c10res14`, and remove stale Black/isort config only if no active workflow uses it.

# Bound Context

`ticket:c10res14` is closed and accepted. Its ticket says the context-aware resolver is now live, production call sites no longer use `_get_setting_for_node()`, and the remaining compatibility wrapper is intentionally retained. Do not remove public compatibility exports from `src/dbt_osmosis/core/osmosis.py` unless you find concrete evidence they are not public or are already deprecated for removal.

Repository guidance says Ruff is canonical. `pyproject.toml` still contains `[tool.black]` and `[tool.isort]`; `.pre-commit-config.yaml`, `Taskfile.yml`, and observed workflow/tooling references use Ruff, not Black or isort.

# Source Snapshot

Relevant current implementation reality:

- `src/dbt_osmosis/core/settings.py` creates `YamlRefactorContext.pool` with a default `ThreadPoolExecutor`, then `__post_init__` mutates `self.pool._max_workers` when dbt threads are available.
- `src/dbt_osmosis/core/introspection.py` exposes `PropertySource.DATABASE` and `PropertyAccessor.get(..., source=PropertySource.DATABASE)` logs that database access is not implemented, then falls back to manifest data.
- `SettingsResolver` uses `ConfigMetaSource`, `UnrenderedConfigSource`, `SupplementaryFileSource`, and `ProjectVarsSource` after `ticket:c10res14`; these classes are not dead if current code confirms that usage.
- `_get_setting_for_node()` has no production call sites but remains imported/exported by `core/osmosis.py` for compatibility and is covered by tests. Treat it as a compatibility wrapper, not removable cleanup for this iteration unless you find stronger evidence.

# Change Class

Low-risk code-behavior cleanup. The private executor mutation and database source fallback are behavior footguns; the cleanup should be small and backed by focused tests. If the diff grows into broad resolver/API refactoring, stop and return `escalate`.

# Verification Targets

- `ticket:c10yag28#ACC-001`
- `ticket:c10yag28#ACC-002`
- `ticket:c10yag28#ACC-003`
- `ticket:c10yag28#ACC-004`
- `ticket:c10yag28#ACC-005`

# Task For This Iteration

Implement the smallest correct cleanup:

1. Replace production mutation of `ThreadPoolExecutor._max_workers` with construction-time worker selection. Preserve default behavior: when `project.runtime_cfg.threads` is a positive integer and no custom pool is supplied, `YamlRefactorContext.pool` should be constructed with that worker count; otherwise use the existing default worker formula. Do not read or mutate `_max_workers` in production code for this logic.
2. Change unsupported database property access so `PropertyAccessor.get(..., source=PropertySource.DATABASE)` or `source="database"` cannot be mistaken for real database introspection. Prefer an explicit `NotImplementedError` with a clear message over silent manifest fallback. Update docstrings/tests accordingly.
3. Verify current resolver source/helper classes are used by `SettingsResolver` or are compatibility wrappers. Remove only truly dead code inside the child write scope. Do not remove `core/osmosis.py` exports in this packet.
4. Remove `[tool.black]` and `[tool.isort]` from `pyproject.toml` if no active workflow or dependency uses them.
5. Add or update focused tests for executor construction and database source behavior. Existing tests may inspect `_max_workers` only as a test assertion because `ThreadPoolExecutor` has no public max-worker accessor; production code must not.

# Verification Posture

Test-first. Before implementation, create or adjust focused tests that fail for the current behavior:

- a test that fails while production `YamlRefactorContext.__post_init__` mutates `ThreadPoolExecutor._max_workers` instead of constructing the pool with the dbt thread count or default count;
- a test that fails while `PropertySource.DATABASE` silently returns manifest data instead of raising a clear unsupported-source error.

Drive those tests green, then run focused validation covering touched tests and files. Recommended commands:

- `uv run pytest tests/core/test_settings.py tests/core/test_config_resolution.py tests/core/test_introspection.py -q`
- `uv run ruff format --check src/dbt_osmosis/core/settings.py src/dbt_osmosis/core/introspection.py tests/core/test_settings.py tests/core/test_config_resolution.py tests/core/test_introspection.py pyproject.toml`
- `uv run ruff check src/dbt_osmosis/core/settings.py src/dbt_osmosis/core/introspection.py tests/core/test_settings.py tests/core/test_config_resolution.py tests/core/test_introspection.py && git diff --check`
- `uv run basedpyright --outputjson src/dbt_osmosis/core/settings.py src/dbt_osmosis/core/introspection.py`

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- the work requires removing public compatibility APIs or changing `core/osmosis.py` exports;
- fixing executor construction requires broad context lifecycle changes beyond `YamlRefactorContext`;
- database property source behavior is documented elsewhere as intentionally falling back to manifest;
- Black/isort are used by active workflow, dependency, or packaged config surfaces that were not visible in the parent snapshot;
- source/config/test files outside child write scope need edits.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`);
- files changed;
- tests/commands run with red and green evidence;
- concise explanation of executor and database-source behavior after the change;
- whether resolver/source helper cleanup required code changes or was verified as already used/compatibility-only;
- blockers or residual risks;
- ticket recommendation.

# Working Notes

Do not wait for GitHub Actions for `.loom`-only pushes. This packet concerns local implementation and validation only; parent will package and push separately.

# Child Output

Outcome: `stop`

Files changed:

- `src/dbt_osmosis/core/settings.py`
- `src/dbt_osmosis/core/introspection.py`
- `pyproject.toml`
- `tests/core/test_settings.py`
- `tests/core/test_introspection.py`

Red evidence:

```text
uv run pytest tests/core/test_settings.py::TestYamlRefactorContext::test_context_constructs_default_pool_with_dbt_threads tests/core/test_settings.py::TestYamlRefactorContext::test_context_preserves_custom_pool_worker_count tests/core/test_introspection.py::TestPropertyAccessorHasUnrenderedJinja::test_database_source_is_explicitly_unsupported -q
4 failed
```

Expected red failures: default pool was constructed with the old default count then mutated to dbt threads; custom pool was mutated from 2 to 4 workers; `PropertySource.DATABASE` / `source="database"` did not raise.

Green evidence:

```text
focused red command rerun -> 4 passed
uv run pytest tests/core/test_settings.py tests/core/test_config_resolution.py tests/core/test_introspection.py -q -> 134 passed, 2 warnings
uv run ruff format --check ... -> 5 files already formatted
uv run ruff check ... && git diff --check -> All checks passed!
uv run basedpyright --outputjson src/dbt_osmosis/core/settings.py src/dbt_osmosis/core/introspection.py -> 0 errors, 309 warnings
```

Implementation summary: `YamlRefactorContext` now constructs its default pool using dbt `runtime_cfg.threads` when it is a positive integer, otherwise the previous default worker formula; caller-supplied pools are preserved; production code no longer reads or mutates `_max_workers`; `PropertySource.DATABASE` / `source="database"` now raises a clear `NotImplementedError`; stale `[tool.black]` and `[tool.isort]` config was removed from `pyproject.toml`.

Resolver/source helper cleanup conclusion: `ConfigMetaSource`, `UnrenderedConfigSource`, `SupplementaryFileSource`, and `ProjectVarsSource` are actively used by `SettingsResolver`; no dead resolver/source helper removal was appropriate. Compatibility `_get_setting_for_node()` was not touched.

Blockers or residual risks: no blockers. basedpyright warning debt remains but error count is zero. `uv` emitted the known local `VIRTUAL_ENV` mismatch warning while running commands successfully.

Ticket recommendation: parent can reconcile this iteration as complete; no further Ralph iteration is needed for the bounded cleanup.

# Parent Merge Notes

Parent accepted the child output as inside scope and consistent with the packet contract. Parent inspected the diff and reran focused pytest, Ruff format check, Ruff lint plus `git diff --check`, and basedpyright; all passed with basedpyright `errorCount: 0` and warnings tolerated by current project policy. Evidence was recorded as evidence:c10yag28-lean-cleanup-validation. Ticket `ticket:c10yag28` was reconciled to `complete_pending_acceptance` pending implementation commit packaging and remote CI.
