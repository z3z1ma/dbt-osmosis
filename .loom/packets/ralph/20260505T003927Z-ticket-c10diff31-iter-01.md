---
id: "packet:ralph-ticket-c10diff31-20260505T003927Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10diff31"
mode: execution
change_class: "code-behavior"
risk_class: "medium"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-05T00:39:27Z"
updated_at: "2026-05-05T00:46:30Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260505T003927Z-ticket-c10diff31-iter-01.md"
  paths:
    - "src/dbt_osmosis/core/diff.py"
    - "tests/core/test_diff.py"
parent_merge_scope:
  records:
    - "ticket:c10diff31"
  paths:
    - ".loom/tickets/20260503-c10diff31-fix-schema-diff-rename-type-noise.md"
    - ".loom/evidence/"
    - ".loom/critique/"
source_fingerprint:
  git_commit: "4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes after `git fetch origin main`; current HEAD contains origin/main."
  compiled_from:
    - "ticket:c10diff31"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "forbidden - local tests and repository inspection are sufficient; child must not fetch or wait on remote CI"
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  ticket:
    - "ticket:c10diff31"
  evidence:
    - evidence:oracle-backlog-scan
links:
  tickets:
    - ticket:c10diff31
---

# Mission

Fix `ticket:c10diff31` schema diff noise by normalizing comparable data types before emitting type changes and making fuzzy rename detection one-to-one and deterministic.

# Bound Context

`ticket:c10diff31` advances `initiative:dbt-110-111-hardening#OBJ-006`: user-facing diff output should fail clearly, respect advertised behavior, and be covered by focused tests.

The ticket is bounded to these claims:

- `ticket:c10diff31#ACC-001`: Case-only type differences such as `VARCHAR` versus `varchar` do not emit changes.
- `ticket:c10diff31#ACC-002`: Whitespace/format-only type differences are normalized when safe.
- `ticket:c10diff31#ACC-003`: Rename detection cannot map two removed columns to the same added column.
- `ticket:c10diff31#ACC-004`: Ambiguous rename candidates produce deterministic output.
- `ticket:c10diff31#ACC-005`: Tests cover normalized type comparisons and one-to-one rename matching.

# Source Snapshot

Relevant current observations from parent inspection at source fingerprint `4e21bdadff2c7ebb9288182cf86ba2de6ba53fe4`:

- `src/dbt_osmosis/core/diff.py:330-355` compares `yaml_col.data_type` directly to `db_col.type` before calling `_classify_type_change()`, so case-only or whitespace-only differences still emit `ColumnTypeChanged` even though `_classify_type_change()` normalizes types internally.
- `src/dbt_osmosis/core/diff.py:439-455` already lowercases and strips spaces for severity classification. Reuse a single conservative comparable-normalization helper rather than expanding adapter-specific alias semantics.
- `src/dbt_osmosis/core/diff.py:389-437` loops over removed columns and calls `process.extractOne()` against the full `added` list every time, so multiple removed columns can pick the same added candidate.
- `tests/core/test_diff.py` already has fixture-based diff tests and a rename regression test at `test_schema_diff_detects_renamed_columns_without_context_node` that can be extended with focused cases.
- `src/dbt_osmosis/core/AGENTS.md` applies to `core/` edits: avoid broad refactors, keep cache and YAML conventions intact, and avoid new `type: ignore` comments.

# Change Class

Code behavior / test coverage. Risk is medium because schema diff output is user-facing and can influence migration decisions. Keep normalization conservative and preserve original type strings in output when there is a real type change.

# Verification Targets

- `ticket:c10diff31#ACC-001`
- `ticket:c10diff31#ACC-002`
- `ticket:c10diff31#ACC-003`
- `ticket:c10diff31#ACC-004`
- `ticket:c10diff31#ACC-005`

# Task For This Iteration

1. Add focused failing tests before implementation for case-only type differences, whitespace-only type differences, one-to-one rename matching, and deterministic ambiguous rename output.
2. Implement conservative type normalization for comparison only. Do not treat adapter aliases such as `string` and `varchar` as equal unless existing code already proves that policy.
3. Preserve original YAML/database type strings in `ColumnTypeChanged` when a real change exists.
4. Make rename matching reserve added candidates so one added column cannot satisfy two removed columns. Prefer deterministic ordering over relying on set/list iteration order.
5. Do not change severity taxonomy except as needed by the existing `_classify_type_change()` behavior.
6. Append child output to this packet before returning.

# Verification Posture

`test-first`: before implementation, add tests that fail for the expected current behavior. The red evidence should show type normalization tests currently emit unwanted `ColumnTypeChanged` records and rename matching currently maps more than one removed column to the same added column or otherwise behaves non-deterministically.

Recommended red command after adding tests and before implementation:

```bash
uv run pytest tests/core/test_diff.py -q -k "type_difference or rename"
```

Recommended green commands after implementation:

```bash
uv run pytest tests/core/test_diff.py -q
uv run ruff format src/dbt_osmosis/core/diff.py tests/core/test_diff.py
uv run ruff check src/dbt_osmosis/core/diff.py tests/core/test_diff.py && git diff --check
```

If basedpyright impact is plausible after the edit, run `uv run basedpyright --outputjson src/dbt_osmosis/core/diff.py` and report the result.

# Stop Conditions

- Stop and report `blocked` if governing records, `origin/main`, the worktree state, or child-write-scope files materially differ from the source fingerprint before implementation.
- Stop if the new red tests cannot be made to fail for meaningful current behavior before implementation; report whether the ticket appears stale and why.
- Stop and report `escalate` if satisfying the ticket requires adapter-specific semantic type alias policy beyond conservative case/whitespace normalization.
- Stop and report `escalate` if rename matching requires a broader redesign of fuzzy matching thresholds or severity taxonomy.
- Do not edit unrelated diff CLI selection behavior, SQL linting, YAML writer/parser behavior, docs, CI, release workflows, or unrelated tests.
- Do not run `git fetch`, mutate Git config/shared refs, push, or wait on GitHub Actions from the child.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: test names, command, failure reason, and confirmation failures match expected current behavior
- green evidence: focused pytest, Ruff format/check, whitespace status, and any skipped/blocked checks
- implementation summary, including the normalization boundary and rename matching behavior
- self-review findings or concerns
- blockers or risks
- ticket recommendation

# Working Notes

Keep the patch minimal. Prefer a tiny helper for comparable data types and deterministic sorting in rename matching over broader abstractions.

At child launch, the expected dirty state is only the parent ticket activation and this packet record. Treat `src/dbt_osmosis/core/diff.py` and `tests/core/test_diff.py` as clean at the packet baseline unless inspection shows otherwise.

# Child Output

Outcome: stop

Files changed:

- `src/dbt_osmosis/core/diff.py`
- `tests/core/test_diff.py`

Records changed:

- `.loom/packets/ralph/20260505T003927Z-ticket-c10diff31-iter-01.md`

Red evidence:

- Command: `uv run pytest tests/core/test_diff.py -q -k "type_difference or rename"`
- Result before implementation: 4 failed, 4 passed, 12 deselected, 2 warnings.
- Expected failures observed:
  - `test_schema_diff_rename_matches_added_columns_one_to_one`: current rename detection returned 2 renames for one added candidate.
  - `test_schema_diff_rename_output_is_deterministic_for_ambiguous_candidates`: ambiguous candidate output depended on added-list order (`customer-id` vs `customer id`).
  - `test_schema_diff_type_difference_ignores_case_only_changes`: case-only `VARCHAR` vs `varchar` emitted `ColumnTypeChanged`.
  - `test_schema_diff_type_difference_ignores_whitespace_only_changes`: whitespace-only `DECIMAL(10, 2)` vs `decimal(10,2)` emitted `ColumnTypeChanged`.

Green evidence:

- `uv run pytest tests/core/test_diff.py -q -k "type_difference or rename"`: 8 passed, 12 deselected, 2 warnings.
- `uv run pytest tests/core/test_diff.py -q`: 20 passed, 2 warnings.
- `uv run ruff format src/dbt_osmosis/core/diff.py tests/core/test_diff.py`: 1 file reformatted, 1 file left unchanged.
- Reran after formatting: `uv run pytest tests/core/test_diff.py -q`: 20 passed, 2 warnings.
- `uv run ruff check src/dbt_osmosis/core/diff.py tests/core/test_diff.py && git diff --check`: All checks passed.
- `uv run basedpyright --outputjson src/dbt_osmosis/core/diff.py`: 0 errors, 12 warnings. Warnings are existing style/deprecation/private-usage findings in `diff.py`; no new blocking type errors observed.

Implementation summary:

- Added conservative comparable type normalization for equality checks only: lowercase and remove whitespace using `split()`/`join()`.
- Reused the same helper for existing severity classification normalization.
- Type changes now compare normalized strings before emitting `ColumnTypeChanged`, while real type changes still preserve the original YAML/database type strings in the change record.
- Rename detection now sorts removed/added candidates and removes each matched added candidate from the available pool so one added column cannot satisfy multiple removed columns. Ambiguous ties are stable because the candidate pool is sorted before fuzzy matching.

Self-review findings or concerns:

- Patch stayed inside the child write scope.
- Normalization deliberately does not add adapter alias policy such as `string == varchar`.
- Greedy sorted rename reservation is deterministic and minimal, but it is not a global optimal assignment algorithm; broader matching strategy changes remain out of scope.

Blockers or risks:

- No blockers encountered.
- Local `uv` commands emitted the environment warning that the parent shell `VIRTUAL_ENV` points at the sibling worktree; `uv` ignored it and used this worktree's `.venv`.

Ticket recommendation:

- Implementation evidence is green for the scoped tests and checks. Recommend parent move `ticket:c10diff31` toward review/acceptance disposition per medium-risk code-behavior policy.

# Parent Merge Notes

Parent accepted the child output as inside scope and consistent with the packet contract. Parent reviewed the source/test diff, reran `uv run pytest tests/core/test_diff.py -q`, `uv run ruff format --check src/dbt_osmosis/core/diff.py tests/core/test_diff.py`, `uv run ruff check src/dbt_osmosis/core/diff.py tests/core/test_diff.py && git diff --check`, and `uv run basedpyright --outputjson src/dbt_osmosis/core/diff.py`; all passed with basedpyright `errorCount: 0` and existing warnings. Evidence was preserved in `evidence:c10diff31-schema-diff-validation`. Ticket `ticket:c10diff31` was reconciled to `review_required` because medium-risk code behavior has recommended critique pending.
