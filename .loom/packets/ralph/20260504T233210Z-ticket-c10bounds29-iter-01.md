---
id: "packet:ralph-ticket-c10bounds29-20260504T233210Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10bounds29"
mode: execution
change_class: "release-packaging"
risk_class: "high"
style: "reference-first"
verification_posture: "test-first"
iteration: 1
created_at: "2026-05-04T23:32:10Z"
updated_at: "2026-05-04T23:38:36Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns output only; parent reconciles ticket, evidence, critique, and packet status"
  paths:
    - pyproject.toml
    - .github/workflows/tests.yml
    - tests/test_package_metadata.py
    - README.md
    - docs/docs/intro.md
    - docs/docs/tutorial-basics/installation.md
parent_merge_scope:
  records:
    - ticket:c10bounds29
    - packet:ralph-ticket-c10bounds29-20260504T233210Z
  paths:
    - .loom/tickets/20260503-c10bounds29-define-dbt-support-range-warning-policy.md
    - .loom/packets/ralph/20260504T233210Z-ticket-c10bounds29-iter-01.md
    - .loom/evidence/20260504-c10bounds29-*.md
source_fingerprint:
  git_commit: "0af38b2b7c2db6b448418525d0dbfdc3068e37bb"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "0af38b2b7c2db6b448418525d0dbfdc3068e37bb"
  git_status_summary: "clean"
  git_status_detail: "git status --short was empty at packet compile time"
  compiled_from:
    - ticket:c10bounds29
    - initiative:dbt-110-111-hardening
    - research:dbt-110-111-api-surfaces
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed"
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 100
  avoid_full_file_reads: false
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  spec: []
  plan: []
  ticket:
    - ticket:c10bounds29
links:
  ticket:
    - ticket:c10bounds29
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
---

# Mission

Implement the accepted support-range policy for `ticket:c10bounds29`: keep published `dbt-core>=1.8` package metadata open, make future dbt minors explicit canaries instead of support guarantees, replace blanket deprecation-warning suppression with targeted warning visibility, and align docs/tests/CI with that policy.

# Bound Context

The governing initiative is `initiative:dbt-110-111-hardening`. It wants confident dbt Core 1.10.x and 1.11.x compatibility without silently overclaiming future dbt support.

The human policy decision for this packet is: **keep open + canary**. Do not add an upper bound such as `<1.12` to published `dbt-core` metadata in this iteration.

Relevant ticket acceptance criteria:

- `ticket:c10bounds29#ACC-001`: package dbt dependency constraints match actual support or clear intentional canaries.
- `ticket:c10bounds29#ACC-002`: documentation states supported dbt-core and adapter version policy.
- `ticket:c10bounds29#ACC-003`: CI has a latest-dbt canary if package allows unreviewed future dbt minors.
- `ticket:c10bounds29#ACC-004`: blanket `ignore::DeprecationWarning` is replaced with targeted ignores or focused warning policy.
- `ticket:c10bounds29#ACC-005`: compatibility jobs surface dbt deprecations relevant to dbt 1.10/1.11 and future migration risk.

# Parent Launch Checklist

- Target ticket readiness: `ticket:c10bounds29` is ready, high risk, release-packaging, mandatory critique. Human decision trigger was resolved by operator answer: keep the dependency open and add canary coverage.
- Source freshness: packet compiled at `0af38b2b7c2db6b448418525d0dbfdc3068e37bb`, equal to `origin/main`; worktree clean.
- Packet content: compiled packet has concrete launch fields and no unresolved template tokens.
- Child write scope: exact paths only; no Loom record writes by child.
- Parent merge scope: parent reconciles ticket, evidence, and packet lifecycle.
- Git context: same clean worktree and branch, no child fetch/prune/config mutations.
- Verification posture: test-first via `tests/test_package_metadata.py`.
- Stop conditions and output contract are below.

# Source Snapshot

Current implementation reality:

- `pyproject.toml` declares `"dbt-core>=1.8"` and globally ignores `DeprecationWarning` in pytest config.
- `.github/workflows/tests.yml` has a blocking dbt/Python matrix for dbt Core 1.8.0, 1.9.0, 1.10.0, and 1.11.0, plus a `latest-core-compat` job for 1.10.0 and 1.11.0. It does not currently have an unpinned latest-dbt canary for future minors.
- `README.md`, `docs/docs/intro.md`, and `docs/docs/tutorial-basics/installation.md` already mention open `dbt-core>=1.8` support and dbt 1.11 validation, but the wording should be sharpened so audited support and future-minor canary status are not ambiguous.
- `tests/test_package_metadata.py` is the existing project-level structural test file for package metadata, extras, CI, and docs consistency.

Relevant research conclusion from `research:dbt-110-111-api-surfaces`: CI and dependency policy should not claim broad dbt 1.11 support until matrix coverage asserts installed versions and runs meaningful fixture behavior; unsupported future dbt versions should be narrowed or covered by scheduled canary coverage before open package resolution is allowed.

# Change Class

Declared above as `release-packaging`, with ticket risk repeated as `high`. This affects package metadata, CI policy, and user-facing support expectations, so mandatory critique follows after implementation and evidence.

# Verification Targets

- ticket:c10bounds29#ACC-001
- ticket:c10bounds29#ACC-002
- ticket:c10bounds29#ACC-003
- ticket:c10bounds29#ACC-004
- ticket:c10bounds29#ACC-005
- initiative:dbt-110-111-hardening#OBJ-001
- initiative:dbt-110-111-hardening#OBJ-005

# Task For This Iteration

1. Add failing structural tests in `tests/test_package_metadata.py` before implementation. The tests should prove the current state is insufficient for the chosen policy. Cover at least:
   - package metadata keeps `dbt-core>=1.8` open and intentionally does not cap future dbt minors;
   - docs state audited/blocking support for dbt Core 1.8.x through 1.11.x, adapter compatibility responsibility, and future dbt Core minors as canary-only until audited;
   - `.github/workflows/tests.yml` has an unpinned latest-dbt canary job for future minors, is non-blocking or canary-scoped, and runs meaningful compatibility checks;
   - pytest config no longer contains blanket `ignore::DeprecationWarning` and instead surfaces dbt/dbt-osmosis deprecations.
2. Run the red check: `uv run pytest tests/test_package_metadata.py -q`. It must fail for the expected policy gaps before implementation.
3. Implement the smallest correct changes inside the child write scope:
   - keep `dbt-core>=1.8` in package metadata;
   - replace the blanket pytest `ignore::DeprecationWarning` with a focused warning policy that surfaces dbt and dbt-osmosis deprecations;
   - add a latest-dbt canary to `.github/workflows/tests.yml` for scheduled/manual CI, using unpinned latest `dbt-core` plus `dbt-duckdb`, with clear canary/non-blocking semantics so future upstream breakage is visible without redefining audited support;
   - ensure compatibility jobs set or inherit warning filters so dbt deprecations can surface in dbt parse/pytest paths;
   - update README and docs support policy text to distinguish package resolution openness, audited/blocking support, adapter compatibility, and future-minor canaries.
4. Run the green check: `uv run pytest tests/test_package_metadata.py -q`.
5. Run a formatting/lint validation proportional to the touched files. Prefer `uv run pre-commit run --files pyproject.toml .github/workflows/tests.yml tests/test_package_metadata.py README.md docs/docs/intro.md docs/docs/tutorial-basics/installation.md` if available.

# Verification Posture

`test-first`.

Expected red: the new package metadata test fails before implementation because the workflow lacks a future-latest-dbt canary, pytest config still has blanket `ignore::DeprecationWarning`, and docs do not clearly state future minors are canary-only.

Expected green: after implementation, `uv run pytest tests/test_package_metadata.py -q` passes and the pre-commit file subset passes or reports only issues the child clearly names.

The child must return red and green command output summaries, including the expected failure reason for red.

# Stop Conditions

- Stop and return `blocked` if the worktree is not at source fingerprint `0af38b2b7c2db6b448418525d0dbfdc3068e37bb` with no unrelated changes in the child write scope.
- Stop and return `escalate` if implementing the canary requires adding a `dbt-core` upper bound or narrowing Python/dbt support contrary to the operator decision.
- Stop and return `escalate` if current test output reveals a large real dbt deprecation remediation project instead of a warning-policy/CI/docs change.
- Stop rather than editing files outside the child write scope.
- Do not update Loom records; parent owns ticket/evidence/packet reconciliation.
- Do not run `git fetch`, `git fetch --prune`, remote edits, Git config edits, destructive commands, publishing, tagging, or release operations.
- For `test-first`, a failing check must exist before implementation, fail for the expected reason, and be driven to green inside this iteration.

# Output Contract

Return exactly these sections:

- Outcome: `continue`, `stop`, `blocked`, or `escalate`.
- Files changed.
- Records changed: should be `None` unless a stop condition required otherwise.
- Red evidence: command, failure summary, and why it failed for the expected reason.
- Green evidence: command results and any pre-commit/lint results.
- Implementation summary.
- Self-review findings or concerns.
- Blockers or residual risks.
- Ticket recommendation.

# Working Notes

Keep the implementation minimal. Do not broaden this ticket into publishing, release tagging, package upload, or support-floor changes.

# Child Output

Outcome: `stop`

Files changed:

- `pyproject.toml`
- `.github/workflows/tests.yml`
- `tests/test_package_metadata.py`
- `README.md`
- `docs/docs/intro.md`
- `docs/docs/tutorial-basics/installation.md`

Records changed: None.

Red evidence:

- Command: `uv run pytest tests/test_package_metadata.py -q`
- Valid red result: `3 failed, 6 passed`
- Failure summary: docs lacked audited dbt Core 1.8.x-1.11.x support and future-minor canary-only policy; workflow lacked `future-dbt-canary`; pytest config still contained blanket `ignore::DeprecationWarning`.
- The red result matched expected policy gaps from the packet.

Green evidence:

- `uv run pytest tests/test_package_metadata.py -q`: `9 passed, 2 warnings`.
- The warnings are visible dbt deprecation warnings from `dbt/cli/options.py`.
- `uv run pre-commit run --files pyproject.toml .github/workflows/tests.yml tests/test_package_metadata.py README.md docs/docs/intro.md docs/docs/tutorial-basics/installation.md`: passed all hooks after `ruff-format` reformatted the test file on the first run.

Implementation summary:

- Kept published package dependency open as `dbt-core>=1.8`.
- Replaced blanket pytest deprecation suppression with focused dbt/dbt-osmosis deprecation visibility.
- Added workflow-level `PYTHONWARNINGS` for dbt/dbt-osmosis deprecations.
- Added scheduled/manual non-blocking `future-dbt-canary` CI job using unpinned latest `dbt-core` and `dbt-duckdb`, running version checks, `pip check`, basedpyright, `dbt parse`, import smoke, and `pytest -q`.
- Updated README and docs to distinguish audited blocking support, open package metadata, adapter responsibility, and future-minor canary-only status.
- Added structural tests covering the support policy, canary workflow, and warning visibility.

Self-review findings or concerns:

- Initial red test briefly overmatched `dbt-core-interface`; fixed before recording the valid red run.
- Canary is intentionally non-blocking and schedule/manual scoped, so future upstream breakage is visible without redefining audited support.

Blockers or residual risks:

- No blockers.
- Residual risk: the unpinned future canary may fail when upstream dbt/adapter releases change; that is expected canary behavior and should be triaged when visible.

Ticket recommendation: accept this implementation iteration and route to mandatory critique / acceptance review for `ticket:c10bounds29`.

# Parent Merge Notes

Parent accepted the child output as inside packet scope after inspecting the implementation diff and rerunning validation. Parent-observed validation is preserved in `evidence:c10bounds29-support-policy-validation`. Ticket moved to `review_required` for mandatory release-packaging/operator-clarity/dbt-compatibility critique before acceptance.
