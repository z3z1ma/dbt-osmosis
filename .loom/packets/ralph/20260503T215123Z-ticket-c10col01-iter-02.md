---
id: packet:ralph-ticket-c10col01-20260503T215123Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:c10col01
mode: execution
change_class: tests
risk_class: medium
style: reference-first
verification_posture: none
iteration: 2
created_at: 2026-05-03T21:51:23Z
updated_at: 2026-05-03T22:01:00Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns implementation output; parent reconciles Loom records
  paths:
    - tests/core/test_transforms.py
parent_merge_scope:
  records:
    - ticket:c10col01
    - packet:ralph-ticket-c10col01-20260503T215123Z
  paths:
    - .loom/packets/ralph/20260503T215123Z-ticket-c10col01-iter-02.md
    - .loom/tickets/20260503-c10col01-preserve-columninfo-config.md
source_fingerprint:
  git_commit: 0fd1a17d5e33c15805685d2d290fd531fba476a2
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 0fd1a17d5e33c15805685d2d290fd531fba476a2
  git_status_summary: dirty_tracked
  git_status_detail: uncommitted ticket:c10col01 iter-01 implementation and packet output present
  compiled_from:
    - ticket:c10col01
    - packet:ralph-ticket-c10col01-20260503T214308Z
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: sibling_worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed - existing dependency/test commands may use installed package metadata; no remote mutation allowed
context_budget:
  posture: normal
  max_source_files: 6
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  spec: []
  plan: []
  ticket:
    - ticket:c10col01
links:
  github_issue: https://github.com/z3z1ma/dbt-osmosis/issues/352
---

# Mission

Close the critique-identified coverage gap for ticket:c10col01 by adding focused test coverage for injected missing columns under `fusion_compat=True`.

# Bound Context

This follows packet:ralph-ticket-c10col01-20260503T214308Z and the read-only critique. Do not change production code unless the new assertion exposes a real bug; if it does, stop and report rather than widening the write scope.

# Parent Launch Checklist

- Target ticket remains the same high-risk compatibility fix.
- Child write scope is test-only: `tests/core/test_transforms.py`.
- Verification posture is `none` because this is a coverage-only refinement on already-green behavior; child still must run the focused test and module tests after editing.

# Source Snapshot

Critique finding: the first regression test forces `context.fusion_compat = False`, while dbt 1.10+ defaults can use fusion-compatible output. `src/dbt_osmosis/core/sync_operations.py` cleans empty nested `config` entries after moving top-level metadata into `config`, but this ticket needs a focused assertion that injected empty config does not leak in that path.

# Change Class

`tests`. This does not change runtime behavior unless the test exposes a real failure, which would require parent escalation because production code is out of this packet's write scope.

# Verification Targets

- ticket:c10col01#ACC-003
- ticket:c10col01#ACC-004

# Task For This Iteration

Add a focused test or parameterize the existing regression test so injected missing columns are synced with `context.fusion_compat = True` and assert that an empty `config` block is not present in the YAML-facing `doc_section` output.

# Verification Posture

`none`: coverage-only refinement after the behavioral fix already landed in the previous Ralph iteration. Required checks are still `uv run pytest tests/core/test_transforms.py::test_inject_missing_columns_preserves_column_config_for_sync` or the newly named focused equivalent, then `uv run pytest tests/core/test_transforms.py`.

# Stop Conditions

- Stop if production code must change.
- Stop if the coverage requires broad `config.meta`/`config.tags` semantics owned by ticket:c10meta02.
- Stop if files outside `tests/core/test_transforms.py` need modification.

# Output Contract

Return outcome, files changed, records changed, verification commands/results, concerns, residual risks, and ticket recommendation.

# Working Notes

GitHub issue: https://github.com/z3z1ma/dbt-osmosis/issues/352

# Child Output

Outcome: `stop` - bounded test-only iteration completed.

Files changed:

- `tests/core/test_transforms.py` parameterized `test_inject_missing_columns_preserves_column_config_for_sync` over `fusion_compat = False` and `fusion_compat = True`.
- The test now explicitly asserts that synced column output does not include an empty `config` block.

Records changed: None.

Verification:

- `uv run pytest tests/core/test_transforms.py::test_inject_missing_columns_preserves_column_config_for_sync` passed with `2 passed`.
- `uv run pytest tests/core/test_transforms.py` passed with `18 passed`.

Concerns / residual risks:

- No dbt 1.11 adapter-backed matrix evidence gathered in this iteration.
- Existing uncommitted ticket implementation and packet files were outside child scope.

Ticket recommendation: Parent can reconcile this coverage iteration as complete and proceed with the ticket's review/acceptance path.

# Parent Merge Notes

Parent accepted the test-only coverage refinement. This resolves the critique's medium finding about the default/fusion-compatible output path for empty injected config blocks.
