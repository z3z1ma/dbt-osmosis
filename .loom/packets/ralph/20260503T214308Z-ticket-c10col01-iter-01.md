---
id: packet:ralph-ticket-c10col01-20260503T214308Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:c10col01
mode: execution
change_class: code-behavior
risk_class: high
style: reference-first
verification_posture: test-first
iteration: 1
created_at: 2026-05-03T21:43:08Z
updated_at: 2026-05-03T21:55:30Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns implementation output; parent reconciles Loom records
  paths:
    - src/dbt_osmosis/core/transforms.py
    - tests/core/test_transforms.py
parent_merge_scope:
  records:
    - ticket:c10col01
    - packet:ralph-ticket-c10col01-20260503T214308Z
  paths:
    - .loom/packets/ralph/20260503T214308Z-ticket-c10col01-iter-01.md
    - .loom/tickets/20260503-c10col01-preserve-columninfo-config.md
source_fingerprint:
  git_commit: 0fd1a17d5e33c15805685d2d290fd531fba476a2
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 0fd1a17d5e33c15805685d2d290fd531fba476a2
  git_status_summary: clean
  git_status_detail: clean before packet directory creation
  compiled_from:
    - ticket:c10col01
    - initiative:dbt-110-111-hardening
    - research:dbt-110-111-api-surfaces
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: sibling_worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed - existing dependency/test commands may use installed package metadata; no remote mutation allowed
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  spec: []
  plan: []
  ticket:
    - ticket:c10col01
links:
  github_issue: https://github.com/z3z1ma/dbt-osmosis/issues/352
---

# Mission

Satisfy ticket:c10col01 by stopping `inject_missing_columns()` from deleting `ColumnInfo.config` while proving injected columns remain serializable and classic YAML output does not leak empty `config` blocks.

# Bound Context

The work is governed by initiative:dbt-110-111-hardening and research:dbt-110-111-api-surfaces. The ticket is high risk because it touches dbt manifest object validity and YAML sync behavior.

Use the sibling worktree `/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening` on branch `loom/dbt-110-111-hardening`. Do not touch unrelated files. Do not run destructive git commands or mutate shared git metadata.

# Parent Launch Checklist

- Target ticket is ready and names one bounded implementation fix.
- Source fingerprint points at `origin/main` commit `0fd1a17d5e33c15805685d2d290fd531fba476a2`.
- Child write scope is limited to `src/dbt_osmosis/core/transforms.py` and `tests/core/test_transforms.py`.
- Verification posture is `test-first` with a focused regression test.
- Parent will reconcile ticket, packet lifecycle, critique/evidence disposition, commit, push, and GitHub issue updates after child output.

# Source Snapshot

Relevant implementation facts:

- `src/dbt_osmosis/core/transforms.py` created `ColumnInfo.from_dict(gen_col)` for injected columns and then deleted `.config` when present.
- dbt-core v1.10.0 and v1.11.0 define `ColumnInfo.config: ColumnConfig = field(default_factory=ColumnConfig)`.
- `ticket:c10meta02` owns broad `config.meta` and `config.tags` behavior. Keep this ticket narrow.
- Existing tests in `tests/core/test_transforms.py` already mock `inject_missing_columns()` database output and assert column case/idempotence behavior.

# Change Class

`code-behavior`, high risk. This has a regression test and still requires critique before closure because it changes core YAML sync object behavior.

# Verification Targets

- ticket:c10col01#ACC-001
- ticket:c10col01#ACC-002
- ticket:c10col01#ACC-003
- ticket:c10col01#ACC-004

# Task For This Iteration

1. Add a failing regression test in `tests/core/test_transforms.py` that injects a missing column and proves the resulting `ColumnInfo` still has a valid `config` attribute and can run through sync serialization.
2. The same test should prove classic serialization does not include an empty `config` block in the resulting YAML-facing output, without broadening into ticket:c10meta02.
3. Implement the smallest code change in `src/dbt_osmosis/core/transforms.py`, expected to be removing the `delattr(..., "config")` behavior.
4. Run a focused red/green test command for the new test and the existing `tests/core/test_transforms.py` module.

# Verification Posture

`test-first`: child reported the focused test failed before implementation with `AttributeError: 'ColumnInfo' object has no attribute 'config'` at `sync_operations.py:81`, then passed after removing the deletion.

# Stop Conditions

- Stop if satisfying the test requires changing shared column serialization beyond this ticket's scope.
- Stop if the correct fix requires supporting `config.meta` or `config.tags` inheritance/read precedence; that belongs to ticket:c10meta02.
- Stop if current source files materially differ from the source snapshot before writing.
- Stop if a targeted red test cannot be produced for the expected reason.

# Output Contract

Return:

- outcome: `continue`, `stop`, `blocked`, or `escalate`
- files changed
- records changed
- red/green evidence with exact commands and observed results
- self-review findings or concerns
- blockers or residual risks
- ticket recommendation

# Working Notes

GitHub issue for this ticket: https://github.com/z3z1ma/dbt-osmosis/issues/352

# Child Output

Outcome: `stop` - bounded implementation iteration completed.

Files changed:

- `src/dbt_osmosis/core/transforms.py` removed deletion of `ColumnInfo.config`.
- `tests/core/test_transforms.py` added regression coverage proving injected columns keep `config` and serialize through `_sync_doc_section` without leaking empty classic-mode `config` output.

Records changed: None.

Evidence gathered:

- Red: `uv run pytest tests/core/test_transforms.py::test_inject_missing_columns_preserves_column_config_for_sync` failed as expected with `AttributeError: 'ColumnInfo' object has no attribute 'config'` at `sync_operations.py:81`.
- Green: `uv run pytest tests/core/test_transforms.py::test_inject_missing_columns_preserves_column_config_for_sync` passed.
- Green: `uv run pytest tests/core/test_transforms.py` passed with `17 passed in 9.57s`.
- Green: `uv run ruff check src/dbt_osmosis/core/transforms.py tests/core/test_transforms.py` passed.
- Green: `uv run ruff format --check src/dbt_osmosis/core/transforms.py tests/core/test_transforms.py` passed.

Self-review findings/concerns:

- Scope stayed within the two allowed files.
- Fix is minimal and leaves sync serialization responsible for filtering `config` from classic YAML output.
- Local tests ran under installed dbt `1.10.20`; no dbt `1.11.x` matrix evidence gathered.

Blockers/residual risks:

- Adapter-backed dbt 1.11 coverage remains outstanding.

Ticket recommendation: Move to critique/review before acceptance because ticket risk class is high and critique policy is mandatory.

# Parent Merge Notes

Parent verified the child changes landed in the sibling worktree, not the original dirty checkout. Parent reran `uv run pytest tests/core/test_transforms.py` from the sibling worktree and observed `17 passed in 9.52s` under Python 3.13.9. Packet consumed; ticket reconciliation and critique are next before acceptance.
