---
id: "packet:ralph-ticket-c10cfg12-20260504T063203Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10cfg12"
mode: execution
change_class: "code-behavior"
risk_class: "high"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T06:32:03Z"
updated_at: "2026-05-04T06:49:30Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T063203Z-ticket-c10cfg12-iter-01.md"
  paths:
    - "tests/core/test_config_resolution.py"
    - "tests/core/test_settings_resolver.py"
    - "tests/core/test_property_accessor.py"
    - "tests/core/test_transforms.py"
    - "tests/core/test_real_config_shapes.py"
    - "tests/support.py"
    - "demo_duckdb/"
parent_merge_scope:
  records:
    - "ticket:c10cfg12"
  paths:
    - ".loom/tickets/20260503-c10cfg12-add-real-dbt-config-shape-fixtures.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "a92a225f0d0f616bc6b7d41788a5542c22e7bc9d"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "a92a225f0d0f616bc6b7d41788a5542c22e7bc9d"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10cfg12 packet compilation"
  compiled_from:
    - "ticket:c10cfg12"
    - "ticket:c10col01"
    - "ticket:c10meta02"
    - "initiative:dbt-110-111-hardening"
    - "research:dbt-110-111-api-surfaces"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal dependency/runtime behavior already present in local test commands; no external service required"
context_budget:
  posture: normal
  max_source_files: 12
  max_excerpt_lines_per_file: 180
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  ticket:
    - "ticket:c10cfg12"
    - "ticket:c10col01"
    - "ticket:c10meta02"
links:
  tickets:
    - ticket:c10cfg12
    - ticket:c10col01
    - ticket:c10meta02
---

# Mission

Add real dbt-parsed fixture coverage for dbt 1.10/1.11 config shapes so `ticket:c10col01#ACC-005` and `ticket:c10meta02#ACC-005` no longer rely only on mocks and broad matrix execution.

# Bound Context

`ticket:c10cfg12` is high-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-001`, `#OBJ-002`, and `#OBJ-004`. The ticket owns exact parsed-fixture evidence gaps converted from closed tickets `ticket:c10col01` and `ticket:c10meta02`.

Stay in fixture/test coverage unless a real parsed fixture exposes a small local compatibility bug directly required to make the test meaningful. If actual resolver behavior fails for real parsed dbt nodes in a way that needs a broader production change, stop and report the failing evidence instead of widening scope into `ticket:c10res14`.

# Source Snapshot

Relevant current state:

- `ticket:c10col01#ACC-005` is accepted with broad dbt 1.10/1.11 matrix coverage, but exact adapter-backed missing-column fixture evidence was converted to `ticket:c10cfg12`.
- `ticket:c10meta02#ACC-005` is accepted with broad dbt 1.10/1.11 matrix coverage, but exact real parsed column `config.meta` / `config.tags` fixture evidence was converted to `ticket:c10cfg12`.
- `research:dbt-110-111-api-surfaces` concludes dbt 1.10/1.11 support `ColumnInfo.config.meta` and `ColumnInfo.config.tags`, while local behavior must verify actual manifest fields from parsed fixtures.
- `tests/core/test_config_resolution.py` has extensive mock-based coverage and one real `yaml_context` test against `orders`, but it still relies mostly on legacy YAML `meta` and mocks.
- `tests/core/test_settings_resolver.py` and `tests/core/test_property_accessor.py` cover column config shapes with mocks.
- `tests/core/test_transforms.py` has missing-column injection tests but not the exact adapter-backed real fixture shape from `c10col01#ACC-005`.
- `tests/support.py` now has `create_temp_project_copy`, `rewrite_duckdb_profile_paths`, and `run_dbt_command`, so a test can build an isolated temp demo fixture without source-tree `demo_duckdb/target`.
- `demo_duckdb` is the canonical fixture, but source fixture semantics should not be changed unless a temp fixture is insufficient. Prefer temp test fixture files created under `tmp_path` or an isolated copied project.

# Change Class

`code-behavior`, high risk, but likely test-only. These tests will define compatibility claims and must be honest across dbt 1.8/1.9/1.10/1.11. Use version-gated assertions if a fixture shape is only valid for dbt 1.10+.

# Verification Targets

- ticket:c10cfg12#ACC-001
- ticket:c10cfg12#ACC-002
- ticket:c10cfg12#ACC-003
- ticket:c10cfg12#ACC-004
- ticket:c10cfg12#ACC-005
- ticket:c10cfg12#ACC-006
- ticket:c10col01#ACC-005
- ticket:c10meta02#ACC-005

# Task For This Iteration

Implement the smallest real parsed-fixture coverage that satisfies the ticket:

- Add tests that create or use a real dbt project fixture and run `dbt parse` through dbt itself, not mocks.
- Prefer a new focused test file such as `tests/core/test_real_config_shapes.py` with an isolated temp demo copy and additional temp fixture model/source files.
- Include node YAML `config: meta:`, SQL `{{ config(meta={...}) }}`, legacy custom config such as `{{ config(dbt_osmosis_...) }}` if useful, and column `config.meta` / `config.tags`.
- Assert actual manifest fields: `node.config.meta`, `node.config.extra`, `node.unrendered_config`, and column `config.meta` / `config.tags` for dbt 1.10+.
- Assert `SettingsResolver` and `PropertyAccessor` see real parsed column config metadata/tags for the fixture node.
- Cover the converted c10col01 gap with an adapter-backed missing-column injection path under the real parsed fixture, proving injected missing columns retain dbt `ColumnInfo.config` and can be sync-serialized.
- Keep dbt 1.8/1.9 compatibility honest. If dbt < 1.10 cannot parse the new shape, gate tests with `pytest.skip` and assert/log the installed dbt version in the skipped/passing tests.
- Avoid source-tree `demo_duckdb/target`, repo-root `test.db`, and destructive Git cleanup.

# Verification Posture

`test-first`:

- Red check: add focused parsed-fixture tests first and run them before implementation/fixture support. Expected red can be missing test fixture helpers, absent parsed fixture shape, or assertion failure showing no real parsed config-shape coverage exists yet. Record the command and failure output.
- Green check: after adding the fixture/helper changes, rerun the focused tests. Include at minimum `uv run pytest tests/core/test_real_config_shapes.py -q` or equivalent.
- Broader check: run relevant existing tests such as `uv run pytest tests/core/test_config_resolution.py tests/core/test_settings_resolver.py tests/core/test_property_accessor.py tests/core/test_transforms.py tests/core/test_real_config_shapes.py -q` if feasible, plus targeted pre-commit for changed files and `git diff --check`.

# Stop Conditions

- Stop if real parsed fixture coverage requires changing production resolver behavior beyond a small local compatibility fix; report the failing evidence and recommend routing to `ticket:c10res14` or another owner.
- Stop if the desired fixture shape cannot parse under dbt 1.10/1.11 with the current dbt-duckdb pairing after one reasonable investigation.
- Stop if satisfying the ticket requires source-tree generated dbt artifacts or repo-root DuckDB files.
- Stop if changing demo source fixture semantics would be broader than adding a focused isolated test fixture.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: test(s), command, failure output, and why it failed for the expected coverage gap
- green evidence: focused and broader tests, hooks/checks, installed dbt version observation, and artifact isolation notes
- implementation summary and rationale for fixture shape/version gating
- disposition of converted c10col01/c10meta02 gaps
- residual risks
- ticket recommendation

# Working Notes

Prefer test-local fixture creation over permanent demo fixture mutations. If adding permanent demo files is simpler, keep them narrowly named for dbt 1.10 config-shape coverage and verify integration smoke still writes only temp copies.

# Child Output

Outcome: `stop` — the bounded parsed-fixture test iteration is implemented and verified locally.

Files changed:

- `tests/core/test_real_config_shapes.py` — added real dbt-parsed temp demo fixture tests for dbt 1.10+ model/column config shapes and adapter-backed missing-column injection serialization.

Records changed:

- `.loom/packets/ralph/20260504T063203Z-ticket-c10cfg12-iter-01.md` — appended this Child Output only.

Red evidence:

- Command: `uv run pytest tests/core/test_real_config_shapes.py -q`
- Result: expected red, `2 failed in 0.19s`.
- Failure output: both new tests failed with `NameError: name '_parsed_config_shape_fixture' is not defined`.
- Why expected: the real parsed-fixture tests existed first, but the fixture/helper support had not been implemented yet, matching the test-first packet allowance for missing fixture helpers.

Green evidence:

- Installed dbt observation: `uv run dbt --version` reported dbt-core `1.10.20` and dbt-duckdb `1.10.0`.
- Focused command: `uv run pytest tests/core/test_real_config_shapes.py -q`
- Focused result: `2 passed in 3.59s`.
- Broader command: `uv run pytest tests/core/test_config_resolution.py tests/core/test_settings_resolver.py tests/core/test_property_accessor.py tests/core/test_transforms.py tests/core/test_real_config_shapes.py -q`
- Broader result: `102 passed, 3 skipped in 14.64s`; the skips were existing demo fixture setup skips in `tests/core/test_property_accessor.py`.
- Artifact isolation: tests copy `demo_duckdb` into `tmp_path` with generated artifacts excluded, rewrite DuckDB profile paths into the temp project, write the added model/YAML only in the temp copy, run dbt parse/run against the temp project, and do not use source-tree `demo_duckdb/target` or repo-root DuckDB files.
- Targeted pre-commit: `pre-commit run --files tests/core/test_real_config_shapes.py .loom/packets/ralph/20260504T063203Z-ticket-c10cfg12-iter-01.md` passed all applicable hooks.
- Whitespace check: `git diff --check` passed with no output.

Implementation summary and rationale:

- Added `tests/core/test_real_config_shapes.py` with a temp copied `demo_duckdb` fixture that writes a narrowly scoped `real_config_shape` model and schema YAML, then invokes real dbt commands through `run_dbt_command`.
- The fixture includes YAML node `config.meta`, SQL `{{ config(meta=..., dbt_osmosis_skip_add_columns=false) }}`, legacy/custom config captured in `node.config.extra`, and column `config.meta` / `config.tags`.
- The parsed-shape test asserts actual manifest object fields (`node.config.meta`, `node.config.extra`, `node.unrendered_config`, column `config.meta`, column `config.tags`) and verifies `SettingsResolver` plus `PropertyAccessor` see the real parsed column metadata/tags.
- The missing-column test builds the temp model in DuckDB, calls real adapter-backed `inject_missing_columns`, proves the injected `ColumnInfo` has non-`None` `config`, and sync-serializes the node through `_sync_doc_section` while retaining the existing parsed column config and omitting empty config from the injected column.
- Tests gate with `pytest.skip` for dbt-core `<1.10.0` because the asserted column config shape is a dbt 1.10+ compatibility claim.

Disposition of converted gaps:

- `ticket:c10meta02#ACC-005`: covered locally by real dbt parse assertions for column `config.meta` / `config.tags`, `node.config.meta`, `node.config.extra`, `node.unrendered_config`, `SettingsResolver`, and `PropertyAccessor` on dbt-core `1.10.20`.
- `ticket:c10col01#ACC-005`: covered locally by a real DuckDB adapter-backed missing-column injection path that proves injected columns retain dbt `ColumnInfo.config` and can be sync-serialized.

Residual risks:

- Local execution observed dbt-core `1.10.20`, not dbt-core `1.11.x`; dbt 1.11 confidence still depends on the broader matrix/CI path.
- dbt-core `<1.10` intentionally skips these shape assertions because the fixture targets the 1.10+ column config namespace.
- The implementation is test-only and did not change production resolver behavior.

Ticket recommendation:

- Parent should accept this iteration, record evidence for `ticket:c10cfg12`, and route to the required/recommended high-risk critique or acceptance review. No further Ralph iteration is recommended unless parent wants local dbt 1.11 evidence in addition to matrix/CI coverage.

# Parent Merge Notes

Parent accepted the Ralph iteration as in scope and added a critique-driven precondition assertion to the missing-column injection test before committing. Local validation after the precondition patch passed focused real config-shape tests, the broader config/property/transform suite, source-artifact guards, targeted pre-commit, and `git diff --check`; durable details are recorded in `evidence:c10cfg12-real-config-shape-fixtures`.

Mandatory critique `critique:c10cfg12-real-config-shape-fixtures` found no remaining implementation blocker after the precondition fix. It keeps one high finding open for the ticket acceptance gate: local evidence is dbt 1.10 only, so dbt 1.11 converted fixture claims must wait for post-commit matrix CI or equivalent evidence.

Ticket reconciliation moved `ticket:c10cfg12` to `complete_pending_acceptance` with local claims supported and post-commit dbt 1.11 CI plus retrospective / promotion follow-through still pending.
