---
id: "packet:ralph-ticket-c10sql21-20260504T134541Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10sql21"
mode: execution
change_class: "code-behavior"
risk_class: "high"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T13:45:41Z"
updated_at: "2026-05-04T14:13:06Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T134541Z-ticket-c10sql21-iter-01.md"
  paths:
    - "src/dbt_osmosis/core/sql_operations.py"
    - "src/dbt_osmosis/cli/main.py"
    - "src/dbt_osmosis/workbench/app.py"
    - "tests/core/test_sql_operations.py"
    - "tests/core/test_cli.py"
    - "tests/core/test_workbench*.py"
parent_merge_scope:
  records:
    - "ticket:c10sql21"
  paths:
    - ".loom/tickets/20260503-c10sql21-fix-sql-compile-contract-coverage.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "34b24c8b8976db24525b2293601f7fa0b77fdbe2"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "34b24c8b8976db24525b2293601f7fa0b77fdbe2"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10sql21 packet compilation"
  compiled_from:
    - "ticket:c10sql21"
    - "initiative:dbt-110-111-hardening"
    - "research:dbt-110-111-api-surfaces"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal local dependency/runtime behavior; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 8
  max_excerpt_lines_per_file: 320
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  evidence:
    - evidence:oracle-backlog-scan
  ticket:
    - "ticket:c10sql21"
links:
  tickets:
    - ticket:c10sql21
---

# Mission

Fix the SQL compile/run contract so plain SQL yields executable SQL for CLI/workbench callers, and add real fixture coverage around the private dbt SQL parser/task path.

# Bound Context

`ticket:c10sql21` is high-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-001` and `#OBJ-006`. It is constrained by `research:dbt-110-111-api-surfaces`, which found dbt 1.10.0/1.11.0 private SQL parser/task signatures still match but need real behavioral tests.

Use local validation for this ticket. Do not wait on GitHub Actions per ticket; final initiative validation will handle CI later. Because this ticket is high risk, parent will run mandatory critique before acceptance.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/core/sql_operations.py::compile_sql_code()` returns the raw `SqlNode` for plain SQL without setting `compiled_code`.
- `tests/core/test_sql_operations.py::test_compile_sql_code_no_jinja` asserts the broken contract with `node.compiled_code is None`.
- `src/dbt_osmosis/cli/main.py::sql compile` prints `node.compiled_code`, so plain SQL can print `None`.
- `src/dbt_osmosis/workbench/app.py::compile()` returns `compile_sql_code(ctx, sql).compiled_code or ""`, so plain scratch SQL can become blank.
- `execute_sql_code()` already executes raw SQL directly when no Jinja is present, but needs real fixture coverage and cleanup verification.
- Temporary manifest nodes are keyed as `SqlOperation.<project>.<uuid>` and cleanup currently runs in a `finally` block.

# Change Class

`code-behavior`, high risk. The work changes user-facing SQL compile output and test coverage around private dbt APIs.

# Verification Targets

- ticket:c10sql21#ACC-001
- ticket:c10sql21#ACC-002
- ticket:c10sql21#ACC-003
- ticket:c10sql21#ACC-004
- ticket:c10sql21#ACC-005
- ticket:c10sql21#ACC-006
- initiative:dbt-110-111-hardening#OBJ-001
- initiative:dbt-110-111-hardening#OBJ-006

# Task For This Iteration

Implement the smallest safe fix that satisfies the ticket:

- Add failing tests first that prove plain SQL compile returns executable SQL instead of `None`, CLI `sql compile "select 1"` emits SQL, and workbench compile preserves the default/raw scratch SQL.
- Add real dbt fixture tests using `yaml_context.project` for plain SQL compile, Jinja/ref SQL compile, executing `select 1`, and temporary manifest node cleanup after success and after parser/compile failure.
- Update tests that currently assert `compiled_code is None`; that assertion encodes user-visible breakage.
- Prefer setting or normalizing `compiled_code` on the returned node for plain SQL so existing callers can keep using `compiled_code` without special-casing raw SQL.
- If a helper is clearer, keep it private and use it consistently in CLI/workbench, but avoid a broad SQL subsystem rewrite.
- Keep private dbt API usage isolated in `sql_operations.py`; do not widen this ticket into replacing all dbt private APIs unless strictly necessary.

# Verification Posture

`test-first`:

- Red checks: write targeted tests first and show they fail because plain compile produces `None`/blank output or cleanup is not verified.
- Green focused checks: run `tests/core/test_sql_operations.py` and any CLI/workbench tests changed or added.
- Broader local checks: run adjacent CLI/workbench smoke tests if touched. Run Ruff format/check, `git diff --check`, and targeted pre-commit.
- Matrix note: tests should be real fixture tests that run under whichever dbt version is installed locally and under the initiative CI matrix for dbt 1.10.x/1.11.x. Do not require switching local environments inside this packet.

# Stop Conditions

- Stop and report `blocked` if dbt 1.10/1.11 adapter availability prevents a meaningful fixture test from being added.
- Stop and report `escalate` if fixing plain SQL compile requires a breaking return-type change for `compile_sql_code()`.
- Stop if a correct fix requires replacing `SqlBlockParser` or `SqlCompileRunner` with a broad compatibility abstraction beyond this ticket.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves the SQL compile contract is broken
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including plain SQL contract, CLI/workbench behavior, real dbt fixture coverage, and manifest cleanup behavior
- residual risks
- ticket recommendation

# Working Notes

The narrowest likely fix is to ensure plain SQL returns a `ManifestSQLNode` whose `compiled_code` is the raw SQL while still avoiding `process_node()`/`SqlCompileRunner` on non-Jinja SQL. Preserve the existing `finally` cleanup shape and add tests for success and failure cleanup rather than broad manifest refactors.

# Child Output

outcome: stop

files changed:

- `src/dbt_osmosis/core/sql_operations.py`
- `tests/core/test_sql_operations.py`
- `tests/core/test_cli.py`
- `tests/core/test_workbench_app.py`

records changed:

- `.loom/packets/ralph/20260504T134541Z-ticket-c10sql21-iter-01.md`

red evidence:

- `uv run pytest tests/core/test_sql_operations.py::test_compile_sql_code_no_jinja tests/core/test_cli.py::test_sql_compile_plain_sql_outputs_sql tests/core/test_workbench_app.py::test_workbench_compile_preserves_raw_scratch_sql -q` failed before implementation with 3 failures:
  - `test_compile_sql_code_no_jinja`: `node.compiled_code` was `None` instead of `SELECT 1 AS mycol`.
  - `test_sql_compile_plain_sql_outputs_sql`: CLI `sql compile "select 1"` exited 0 but final printed line was `None` instead of `select 1`.
  - `test_workbench_compile_preserves_raw_scratch_sql`: workbench `compile(default_prompt)` returned `""` instead of the raw scratch SQL.
- This proves the broken contract was that plain SQL parsed successfully but did not populate `compiled_code`, causing existing CLI/workbench callers that read `compiled_code` to print `None` or return blank.

green evidence:

- `uv run pytest tests/core/test_sql_operations.py tests/core/test_cli.py::test_sql_compile_plain_sql_outputs_sql tests/core/test_workbench_app.py -q` passed: 9 passed.
- `uv run pytest tests/core/test_cli.py tests/core/test_workbench_app.py -q` passed: 18 passed.
- `uv run pytest tests/core/test_sql_operations.py tests/core/test_cli.py tests/core/test_workbench_app.py -q` passed: 25 passed.
- `uv run ruff format src/dbt_osmosis/core/sql_operations.py tests/core/test_sql_operations.py tests/core/test_cli.py tests/core/test_workbench_app.py` completed with 4 files unchanged.
- `uv run ruff check src/dbt_osmosis/core/sql_operations.py tests/core/test_sql_operations.py tests/core/test_cli.py tests/core/test_workbench_app.py` passed.
- `git diff --check` passed with no output.
- `uv run pre-commit run --files src/dbt_osmosis/core/sql_operations.py tests/core/test_sql_operations.py tests/core/test_cli.py tests/core/test_workbench_app.py` passed all applicable hooks.

implementation summary:

- Plain SQL compile now normalizes the parsed SQL operation node by setting `node.compiled_code = node.raw_code` while still skipping `process_node()` and `SqlCompileRunner` for non-Jinja SQL.
- CLI `sql compile` and workbench `compile()` require no source special-casing; they now receive executable SQL through the existing `compiled_code` contract.
- Real fixture coverage now exercises plain SQL compile, Jinja/ref compile through dbt's parser/compile path, `execute_sql_code("select 1 as value")`, and temporary manifest SQL operation cleanup after successful ref compile and forced compile failure.
- Workbench compile coverage uses local import stubs for optional UI dependencies that are not installed in the dev environment, then exercises the real `dbt_osmosis.workbench.app.compile()` function against `yaml_context.project`.

residual risks:

- Local validation ran with installed dbt 1.10.20 and duckdb adapter; dbt 1.11 matrix validation remains for the parent/initiative CI path.
- Workbench optional dependencies (`streamlit`, `pandas`, etc.) were not installed locally, so the workbench test stubs import-time UI dependencies and validates only the compile helper contract, not full Streamlit rendering.
- `uv` emitted a benign warning that the active `VIRTUAL_ENV` path pointed at a sibling checkout and was ignored.

ticket recommendation:

- Move `ticket:c10sql21` to review/acceptance follow-through with mandatory critique next; no additional Ralph implementation iteration appears necessary for this bounded SQL compile/run contract slice.

# Parent Merge Notes

Parent accepted the child output as in scope and complete, then hardened workbench coverage after mandatory critique found ACC-003 incomplete:

- Re-ran child focused SQL/CLI/workbench checks.
- Added parent-side workbench `run_query()` regression that uses `Preview.initial_state()` default query template and the default scratch SQL.
- Removed the cached stub-imported workbench app and preview modules from `sys.modules` after each test import to avoid poisoning later imports.
- Clarified `compile_sql_code()` docstring to state that returned `compiled_code` is executable SQL.
- Re-ran related SQL/CLI/workbench/legacy/model-validation suites: `39 passed`.
- Ran Ruff format/check, `git diff --check`, and targeted pre-commit successfully.
- Ran final mandatory read-only critique; no findings remained.
- Committed implementation as `098d4d2188706c32b49b2cc0eba16100018e41df`.
- Recorded evidence in `evidence:c10sql21-sql-compile-contract-validation`.
- Recorded final critique in `critique:c10sql21-sql-compile-contract-review`.

Outcome: consumed; ticket reconciliation completed in `ticket:c10sql21`.
