---
id: "packet:ralph-ticket-c10docs09-20260504T025259Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10docs09"
mode: execution
change_class: "release-packaging"
risk_class: "high"
style: "reference-first"
verification_posture: "observation-first"
iteration: "1"
created_at: "2026-05-04T02:52:59Z"
updated_at: "2026-05-04T03:07:28Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns output only; parent reconciles ticket, evidence, critique, wiki, and packet status"
  paths:
    - "docs/docusaurus.config.js"
    - "docs/package.json"
    - "docs/package-lock.json"
    - ".github/workflows/tests.yml"
    - ".github/dependabot.yml"
parent_merge_scope:
  records:
    - "ticket:c10docs09"
    - "packet:ralph-ticket-c10docs09-20260504T025259Z"
  paths:
    - ".loom/tickets/20260503-c10docs09-fix-docs-build-ci.md"
    - ".loom/packets/ralph/20260504T025259Z-ticket-c10docs09-iter-01.md"
source_fingerprint:
  git_commit: "5512c32faa57f5dd615a5eeece585e0b18b5b967"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "5512c32faa57f5dd615a5eeece585e0b18b5b967"
  git_status_summary: "clean"
  git_status_detail: "clean before packet compilation"
  compiled_from:
    - "ticket:c10docs09"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: sibling_worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed - npm install/build and package metadata checks may need registry access"
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: false
sources:
  constitution:
    - constitution:main
  initiative:
    - "initiative:dbt-110-111-hardening"
  research: []
  spec: []
  plan: []
  ticket:
    - "ticket:c10docs09"
links:
  issue: "https://github.com/z3z1ma/dbt-osmosis/issues/360"
---

# Mission

Make the docs site build surface consistent and validated in CI for `ticket:c10docs09` without broad docs content or visual redesign.

# Bound Context

`ticket:c10docs09` owns this work. It is high-risk release-packaging work because docs build failure can block releases and migration guidance, and dependency changes affect the supply-chain surface.

# Parent Launch Checklist

- Target ticket is `ready`, high risk, and has bounded acceptance criteria for docs build CI.
- Child write scope is limited to docs config/package files, `.github/workflows/tests.yml`, and `.github/dependabot.yml`.
- Parent keeps ticket/evidence/critique/wiki/packet reconciliation.
- Source fingerprint matches `origin/main` and the worktree was clean before packet compilation.
- Verification posture is observation-first because the work is CI/config/dependency behavior that can be validated with before/after local commands and workflow inspection.

# Source Snapshot

Current docs/config shape before this packet:

- `docs/docusaurus.config.js` uses ESM `import {themes as prismThemes} from 'prism-react-renderer';` but still uses `require.resolve(...)` and `module.exports = config`.
- `docs/package.json` pins Docusaurus packages at `3.7.0` and uses `react` / `react-dom` `^19.0.0`.
- `.github/workflows/tests.yml` has Python/dbt tests and pip smoke jobs but no PR docs build job.
- `.github/workflows/release.yml` now builds docs in the release validation job, but that only runs after successful push `Tests` on `main`; it does not satisfy pull-request docs CI coverage.
- `.github/dependabot.yml` only covers pip at repository root and does not cover docs npm dependencies.

Ticket acceptance requires:

- `ticket:c10docs09#ACC-001` - `npm --prefix docs ci` succeeds on Node 18 and current LTS.
- `ticket:c10docs09#ACC-002` - `npm --prefix docs run build` succeeds in CI.
- `ticket:c10docs09#ACC-003` - Docusaurus config no longer mixes incompatible ESM/CJS runtime patterns.
- `ticket:c10docs09#ACC-004` - React and Docusaurus peer dependencies are valid according to `npm ls` or equivalent.
- `ticket:c10docs09#ACC-005` - CI includes a docs build job on pull requests.
- `ticket:c10docs09#ACC-006` - Dependabot or another update mechanism covers docs npm dependencies.

# Change Class

`release-packaging`, high risk. Minimize the diff. Do not rewrite docs content or redesign the docs site.

# Verification Targets

- `ticket:c10docs09#ACC-001`
- `ticket:c10docs09#ACC-002`
- `ticket:c10docs09#ACC-003`
- `ticket:c10docs09#ACC-004`
- `ticket:c10docs09#ACC-005`
- `ticket:c10docs09#ACC-006`

# Task For This Iteration

Update only files in child write scope.

Required shape:

- Convert Docusaurus config to a consistent runtime module style. Prefer the smallest change that matches the existing file and Docusaurus 3 behavior.
- Fix or verify the React / Docusaurus peer dependency relationship. If dependencies change, update `docs/package-lock.json` through npm, not manual lockfile editing.
- Add a docs CI job to `.github/workflows/tests.yml` that runs on pull requests through the existing workflow trigger. It should run `npm --prefix docs ci`, a peer/dependency validation check such as `npm --prefix docs ls`, and `npm --prefix docs run build`.
- CI should validate Node 18 and current LTS. At the time of this packet, prefer Node 18 plus Node 24 unless local/npm evidence shows the selected docs stack cannot support Node 24; if so, document why and choose the strongest honest current-LTS approximation.
- Add Dependabot coverage for `docs/` npm dependencies.
- Keep release workflow docs validation intact unless a minimal shared improvement is necessary.

# Verification Posture

Observation-first.

Before changing, observe and report the current state for at least:

- Docusaurus config module style issue by file inspection.
- Current docs dependency status with `npm --prefix docs ls` if feasible.
- Current docs install/build status with `npm --prefix docs ci` and `npm --prefix docs run build` if feasible.

After changing, run the smallest honest local checks available:

- `npm --prefix docs ci`.
- `npm --prefix docs ls` or an equivalent peer/dependency check.
- `npm --prefix docs run build`.
- `pre-commit run --files docs/docusaurus.config.js docs/package.json docs/package-lock.json .github/workflows/tests.yml .github/dependabot.yml`.

Do not poll GitHub Actions repeatedly. Parent will check CI only after a major push if needed.

# Stop Conditions

- Stop if satisfying peer dependencies requires a broad Docusaurus major upgrade with content migrations beyond this ticket.
- Stop if `docs/` build failure is caused by broad content rot rather than config/dependency/CI setup.
- Stop if changes outside child write scope are required.
- Stop rather than expanding into release workflow redesign; `ticket:c10rel08` already owns that surface.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- before/after observations
- exact CI/docs dependency shape after the change
- evidence gathered, including commands and pass/fail results
- self-review findings or concerns
- blockers or residual risks
- ticket recommendation

# Working Notes

Issue for this ticket is expected to be GitHub issue #360.

# Child Output

Outcome: stop.

Files changed:

- `docs/docusaurus.config.js`
- `docs/package.json`
- `docs/package-lock.json`
- `.github/workflows/tests.yml`
- `.github/dependabot.yml`

Before observations:

- `docs/docusaurus.config.js` mixed ESM `import` with CommonJS `require.resolve` / `module.exports`.
- `npm --prefix docs ls` initially failed because `node_modules` was absent.
- `npm --prefix docs ci` passed before change but emitted React 19 peer override warnings.
- `npm --prefix docs run build` passed before change.

After observations:

- Docusaurus config is consistently CommonJS.
- React dependencies are pinned to `^18.3.1`; `npm ci` no longer emits React peer override warnings.
- Docs install, dependency validation, and build pass locally.

Exact CI/docs dependency shape after child change:

- `Tests` workflow includes a `docs` job.
- Docs CI matrix: Node `18` and `24`.
- Docs CI commands: `npm --prefix docs ci`, `npm --prefix docs ls`, `npm --prefix docs run build`.
- Docusaurus packages remain `3.7.0`.
- `react` and `react-dom` are `^18.3.1`.
- Dependabot covers npm dependencies in `/docs`.

Evidence gathered by child:

- `node --version && npm --version && npm --prefix docs ls`: before install, `npm ls` failed with missing dependencies; local Node was `v22.22.1`, npm `10.9.4`.
- `npm --prefix docs ci`: before change passed with React 19 peer override warnings.
- `npm --prefix docs run build`: before change passed.
- `npm --prefix docs ci`: after change passed; 1292 packages installed; 30 audit vulnerabilities reported.
- `npm --prefix docs ls`: after change passed with React/React DOM `18.3.1`.
- `npm --prefix docs run build`: after change passed.
- `pre-commit run --files docs/docusaurus.config.js docs/package.json docs/package-lock.json .github/workflows/tests.yml .github/dependabot.yml`: passed.
- `git diff --stat -- ...`: scoped diff was 5 files, 62 insertions, 20 deletions.

Self-review concerns / residual risks:

- Local verification used Node `v22.22.1`; Node 18/24 validation is encoded in CI but not locally executed.
- `npm audit` still reports existing vulnerabilities; this iteration did not broaden into dependency/security remediation.

Ticket recommendation: move `ticket:c10docs09` to review/acceptance path; implementation scope is complete and ready for parent reconciliation plus high-risk critique/CI confirmation.

# Parent Merge Notes

Parent inspected the workflow and docs diffs, changed the new docs CI job to use `actions/setup-node@v4.0.4` for consistency with Release, added npm cache configuration using `docs/package-lock.json`, reran `npm --prefix docs ci`, `npm --prefix docs ls`, `npm --prefix docs run build`, and `pre-commit run --files docs/docusaurus.config.js docs/package.json docs/package-lock.json .github/workflows/tests.yml .github/dependabot.yml`; recorded `evidence:c10docs09-local-docs-ci-validation`; and is routing to mandatory critique before commit/push acceptance.

Mandatory critique passed for commit/push trial in `critique:c10docs09-docs-ci-hardening`. It raised one low finding that `docs/package-lock.json` drifted the peer-only TypeScript lock entry from `5.7.2` to `6.0.3`; parent inspected `npm --prefix docs explain typescript` and recorded the drift as documented non-blocking residual rather than expanding scope. Node 18/24 GitHub Actions docs evidence remains pending before ticket acceptance.
