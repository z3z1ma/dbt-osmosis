---
id: packet:ralph-ticket-gh305wb-20260505T081714Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:gh305wb
mode: execution
change_class: release-packaging
risk_class: medium
style: reference-first
verification_posture: test-first
iteration: 1
created_at: 2026-05-05T08:17:14Z
updated_at: 2026-05-05T08:25:59Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - packet:ralph-ticket-gh305wb-20260505T081714Z
  paths:
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/pyproject.toml
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/uv.lock
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/src/dbt_osmosis/cli/main.py
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/tests/core/test_cli.py
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/tests/test_package_metadata.py
parent_merge_scope:
  records:
    - ticket:gh305wb
  paths:
    - .loom/evidence/
    - .loom/critique/
    - .loom/tickets/20260505-gh305wb-fix-workbench-extra-preflight.md
    - .loom/packets/ralph/20260505T081714Z-ticket-gh305wb-iter-01.md
source_fingerprint:
  git_commit: 72301c237cee12fff994747651d76e8d2563e74c
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 72301c237cee12fff994747651d76e8d2563e74c
  git_status_summary: clean_for_gh305_scope
  git_status_detail: prior issue tickets are committed and pushed; gh305 ticket record only will be updated by parent packet setup
  compiled_from:
    - ticket:gh305wb
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed_for_uv_lock_only
context_budget:
  posture: normal
  max_source_files: 6
  max_excerpt_lines_per_file: 140
  avoid_full_file_reads: true
sources:
  constitution: []
  initiative:
    - initiative:issue-pr-zero
  research: []
  spec: []
  plan: []
  ticket:
    - ticket:gh305wb
links: {}
---

# Mission

Make the workbench optional extra install and preflight cleanly when `ydata_profiling` needs IPython, while keeping OpenAI optional for workbench startup.

# Bound Context

This packet advances only `ticket:gh305wb`. It should not implement real workbench AI, redesign the Streamlit UI, or add OpenAI to the workbench extra.

# Parent Launch Checklist

- Target ticket is ready and scoped to packaging/preflight behavior.
- Child write scope is limited to optional dependency metadata, workbench preflight, package/CLI tests, and lockfile if needed.
- Verification posture is test-first.

# Source Snapshot

- `pyproject.toml` `workbench` extra includes `ydata-profiling` but not `ipython`.
- `src/dbt_osmosis/workbench/app.py` imports `ydata_profiling` at module import time.
- `src/dbt_osmosis/cli/main.py` currently checks `_WORKBENCH_APP_MODULES` with `importlib.util.find_spec()`, which does not catch transitive import failures like missing `IPython`.
- Existing `tests/core/test_cli.py` has workbench smoke/preflight tests around missing Streamlit and missing top-level dependency hints.
- Existing `tests/test_package_metadata.py` checks optional extras and workbench requirements.

# Change Class

`release-packaging`, medium risk because optional extras and lock metadata affect user installs and preflight behavior.

# Verification Targets

- ticket:gh305wb#ACC-001
- ticket:gh305wb#ACC-002
- ticket:gh305wb#ACC-003
- ticket:gh305wb#ACC-004
- ticket:gh305wb#ACC-005

# Task For This Iteration

Add the minimal package metadata and CLI preflight changes so `dbt-osmosis[workbench]` installs the transitive dependency needed for `ydata_profiling` startup and the CLI catches transitive workbench import failures with the workbench extra hint before launching Streamlit. Keep OpenAI out of workbench startup requirements.

# Verification Posture

`test-first`: add failing package/preflight tests first, observe red, implement the minimal fix, update/validate the lockfile if dependency metadata changes, then run focused CLI/package metadata tests, Ruff checks, `uv lock --check`, and `git diff --check`.

# Stop Conditions

Stop if the fix requires adding OpenAI to the workbench extra, implementing real AI assistant behavior, broad workbench import restructuring, or dependency changes unrelated to the workbench optional extra.

# Output Contract

Return outcome, files changed, red evidence, green evidence, risks/blockers, and ticket recommendation.

# Working Notes

Prefer importing the declared workbench modules in preflight so transitive import errors are caught before launching Streamlit. Keep the error message clear and include `pip install dbt-osmosis[workbench]`.

# Child Output

Outcome: stop.

Files changed:
- `pyproject.toml`
- `uv.lock`
- `src/dbt_osmosis/cli/main.py`
- `tests/core/test_cli.py`
- `tests/test_package_metadata.py`
- `.loom/packets/ralph/20260505T081714Z-ticket-gh305wb-iter-01.md`

Red evidence:
- `uv run pytest tests/core/test_cli.py::test_workbench_transitive_import_failure_has_workbench_extra_hint tests/test_package_metadata.py::test_base_dependencies_and_optional_extras_are_intentional` -> 2 failed as expected: transitive `IPython` import failure was not caught before Streamlit launch, and `ipython` was absent from the workbench extra.

Green evidence:
- `uv run pytest tests/core/test_cli.py tests/test_package_metadata.py` -> 45 passed, 2 warnings.
- `uv run ruff check src/dbt_osmosis/cli/main.py tests/core/test_cli.py tests/test_package_metadata.py` -> All checks passed.
- `uv lock --check` -> Resolved 179 packages in 6ms.
- `git diff --check` -> passed with no output.

Blockers or residual risks:
- No blockers. Full repository test suite was not run; focused packet-required tests passed.

Recommended ticket disposition:
- Move to `review_required` or parent acceptance follow-through; this bounded implementation is complete and medium-risk packaging/preflight change likely warrants critique/review.

# Parent Merge Notes

Parent reran focused CLI/package tests, Ruff, format check, `uv lock --check`, and `git diff --check`; Oracle critique returned `accept` with no findings. This packet is consumed as the implementation iteration for `ticket:gh305wb`.
