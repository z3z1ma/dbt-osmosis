---
id: "packet:ralph-ticket-c10wb22-20260504T141941Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10wb22"
mode: execution
change_class: "code-behavior"
risk_class: "high"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T14:19:41Z"
updated_at: "2026-05-04T14:43:11Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T141941Z-ticket-c10wb22-iter-01.md"
  paths:
    - "src/dbt_osmosis/cli/main.py"
    - "src/dbt_osmosis/workbench/app.py"
    - "tests/core/test_cli.py"
    - "tests/core/test_workbench_app.py"
    - "tests/core/test_workbench*.py"
parent_merge_scope:
  records:
    - "ticket:c10wb22"
  paths:
    - ".loom/tickets/20260503-c10wb22-fix-workbench-server-deps-target.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "a39da58b0820266bf2a8b46f1fd2f768b8f034e4"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "a39da58b0820266bf2a8b46f1fd2f768b8f034e4"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10wb22 packet compilation"
  compiled_from:
    - "ticket:c10wb22"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal local dependency/runtime behavior; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 8
  max_excerpt_lines_per_file: 320
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  evidence:
    - evidence:oracle-backlog-scan
  ticket:
    - "ticket:c10wb22"
links:
  tickets:
    - ticket:c10wb22
---

# Mission

Fix workbench Streamlit server binding flags, optional dependency failures, and target switching so users see truthful server/dbt behavior.

# Bound Context

`ticket:c10wb22` is high-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-006`. The ticket follows `ticket:c10sql21`, which fixed the SQL compile contract used by workbench compile/run helpers.

Use local validation only for this ticket. Do not wait on GitHub Actions per ticket; final initiative validation will handle CI later. Because this ticket is high risk, parent will run mandatory critique before acceptance.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/cli/main.py::workbench()` passes Streamlit `--browser.serverAddress` and `--browser.serverPort`, which control browser behavior rather than server bind address/port.
- The workbench CLI invokes `streamlit` directly, so missing optional workbench dependencies fail as low-level `FileNotFoundError`/import errors instead of a clear install hint.
- `--options` and `--config` paths also invoke `streamlit` directly.
- `src/dbt_osmosis/workbench/app.py::change_target()` mutates `ctx.runtime_cfg.target_name` and calls `_reload_manifest(ctx)`, but does not rebuild credentials/adapter/context for the selected target.
- Existing `tests/core/test_workbench_app.py` already has import stubs for optional workbench UI dependencies and can be extended.

# Change Class

`code-behavior`, high risk. The work changes user-facing server invocation and target-switching behavior.

# Verification Targets

- ticket:c10wb22#ACC-001
- ticket:c10wb22#ACC-002
- ticket:c10wb22#ACC-003
- ticket:c10wb22#ACC-004
- ticket:c10wb22#ACC-005
- ticket:c10wb22#ACC-006
- initiative:dbt-110-111-hardening#OBJ-006

# Task For This Iteration

Implement the smallest safe fix that satisfies the ticket:

- Add failing CLI tests first for `dbt-osmosis workbench --host 0.0.0.0 --port 8502` expecting Streamlit `server.address=0.0.0.0` and `server.port=8502` server flags while preserving pass-through args after `--`.
- Add failing CLI tests for missing Streamlit on normal, `--options`, and `--config` paths. The failure should be a clear `ClickException` or Click-visible error with an install command such as `pip install dbt-osmosis[workbench]` or equivalent.
- Do not import Streamlit at `src/dbt_osmosis/cli/main.py` module import time.
- Add workbench target-switch tests with mocked/stubbed `create_dbt_project_context`, `_parse_args`, old adapter connection closing/release, and compile behavior. Verify target change rebuilds `DbtProjectContext` with selected target, swaps state to the new context, closes/releases old adapter connections safely, recompiles the current query, and leaves old target/context active with a clear error on failure.
- Keep pass-through Streamlit args after `--` intact.
- Avoid launching a real browser or Streamlit server in tests.

# Verification Posture

`test-first`:

- Red checks: write targeted tests first and show they fail because current flags are browser flags, missing Streamlit is not handled clearly, or target switching mutates in place.
- Green focused checks: run `tests/core/test_cli.py` workbench tests and `tests/core/test_workbench_app.py` or new workbench target-switch tests.
- Broader local checks: run adjacent CLI/workbench/SQL helper tests if touched. Run Ruff format/check, `git diff --check`, and targeted pre-commit.

# Stop Conditions

- Stop and report `blocked` if the correct Streamlit CLI flags differ across supported Streamlit versions and require a documented support decision.
- Stop and report `escalate` if safe target switching requires a large workbench state architecture rewrite beyond this ticket.
- Stop if optional dependency handling would require adding Streamlit to base install dependencies.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves current workbench behavior is wrong
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including Streamlit flags, optional dependency errors, pass-through args, target-context rebuild, old adapter cleanup, and failure rollback behavior
- residual risks
- ticket recommendation

# Working Notes

Prefer a small CLI helper for finding the Streamlit executable and producing one consistent install hint. For target switching, prefer constructing a fresh `DbtConfiguration`/`DbtProjectContext` for the selected target over mutating `runtime_cfg.target_name` in place. Preserve existing state fields where practical, but fail closed if new context creation fails.

# Child Output

outcome: stop

files changed:
- `src/dbt_osmosis/cli/main.py`
- `src/dbt_osmosis/workbench/app.py`
- `tests/core/test_cli.py`
- `tests/core/test_workbench_app.py`

records changed:
- `.loom/packets/ralph/20260504T141941Z-ticket-c10wb22-iter-01.md`

red evidence:
- `uv run pytest tests/core/test_workbench_app.py -k 'change_target'` failed 2 tests because current `change_target()` mutated the existing context and called `_reload_manifest(ctx)`, which proved it did not rebuild a fresh selected-target context and did not have rollback behavior.
- `uv run pytest tests/core/test_cli.py -k 'workbench'` failed 4 tests: host/port were passed as `--browser.serverAddress`/`--browser.serverPort`, and missing Streamlit surfaced as raw `FileNotFoundError` without the workbench extra install hint for normal, `--options`, and `--config` paths.

green evidence:
- `uv run pytest tests/core/test_cli.py -k 'workbench'`: 5 passed.
- `uv run pytest tests/core/test_workbench_app.py -k 'change_target'`: 2 passed.
- `uv run pytest tests/core/test_cli.py tests/core/test_workbench_app.py`: 25 passed.
- `uv run ruff check src/dbt_osmosis/cli/main.py src/dbt_osmosis/workbench/app.py tests/core/test_cli.py tests/core/test_workbench_app.py`: passed.
- `uv run ruff format --check src/dbt_osmosis/cli/main.py src/dbt_osmosis/workbench/app.py tests/core/test_cli.py tests/core/test_workbench_app.py`: passed after formatting `src/dbt_osmosis/workbench/app.py` with `uv run ruff format ...`.
- `git diff --check`: passed with no output.
- `uv run pre-commit run --files src/dbt_osmosis/cli/main.py src/dbt_osmosis/workbench/app.py tests/core/test_cli.py tests/core/test_workbench_app.py`: passed.
- `uv run pre-commit run --files .loom/packets/ralph/20260504T141941Z-ticket-c10wb22-iter-01.md`: passed.

implementation summary:
- Workbench CLI now resolves the `streamlit` executable lazily, so `src/dbt_osmosis/cli/main.py` still does not import Streamlit at module import time.
- Missing Streamlit now raises a Click-visible error with `pip install dbt-osmosis[workbench]` for normal launch, `--options`, and `--config` paths.
- Workbench launch now binds Streamlit with `--server.address=<host>` and `--server.port=<port>` and keeps unknown/pass-through Streamlit args intact before the app script.
- Target switching now builds a new `DbtConfiguration` for the selected target, creates a fresh dbt project context, swaps state to the new context only after successful creation, refreshes model nodes, recompiles the current query against the new context, and closes the old context connections.
- Failed target switching leaves the old context active, restores the old target in app/widget state, preserves the old compiled query, and displays a clear Streamlit error.

residual risks:
- Validation used focused unit/stub tests; no live Streamlit server launch or real multi-target dbt adapter switch was exercised locally.
- Broader full-suite/CI validation was not run per packet guidance to use local focused validation for this ticket.
- The ticket record had a pre-existing activation/packet-link modification outside child write scope; it was not edited by this child worker.

ticket recommendation:
- Move `ticket:c10wb22` to review-required or complete-pending-acceptance after parent reconciliation, then run mandatory critique before acceptance/closure.

# Parent Merge Notes

Parent accepted the child output as in scope and complete, then hardened the implementation after mandatory critique found target-state and dependency preflight gaps:

- Re-ran child focused CLI/workbench checks.
- Updated `change_target()` to read the requested target from Streamlit widget state `state.target_name`, with fallback to `state.app.target_name`.
- Added normal-launch workbench extra dependency preflight with a clear `pip install dbt-osmosis[workbench]` hint.
- Added direct literal Click `--` pass-through coverage.
- Re-ran focused workbench validation: `11 passed, 16 deselected`.
- Re-ran related CLI/workbench/SQL/legacy suites: `36 passed`.
- Ran Ruff format/check, `git diff --check`, and targeted pre-commit successfully.
- Ran final mandatory read-only critique; no findings remained.
- Committed implementation as `ab138546acf0e32d7ad9339382d291bac8365fdc`.
- Recorded evidence in `evidence:c10wb22-workbench-target-validation`.
- Recorded final critique in `critique:c10wb22-workbench-target-review`.

Outcome: consumed; ticket reconciliation completed in `ticket:c10wb22`.
