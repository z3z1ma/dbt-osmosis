---
id: "packet:ralph-ticket-c10rel08-20260504T012824Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10rel08"
mode: execution
change_class: "release-packaging"
risk_class: "high"
style: "reference-first"
verification_posture: "observation-first"
iteration: "1"
created_at: "2026-05-04T01:28:24Z"
updated_at: "2026-05-04T01:50:21Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns output only; parent reconciles ticket, evidence, critique, wiki, and packet status"
  paths:
    - ".github/workflows/release.yml"
parent_merge_scope:
  records:
    - "ticket:c10rel08"
    - "packet:ralph-ticket-c10rel08-20260504T012824Z"
  paths:
    - ".loom/tickets/20260503-c10rel08-validate-before-release-tag-publish.md"
    - ".loom/packets/ralph/20260504T012824Z-ticket-c10rel08-iter-01.md"
source_fingerprint:
  git_commit: "a32f3a7008fb5f70c4c5d6c5fc078540b951384d"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "a32f3a7008fb5f70c4c5d6c5fc078540b951384d"
  git_status_summary: "clean"
  git_status_detail: "clean before packet compilation"
  compiled_from:
    - "ticket:c10rel08"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: sibling_worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed - required for package/tool smoke commands and GitHub workflow context inspection if needed"
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 100
  avoid_full_file_reads: false
sources:
  constitution:
    - constitution:main
  initiative:
    - "initiative:dbt-110-111-hardening"
  research: []
  spec: []
  plan: []
  ticket:
    - "ticket:c10rel08"
links:
  issue: "https://github.com/z3z1ma/dbt-osmosis/issues/359"
---

# Mission

Make `.github/workflows/release.yml` validate build, metadata, install smoke, tests/docs/package checks, and release-note intent before any version tag or publish step can run.

# Bound Context

`ticket:c10rel08` owns this work. It is high-risk release-packaging work because the current release workflow can tag a version before build or publish validation. The implementation must not perform a real release, push tags, publish to PyPI, or mutate Git state.

# Parent Launch Checklist

- Target ticket is `ready`, high risk, and has bounded acceptance criteria for release workflow hardening.
- Child write scope is limited to `.github/workflows/release.yml`.
- Parent keeps ticket/evidence/critique/wiki/packet reconciliation.
- Source fingerprint matches `origin/main` and the worktree was clean before packet compilation.
- Verification posture is observation-first because the workflow behavior is best validated through before/after static workflow shape plus local smoke checks, not by creating real tags or releases.

# Source Snapshot

Current `.github/workflows/release.yml` shape before this packet:

- Trigger: push to `main`.
- It checks out with `fetch-depth: 2`, sets up Python 3.10, installs pip and uv.
- `Detect and tag new version` runs before `Build package` and before `Publish package on PyPI`.
- `Build package` uses `uvx --from=hatchling==1.26.3 hatchling build`.
- PyPI publish uses `pypa/gh-action-pypi-publish@v1.13.0` with `PYPI_TOKEN`.
- Release notes use `release-drafter/release-drafter@v5.23.0` after publish/tag detection.

Ticket acceptance requires:

- `ticket:c10rel08#ACC-001` - build sdist/wheel and validate metadata before tag creation.
- `ticket:c10rel08#ACC-002` - clean install from built wheel verifies `dbt-osmosis --help`, `python -m dbt_osmosis`, and importing `dbt_osmosis.cli.main`.
- `ticket:c10rel08#ACC-003` - required test/docs/package checks pass before PyPI publish.
- `ticket:c10rel08#ACC-004` - failed build/test cannot leave a new version tag or published GitHub release.
- `ticket:c10rel08#ACC-005` - workflow permissions and PyPI auth are explicit and least-privilege for the chosen publishing method.
- `ticket:c10rel08#ACC-006` - release notes/changelog source of truth is documented or enforced.

# Change Class

`release-packaging`, high risk. The child should minimize the workflow diff while making tag/publish steps depend on validation. Mandatory critique will happen after the child returns.

# Verification Targets

- `ticket:c10rel08#ACC-001`
- `ticket:c10rel08#ACC-002`
- `ticket:c10rel08#ACC-003`
- `ticket:c10rel08#ACC-004`
- `ticket:c10rel08#ACC-005`
- `ticket:c10rel08#ACC-006`

# Task For This Iteration

Update `.github/workflows/release.yml` only.

Required shape:

- No step that creates a tag, publishes PyPI, or publishes a GitHub release may run before validation steps complete successfully.
- Add explicit workflow/job permissions. Use least privilege for current token-based PyPI publishing. Do not switch to trusted publishing unless the workflow can do so without repository/PyPI external setup uncertainty.
- Build sdist/wheel before tag detection/creation.
- Validate package metadata before tag detection/creation, preferably with `twine check --strict dist/*` or an equivalent explicit metadata checker.
- Add clean wheel install smoke from the built wheel that verifies `dbt-osmosis --help`, `python -m dbt_osmosis` or `python -m dbt_osmosis --help` if no-arg behavior is inappropriate, and `import dbt_osmosis.cli.main`.
- Add required checks before tag/publish. At minimum include package checks and the existing repository test/docs commands that are practical in this workflow; if full docs build is too heavy or blocked, explain in child output and encode the strongest safe gate you can.
- Keep Release Drafter as release notes source unless you can safely improve it without broad changelog redesign. Document in the workflow that Release Drafter is the GitHub release notes source for this workflow.
- Keep no-op/dev-build behavior for pushes where no version tag is detected, but do not let no-tag behavior publish or create releases.

# Verification Posture

Observation-first.

Before changing, observe and report the current release ordering risk: tag detection/tag creation occurs before build/package validation.

After changing, report the new ordering and run the smallest honest local checks available, such as:

- `pre-commit run --files .github/workflows/release.yml`.
- `uvx --from=hatchling==1.26.3 hatchling build` if feasible.
- `twine check --strict dist/*` or the exact metadata check command encoded in the workflow if feasible.
- Clean wheel install smoke if feasible.

Do not run commands that create tags, push, publish, or mutate shared Git metadata.

# Stop Conditions

- Stop if `.github/workflows/release.yml` or `ticket:c10rel08` has materially changed from the packet snapshot.
- Stop if satisfying acceptance appears to require PyPI trusted-publishing setup, secrets changes, or other external configuration not expressible safely in repository files.
- Stop if local validation would require creating tags, publishing packages, or pushing refs.
- Stop rather than expanding into `ticket:c10pkg10` package metadata cleanup or `ticket:c10docs09` docs build repair.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- exact workflow ordering after the change
- evidence gathered, including commands and pass/fail results
- self-review findings or concerns
- blockers or residual risks
- ticket recommendation

# Working Notes

Issue for this ticket is expected to be GitHub issue #359.

# Child Output

Outcome: stop.

Files changed:

- `.github/workflows/release.yml`

Records changed by child: None.

Workflow ordering after change:

1. checkout/setup Python/setup Node/install pip and uv/check parent
2. `uv lock --check`, install check dependencies, `ruff check`, `basedpyright`, `dbt parse`, `pytest -q`
3. `npm ci`, docs build
4. hatchling package build
5. `twine check --strict dist/*`
6. clean wheel install smoke: `dbt-osmosis --help`, `python -m dbt_osmosis --help`, import `dbt_osmosis.cli.main`, `pip check`
7. declare Release Drafter as release notes source
8. detect/create version tag
9. no-tag path: dev version bump and dev package build only
10. tag path: publish PyPI, then publish GitHub release notes through Release Drafter

Evidence gathered by child:

- `pre-commit run --files .github/workflows/release.yml` passed.
- Temp worktree package smoke passed: hatchling build, `twine check --strict`, clean Python 3.10 wheel install, CLI/module help, import smoke, `pip check`.
- `git diff --check -- .github/workflows/release.yml` passed.

Self-review concerns / residual risks:

- Did not execute the real GitHub release workflow, create tags, publish packages, or publish releases.
- Full encoded docs/tests are gated in workflow but were not run locally due cost.

Ticket recommendation: route to mandatory critique for high-risk release-packaging change.

# Parent Merge Notes

Parent inspected the workflow diff, reran `pre-commit run --files .github/workflows/release.yml`, reran local hatchling build, `twine check --strict`, clean wheel install smoke, CLI/module help, import smoke, and `pip check`; recorded `evidence:c10rel08-local-release-workflow-validation`; and is routing to mandatory critique before commit/push acceptance.

Mandatory critique initially found pre-final blockers in validation credentials and workflow-run scope. Parent follow-up split validation and release into separate jobs, made validation read-only with `persist-credentials: false`, restricted write credentials to the post-validation release job, narrowed `workflow_run` to successful same-repository push runs of `Tests` on `main`, reran `pre-commit run --files .github/workflows/release.yml`, and recorded final passing critique in `critique:c10rel08-release-workflow-hardening`.
