---
id: "packet:ralph-ticket-c10pkg10-20260504T033410Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10pkg10"
mode: execution
change_class: "release-packaging"
risk_class: "medium"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T03:34:10Z"
updated_at: "2026-05-04T04:18:06Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md"
  paths:
    - "pyproject.toml"
    - "uv.lock"
    - "Taskfile.yml"
    - ".pre-commit-config.yaml"
    - ".github/workflows/tests.yml"
    - ".gitignore"
    - "src/dbt_osmosis/workbench/requirements.txt"
    - "README.md"
    - "docs/docs/intro.md"
    - "docs/docs/reference/cli.md"
    - "docs/docs/tutorial-basics/installation.md"
    - "docs/docs/tutorial-yaml/synthesize.md"
    - "tests/test_package_metadata.py"
parent_merge_scope:
  records:
    - "ticket:c10pkg10"
  paths:
    - ".loom/tickets/20260503-c10pkg10-rationalize-packaging-extras-dev-deps.md"
    - ".loom/evidence/"
    - ".loom/critique/"
source_fingerprint:
  git_commit: "83ee46a57a7aaf9349edb9ac5acacb02f8dee236"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "83ee46a57a7aaf9349edb9ac5acacb02f8dee236"
  git_status_summary: "dirty_tracked"
  git_status_detail: "Expected parent-compiled records only: packet file added and ticket:c10pkg10 activated; no source/package files changed before launch"
  compiled_from:
    - "ticket:c10pkg10"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed - package resolution and install smoke may need package indexes"
context_budget:
  posture: normal
  max_source_files: 10
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research: []
  spec: []
  plan: []
  ticket:
    - "ticket:c10pkg10"
links:
  tickets:
    - ticket:c10pkg10
---

# Mission

Rationalize package metadata and install smoke coverage for `ticket:c10pkg10` without making a product decision about the experimental SQL proxy.

# Bound Context

`ticket:c10pkg10` is medium-risk release-packaging work under `initiative:dbt-110-111-hardening`. Keep base install truthful and small, keep optional features behind documented extras, and stop if the work would remove the SQL proxy or change it from experimental to supported; `ticket:c10proxy25` owns that decision.

# Source Snapshot

- `pyproject.toml` currently has base dependencies `click`, `dbt-core`, `dbt-core-interface`, `ruamel.yaml`, `rich`, `pluggy`, `mysql-mimic`, and `rapidfuzz`.
- `src/dbt_osmosis/core/sql_lint.py` imports `sqlglot` directly and `src/dbt_osmosis/cli/main.py` imports `SQLLinter` at module import time, so `sqlglot` is base CLI behavior unless that import boundary changes.
- `src/dbt_osmosis/sql/proxy.py` imports `mysql_mimic` and `sqlglot`; no CLI command currently imports the proxy module.
- `src/dbt_osmosis/core/llm.py` treats `openai` and `azure.identity` as optional imports. Docs currently say Azure AD users should install `azure-identity` separately after `dbt-osmosis[openai]`.
- `src/dbt_osmosis/workbench/requirements.txt` currently says `dbt-osmosis[workbench,duckdb]==1.1.5`, but package version is `1.3.0` and no `duckdb` extra exists.
- `workbench` extra currently includes Streamlit/UI deps plus `dbt-duckdb`; docs advertise `dbt-osmosis[workbench]` for the workbench.
- `.gitignore` ignores `.python-version`; `Taskfile.yml` already falls back when `.python-version` is absent, but `venv.sources` still lists `.python-version`.
- Ruff versions drift: `pyproject.toml` has `ruff~=0.8.4`, `uv.lock` resolves `ruff 0.8.6`, `.pre-commit-config.yaml` pins `ruff-pre-commit v0.8.0`, and `Taskfile.yml` uses unpinned `uvx ruff`.

# Change Class

Release-packaging. The evidence must show package metadata, lockfile freshness, install smoke, and user-facing install docs stay coherent. This packet is test-first because the ticket acceptance names observable metadata/install behavior.

# Verification Targets

- `ticket:c10pkg10#ACC-001`
- `ticket:c10pkg10#ACC-002`
- `ticket:c10pkg10#ACC-003`
- `ticket:c10pkg10#ACC-004`
- `ticket:c10pkg10#ACC-005`
- `ticket:c10pkg10#ACC-006`

# Task For This Iteration

Implement the smallest package metadata cleanup that satisfies the ticket:

- Add a focused package-metadata regression check before changing metadata. Prefer `tests/test_package_metadata.py` using `tomllib` to assert the intended base/extras/dev-tool shape. The first run should fail for the current metadata.
- Add `sqlglot` as a direct base dependency because base CLI lint imports it.
- Move `mysql-mimic` out of base dependencies into a documented optional `proxy` extra unless inspection proves the proxy is advertised base behavior. Do not remove proxy code.
- Add a `duckdb` extra for the demo/developer adapter and remove `dbt-duckdb` from `workbench` if the workbench itself does not require DuckDB. Keep CI/dev installs that need DuckDB explicit via the new extra or direct adapter install.
- Add an `azure` optional extra for `azure-identity`; keep OpenAI SDK under `openai`.
- Remove or update `src/dbt_osmosis/workbench/requirements.txt` so it no longer references stale package version `1.1.5` or a nonexistent extra.
- Align Ruff dev tooling across pyproject, dependency group, Taskfile, and pre-commit. Prefer the locked Ruff version if that keeps formatting behavior stable.
- Make Taskfile handling of missing `.python-version` explicit enough that `task --list` and common commands do not fail in a fresh clone. Do not require tracking `.python-version` unless that is simpler and justified.
- Add or update CI pip smoke to cover base install plus supported extras (`openai`, `azure`, `workbench`, `duckdb`, `proxy`) without exploding the matrix.
- Update README/Docusaurus docs only where install extras changed.
- Run `uv lock` after metadata changes and keep `uv.lock` fresh.

# Verification Posture

`test-first`:

- Red check: create or update a focused package metadata test before implementation, then run it and show it fails for the current metadata. The expected failure should be about at least one real gap from the ticket, such as base `mysql-mimic`, missing direct `sqlglot`, missing `duckdb`/`proxy`/`azure` extras, stale workbench requirements, or Ruff tool drift.
- Green check: after implementation, rerun the focused package metadata test and a lock/install-smoke set.
- Broader checks expected after green: `uv lock --check`, `pre-commit run --files` for changed metadata/workflow/docs files, and at least one actual install smoke for base and each supported extra. Use temporary environments under `/var/folders/1b/6mg4g2fs2zx99h46b9j5r7mh0000gp/T/opencode` or another safe temp location.

# Stop Conditions

- Stop if satisfying the ticket requires removing the SQL proxy or making it supported; return `blocked` and cite `ticket:c10proxy25`.
- Stop if package resolution requires changing the dbt support range, Python support range, or adapter boundary; that belongs to `ticket:c10bounds29` / `ticket:c10lock07`.
- Stop if install smoke requires network access that is unavailable after one reasonable retry.
- Stop if the package metadata change would require large docs rewrites beyond install/extras references.
- Stop if governing records or child-write-scope files are materially different from this packet's source fingerprint.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- red evidence: command, failure, and why it failed for the expected packaging gap
- green evidence: focused test, lock check, install smoke, and any hooks run
- package/extras decisions made and rationale
- any residual risks or follow-up recommendations, especially proxy/workbench/LLM boundary concerns
- ticket recommendation

# Working Notes

Prefer minimal metadata and CI changes over broad dependency redesign. If there is a choice between adding an extra and removing an experimental feature, add the extra and leave the feature decision to its ticket.

# Child Output

Outcome: `stop` — bounded package metadata cleanup implemented and validated; recommend parent route to release-packaging critique/acceptance review.

Files changed in child write scope:

- `pyproject.toml`
- `uv.lock`
- `Taskfile.yml`
- `.pre-commit-config.yaml`
- `.github/workflows/tests.yml`
- `src/dbt_osmosis/workbench/requirements.txt`
- `README.md`
- `docs/docs/intro.md`
- `docs/docs/reference/cli.md`
- `docs/docs/tutorial-basics/installation.md`
- `docs/docs/tutorial-yaml/synthesize.md`
- `tests/test_package_metadata.py`
- `.loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`

Red evidence:

- Command: `uv run pytest tests/test_package_metadata.py`
- Result: expected red failure, `4 failed in 0.15s`.
- Relevant failures:
  - `assert "sqlglot" in base_dependencies` failed; base deps only had `mysql-mimic` transitively providing `sqlglot`.
  - `assert "==1.1.5" not in requirements` failed for `src/dbt_osmosis/workbench/requirements.txt` (`dbt-osmosis[workbench,duckdb]==1.1.5`).
  - `assert 'ruff==0.8.6' in project_dev` failed because pyproject had `ruff~=0.8.4`.
  - `assert "sources:\n      - .python-version" not in taskfile` failed because `Taskfile.yml` still treated ignored `.python-version` as a venv source.

Green evidence:

- Command: `uv lock`
  - Result: `Resolved 165 packages in 189ms` after final metadata adjustment.
- Command: `uv lock --check`
  - Result: `Resolved 165 packages in 8ms` (exit 0).
- Command: `uv run pytest tests/test_package_metadata.py`
  - Result: `4 passed in 0.01s`.
- Command: `task --list`
  - Result: task list rendered successfully without relying on `.python-version`.
- Command: `pre-commit run --files pyproject.toml uv.lock Taskfile.yml .pre-commit-config.yaml .github/workflows/tests.yml src/dbt_osmosis/workbench/requirements.txt README.md docs/docs/intro.md docs/docs/reference/cli.md docs/docs/tutorial-basics/installation.md docs/docs/tutorial-yaml/synthesize.md tests/test_package_metadata.py`
  - Result: check-ast, check-yaml, check-toml, end-of-file-fixer, trailing-whitespace, detect-private-key, debug-statements, ruff-format, ruff, gitleaks, and actionlint all passed.
- Command: final base install smoke using a temp venv under `/var/folders/1b/6mg4g2fs2zx99h46b9j5r7mh0000gp/T/opencode` with `python -m pip install -q -e .`, `python -m pip check`, `dbt-osmosis --help`, and import assertions.
  - Result: `No broken requirements found.`; `base python: 3.12.7`; `base dbt-osmosis: 1.3.0`; `base dbt-core: 1.11.8`; `base sqlglot: 28.10.1`; `base mysql_mimic installed: False`.
- Command: final all-extras install smoke using `uv venv --seed --python 3.13` under `/var/folders/1b/6mg4g2fs2zx99h46b9j5r7mh0000gp/T/opencode`, then `python -m pip install -q -e ".[openai,azure,workbench,duckdb,proxy]" "dbt-core~=1.11.0" "dbt-duckdb~=1.10.1"`, `python -m pip check`, `dbt-osmosis --help`, and import assertions.
  - Result: `No broken requirements found.`; `extras python: 3.13.9`; `extras dbt-osmosis: 1.3.0`; `extras dbt-core: 1.11.8`; `extras dbt-duckdb: 1.10.1`; `extras openai: 1.58.1`; `extras azure-identity: 1.25.3`; `extras streamlit: 1.57.0`; `extras mysql-mimic: 3.0.2`.
- Command: `task pip-install-smoke`
  - Result: passed on Python `3.13.9`; printed `dbt-core: 1.11.8`, `dbt-duckdb: 1.10.1`, `dbt-adapters: 1.22.10`, `dbt-osmosis: 1.3.0`; `dbt-osmosis --help` succeeded and `import dbt_osmosis; import dbt_osmosis.cli.main` succeeded.

Package/extras decisions and rationale:

- Added direct base `sqlglot>=28,<29` because base CLI imports SQL linting at module import time and SQL linting imports `sqlglot` directly.
- Moved `mysql-mimic` out of base into new `proxy` extra because only `src/dbt_osmosis/sql/proxy.py` imports it and the proxy is experimental/not a base CLI path.
- Added `duckdb` extra for demo/developer adapter installation and removed `dbt-duckdb` from `workbench`; CI/dev paths that need DuckDB now request `duckdb` explicitly and still pin matrix adapter versions directly.
- Added `azure` extra for `azure-identity`; retained `openai` for the OpenAI SDK.
- Updated workbench requirements to `dbt-osmosis[workbench,duckdb]==1.3.0` so the hosted/demo workbench keeps DuckDB explicit without putting DuckDB in the workbench extra.
- Aligned Ruff on locked `0.8.6` across pyproject optional dev, dependency group dev, Taskfile `uvx`, and pre-commit `ruff-pre-commit`.
- Removed `.python-version` from Taskfile `venv.sources`; `UV_PYTHON` fallback already handles fresh clones where `.python-version` is ignored.
- Updated CI and Taskfile pip smoke to install `.[openai,azure,workbench,duckdb,proxy]` together rather than adding an extras matrix.
- Raised the workbench Streamlit floor to `>=1.45.0,<2.0` after install smoke exposed that the previous `<1.42.0` range could not coexist with dbt-core 1.11's `protobuf>=6` requirement.
- Updated install docs and LLM docs for the new `duckdb`, `azure`, and `proxy` extras.

Residual risks / follow-up recommendations:

- Workbench import/runtime behavior with Streamlit 1.57.0 was package-smoked but not interactively launched; recommend critique pay attention to `streamlit-elements-fluence` compatibility with newer Streamlit.
- Proxy remains experimental and only dependency-routed; no product support decision was made. `ticket:c10proxy25` should still own the proxy support/removal decision.
- `mysql-mimic` resolved to 3.0.2 through the `>=2.5.7` proxy extra. If proxy behavior is later formalized, add proxy-specific runtime tests and consider a tighter bound there.

Ticket recommendation:

- Move `ticket:c10pkg10` to `review_required` for medium-risk release-packaging critique, with the above evidence as acceptance support.

Continuation update for `ticket:c10pkg10#ACC-006`:

- Outcome remains `stop`; parent identified that the prior all-extras-together smoke could mask per-extra dependency gaps, so this continuation changed the smoke contract to install base and each supported user-facing optional extra in separate fresh environments.
- Additional files changed in this continuation:
  - `Taskfile.yml`
  - `.github/workflows/tests.yml`
  - `tests/test_package_metadata.py`
  - `.loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`
- Implementation update:
  - `Taskfile.yml` `pip-install-smoke` now runs `base`, `openai`, `azure`, `workbench`, `duckdb`, and `proxy` smokes independently, each in its own Python 3.13 venv.
  - `.github/workflows/tests.yml` mirrors the same independent smoke loop inside the existing pip smoke job, avoiding a matrix expansion.
  - `tests/test_package_metadata.py` now asserts that the Taskfile and CI workflow include separate install targets for `.`, `.[openai]`, `.[azure]`, `.[workbench]`, `.[duckdb]`, and `.[proxy]`.
- Additional evidence:
  - Command: `uv run pytest tests/test_package_metadata.py`
    - Result: `4 passed in 0.06s`.
  - Command: `uv lock --check`
    - Result: `Resolved 165 packages in 5ms`.
  - Command: `task pip-install-smoke`
    - Result: passed. The output was long/truncated by the harness, but the captured output showed each independent smoke reaching `No broken requirements found.` and package assertions:
      - `smoke: base`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: openai`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: azure`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: workbench`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: duckdb`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`; `dbt-duckdb: 1.10.1`.
      - `smoke: proxy`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
    - The smoke scripts also assert expected per-extra modules are importable, base does not install `mysql_mimic`, and workbench alone does not install `dbt.adapters.duckdb`.
  - Command: `pre-commit run --files Taskfile.yml .github/workflows/tests.yml tests/test_package_metadata.py .loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`
    - Result: check-ast, check-yaml, end-of-file-fixer, trailing-whitespace, detect-private-key, debug-statements, ruff-format, ruff, gitleaks, and actionlint all passed.
- Package/extras decisions remain unchanged from the prior child output.
- Residual risks remain unchanged: workbench install is independently smoked, but interactive Streamlit workbench launch/runtime compatibility still needs review attention.
- Ticket recommendation remains: move `ticket:c10pkg10` to `review_required` for medium-risk release-packaging critique.

Continuation update for `ticket:c10pkg10#ACC-004`:

- Outcome remains `stop`; parent identified that only Ruff was aligned and `[dependency-groups].dev` still diverged from `project.optional-dependencies.dev`.
- Additional files changed in this continuation:
  - `pyproject.toml`
  - `uv.lock`
  - `tests/test_package_metadata.py`
  - `.loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`
- Red evidence:
  - Command after adding the regression assertion and before changing `pyproject.toml`: `uv run pytest tests/test_package_metadata.py`
  - Result: expected failure, `1 failed, 3 passed in 0.13s`.
  - Failure: `test_dev_dependency_surfaces_are_canonicalized` failed on `assert dependency_group_dev == project_dev`; diff showed `basedpyright>=1.38.4` vs `basedpyright~=1.38.4`, missing `pytest-cov`, `hypothesis`, `mutmut`, `pre-commit`, and extra `openai>=1.58.1` in the dependency group.
- Implementation update:
  - Kept the optional `dev` extra as the canonical dev dependency list.
  - Updated `[dependency-groups].dev` to match `project.optional-dependencies.dev` exactly, including ordering and specifiers.
  - Updated `tests/test_package_metadata.py` to assert the two dev surfaces remain identical, while keeping Ruff Taskfile/pre-commit pin checks.
- Additional evidence:
  - Command: `uv lock`
    - Result: `Resolved 165 packages in 275ms`.
  - Command: `uv lock --check`
    - Result: `Resolved 165 packages in 7ms`.
  - Command: `uv run pytest tests/test_package_metadata.py`
    - Result: `4 passed in 0.41s`.
  - Command: `uv run python` metadata smoke for the dev dependency group.
    - Result: `basedpyright: 1.38.4`; `ruff: 0.8.6`; `pytest: 8.3.5`; `pytest-cov: 6.3.0`; `hypothesis: 6.150.0`; `mutmut: 3.4.0`; `pre-commit: 4.5.1`; `dbt-duckdb: 1.10.0`.
  - Command: `pre-commit run --files pyproject.toml uv.lock tests/test_package_metadata.py .loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`
    - Result: check-ast, check-toml, end-of-file-fixer, trailing-whitespace, detect-private-key, debug-statements, ruff-format, ruff, and gitleaks passed.
- Install smoke decision:
  - Base and optional package metadata did not change; only the uv dev dependency group was aligned to the already-canonical optional `dev` extra. The smallest honest smoke was the uv dev dependency metadata smoke above rather than rerunning all pip install smokes.
- Package/extras decisions remain unchanged from prior child output.
- Residual risks remain unchanged: workbench install is independently smoked, but interactive Streamlit workbench launch/runtime compatibility still needs review attention.
- Ticket recommendation remains: move `ticket:c10pkg10` to `review_required` for medium-risk release-packaging critique.

Continuation update for critique findings before acceptance:

- Outcome remains `stop`; addressed all six critique findings inside the existing child write scope.
- Additional files changed in this continuation:
  - `pyproject.toml`
  - `uv.lock`
  - `Taskfile.yml`
  - `.github/workflows/tests.yml`
  - `src/dbt_osmosis/workbench/requirements.txt`
  - `README.md`
  - `docs/docs/intro.md`
  - `docs/docs/tutorial-basics/installation.md`
  - `tests/test_package_metadata.py`
  - `.loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`
- Red / critique evidence:
  - Added regression assertions first for direct `PyYAML`, Python 3.10 `tomli`, pinned pre-commit tool install, unmasked DuckDB smoke, and stronger workbench smoke strings.
  - Command before fixes: `uv run pytest tests/test_package_metadata.py`
  - Result: expected failure, `3 failed, 1 passed in 0.20s`.
  - Failures covered missing direct `pyyaml`, missing `tomli>=2; python_version < "3.11"`, and DuckDB smoke still explicitly installing `dbt-duckdb~=1.10.1`.
  - After strengthening the workbench import smoke, command `task pip-install-smoke` initially failed in the independent workbench smoke with `ModuleNotFoundError: No module named 'pkg_resources'` from `ydata_profiling`, even though `setuptools>=70` resolved to `setuptools-82.0.1`. This exposed a real package issue in the new smoke.
- Implementation update:
  - Replaced raw `import tomllib` in `tests/test_package_metadata.py` with `tomllib` / `tomli` compatibility import and added `tomli>=2; python_version < "3.11"` to canonical dev dependencies.
  - Added direct base `PyYAML>=6.0` because `src/dbt_osmosis/cli/main.py` imports `yaml` directly.
  - Strengthened `pip-install-smoke` in `Taskfile.yml` and `.github/workflows/tests.yml` to import smoke modules, not only check `find_spec`; workbench now imports `streamlit`, `streamlit_ace`, `streamlit_elements_fluence`, `ydata_profiling`, `feedparser`, and representative workbench component modules (`dashboard`, `feed`, `profiler`). Full `dbt_osmosis.workbench.app` import is still avoided because it calls `st.set_page_config` at import time and is closer to launching UI setup.
  - Removed explicit `dbt-duckdb~=1.10.1` from the independent `.[duckdb]` smoke so the `duckdb` extra supplies `dbt-duckdb`; the smoke still constrains `dbt-core~=1.11.0` and asserts `dbt-duckdb` resolves to `1.10.x`.
  - Changed Taskfile pre-commit install from unpinned `uv tool install pre-commit` to `uv tool install 'pre-commit>3.0.0,<5'`, matching the canonical dev dependency bound.
  - Clarified proxy docs in README and installation/intro docs: `dbt-osmosis[proxy]` is dependency-only for the experimental proxy runtime and `ticket:c10proxy25` owns support semantics.
  - Tightened workbench `setuptools` to `setuptools>=70,<81` in `pyproject.toml` and `src/dbt_osmosis/workbench/requirements.txt` because the stronger workbench import smoke showed `ydata_profiling` still imports `pkg_resources`, which is absent with `setuptools-82.0.1`.
- Additional evidence:
  - Command: `uv lock`
    - Result: `Resolved 165 packages in 273ms` after final dependency adjustments.
  - Command: `uv lock --check`
    - Result: `Resolved 165 packages in 6ms`.
  - Command: `uv run pytest tests/test_package_metadata.py` under Python 3.10 environment
    - Result: `5 passed in 0.12s`.
  - Command: `uv run --python 3.10 pytest tests/test_package_metadata.py`
    - Result: `5 passed in 0.53s`; this verified the `tomllib`/`tomli` fallback path is installable in the Python 3.10 dev/test environment.
  - Command: `pre-commit run --files pyproject.toml uv.lock Taskfile.yml .github/workflows/tests.yml README.md docs/docs/intro.md docs/docs/tutorial-basics/installation.md tests/test_package_metadata.py .loom/packets/ralph/20260504T033410Z-ticket-c10pkg10-iter-01.md`
    - Result: check-ast, check-yaml, check-toml, end-of-file-fixer, trailing-whitespace, detect-private-key, debug-statements, ruff-format, ruff, gitleaks, and actionlint passed.
  - Command: `task pip-install-smoke`
    - Result: passed after the `setuptools<81` workbench fix. Captured output shows each independent smoke reached `No broken requirements found.`:
      - `smoke: base`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: openai`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: azure`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
      - `smoke: workbench`; resolved `setuptools-80.10.2`, imported the stronger workbench dependency/component set, and printed `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`. It emitted Streamlit bare-mode warnings (`runner.fileWatcherType` invalid config and missing `ScriptRunContext`) but completed successfully.
      - `smoke: duckdb`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`; `dbt-duckdb: 1.10.1` with no explicit adapter install argument.
      - `smoke: proxy`; `dbt-osmosis: 1.3.0`; `dbt-core: 1.11.8`.
- Residual risks:
  - Workbench smoke now imports representative dependencies/components but still does not launch the full Streamlit app or call runtime dashboard flows; Streamlit bare-mode warnings are expected from importing Streamlit-backed components outside `streamlit run`.
  - Proxy remains dependency-only and experimental; support semantics stay out of scope and are owned by `ticket:c10proxy25`.
- Ticket recommendation remains: move `ticket:c10pkg10` to `review_required` for another release-packaging critique/acceptance pass.

# Parent Merge Notes

Parent reconciled the Ralph output into `ticket:c10pkg10`, `evidence:c10pkg10-package-metadata-smoke`, and `critique:c10pkg10-release-packaging-review`.

Ticket disposition after reconciliation: `complete_pending_acceptance`. The implementation, local evidence, and critique are sufficient for a guarded implementation commit. Final closure still requires post-commit/main CI evidence and retrospective / promotion disposition.
