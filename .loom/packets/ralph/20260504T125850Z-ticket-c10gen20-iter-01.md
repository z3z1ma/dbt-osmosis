---
id: "packet:ralph-ticket-c10gen20-20260504T125850Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10gen20"
mode: execution
change_class: "code-behavior"
risk_class: "high"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T12:58:50Z"
updated_at: "2026-05-04T13:41:04Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T125850Z-ticket-c10gen20-iter-01.md"
  paths:
    - "src/dbt_osmosis/cli/main.py"
    - "src/dbt_osmosis/core/generators.py"
    - "tests/core/test_cli_generate_group.py"
    - "tests/core/test_generators.py"
parent_merge_scope:
  records:
    - "ticket:c10gen20"
  paths:
    - ".loom/tickets/20260503-c10gen20-safe-generate-nl-yaml-writes.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "bcfc8330dd9eee053f0f195c84cea555e48295b5"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "bcfc8330dd9eee053f0f195c84cea555e48295b5"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10gen20 packet compilation"
  compiled_from:
    - "ticket:c10gen20"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal local dependency/runtime behavior; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 8
  max_excerpt_lines_per_file: 320
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  evidence:
    - evidence:oracle-backlog-scan
  ticket:
    - "ticket:c10gen20"
links:
  tickets:
    - ticket:c10gen20
---

# Mission

Route generate/NL schema YAML writes through the ruamel/schema helper pipeline with explicit overwrite safety, project-root output validation, dry-run planned-write reporting, and parse-safe generated descriptions.

# Bound Context

`ticket:c10gen20` is high-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-003` and `#OBJ-006`. The ticket explicitly focuses on YAML writes from generate/NL surfaces; SQL file writes may remain direct if that keeps scope bounded.

Use local validation only for this ticket. Do not wait on GitHub Actions; final initiative validation will handle CI later. Because this ticket is high risk, parent will run mandatory critique before acceptance.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/cli/main.py::generate model` writes schema YAML with `import yaml` and `Path(schema_path).write_text(yaml.dump(...))`.
- `generate sources` writes `result.yaml_content` directly with `Path.write_text()`.
- `generate staging` writes `result.yaml_content` directly with `Path.write_text()`.
- `src/dbt_osmosis/core/generators.py::generate_staging_from_source(use_ai=True)` hand-builds YAML strings with f-strings, which can break when descriptions contain colons, quotes, multiline text, or Jinja.
- Schema reader/writer helpers live under `src/dbt_osmosis/core/schema/reader.py`, `writer.py`, and `parser.py`; `_write_yaml()` provides ruamel serialization, atomic writes, dry-run mutation reporting, and preserved top-level section restoration when `_read_yaml()` has populated original-cache state.
- Existing generate CLI smoke tests are in `tests/core/test_cli_generate_group.py`; generator unit tests are in `tests/core/test_generators.py`.

# Change Class

`code-behavior`, high risk. The work changes generated YAML write behavior and overwrite safety for user files.

# Verification Targets

- ticket:c10gen20#ACC-001
- ticket:c10gen20#ACC-002
- ticket:c10gen20#ACC-003
- ticket:c10gen20#ACC-004
- ticket:c10gen20#ACC-005
- ticket:c10gen20#ACC-006
- initiative:dbt-110-111-hardening#OBJ-003
- initiative:dbt-110-111-hardening#OBJ-006

# Task For This Iteration

Implement the smallest safe writer abstraction that satisfies the ticket:

- Add failing tests first for existing schema YAML overwrite refusal, explicit overwrite preserving non-osmosis top-level sections, project-root output validation, dry-run planned-write reporting, and generated descriptions containing colon/quotes/multiline/Jinja parsing cleanly after write.
- Add an explicit overwrite opt-in for generate YAML writes, for example `--overwrite`, defaulting to fail closed when the target YAML file already exists.
- Validate generated YAML output paths are under the dbt project root by default. If adding an explicit external-path opt-in is necessary, name it clearly and test both fail and opt-in behavior. Prefer fail-closed without new external opt-in if enough for existing command surfaces.
- Use ruamel/schema helpers for YAML serialization. For existing YAML files, read first through `_read_yaml()` before `_write_yaml()` so non-osmosis top-level sections are preserved on overwrite.
- Keep SQL model file behavior out of scope unless a small path-validation guard is needed to keep YAML path handling coherent.
- Ensure dry-run output lists every file that would be written, including schema YAML paths, while writing no files.
- For AI staging YAML, build structured YAML data rather than f-string YAML so descriptions parse safely.

# Verification Posture

`test-first`:

- Red checks: write targeted tests first and show they fail because current command paths overwrite existing YAML, write outside project root, omit planned YAML writes in dry-run, or produce unparseable YAML from special descriptions.
- Green focused checks: run the new/changed generate CLI and generator tests.
- Broader local checks: run `tests/core/test_cli_generate_group.py`, `tests/core/test_generators.py`, and any focused schema writer tests if touched. Run Ruff format/check, `git diff --check`, and targeted pre-commit.

# Stop Conditions

- Stop and report `blocked` if existing silent-overwrite behavior must be preserved for compatibility and needs human approval before changing.
- Stop and report `escalate` if a correct fix requires redesigning generation APIs or prompt formats rather than a bounded writer/path abstraction.
- Stop if preserving existing non-osmosis sections cannot be achieved through `_read_yaml()`/`_write_yaml()` without broad schema sync changes.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves unsafe generated YAML writes
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including overwrite policy, path validation, dry-run planned writes, ruamel/schema helper usage, preservation behavior, and special-description parsing
- residual risks
- ticket recommendation

# Working Notes

Prefer one small CLI-local helper if sufficient. Avoid adding a new public generation subsystem. Tests should assert behavior and parseability rather than exact YAML formatting. If a command produces both SQL and YAML, the ticket's data-preservation acceptance is YAML-focused; do not widen to SQL overwrite policy unless the narrow implementation naturally shares a path guard.

# Child Output

Outcome: stop

Files changed:

- `src/dbt_osmosis/cli/main.py`
- `src/dbt_osmosis/core/generators.py`
- `tests/core/test_cli_generate_group.py`
- `tests/core/test_generators.py`

Records changed:

- `.loom/packets/ralph/20260504T125850Z-ticket-c10gen20-iter-01.md`

Red evidence:

- Command: `uv run pytest tests/core/test_cli_generate_group.py tests/core/test_generators.py -q`
- Result before implementation: failed as expected with 6 failing tests and 30 passing tests.
- Failure highlights proving unsafe generated YAML writes:
  - `test_generate_model_refuses_existing_schema_without_overwrite` exited 0 and overwrote existing schema YAML instead of failing closed.
  - `test_generate_model_overwrite_preserves_unmanaged_top_level_sections` reported `No such option: --overwrite`.
  - `test_generate_model_refuses_schema_path_outside_project_root` exited 0 and allowed a schema path outside the dbt project root.
  - `test_generate_sources_refuses_existing_yaml_without_overwrite` exited 0 and overwrote existing source YAML.
  - `test_generate_staging_dry_run_reports_planned_writes` omitted `Planned writes:` output.
  - `test_generate_staging_ai_yaml_escapes_special_descriptions` raised `ruamel.yaml.scanner.ScannerError: mapping values are not allowed here` for colon/quotes/multiline/Jinja descriptions.

Green evidence:

- Command: `uv run pytest tests/core/test_cli_generate_group.py tests/core/test_generators.py -q`
- Result after implementation and formatting: `36 passed in 23.23s`.
- Command: `uv run ruff format src/dbt_osmosis/cli/main.py src/dbt_osmosis/core/generators.py tests/core/test_cli_generate_group.py tests/core/test_generators.py`
- Result: `2 files reformatted, 2 files left unchanged`.
- Command: `uv run ruff check src/dbt_osmosis/cli/main.py src/dbt_osmosis/core/generators.py tests/core/test_cli_generate_group.py tests/core/test_generators.py`
- Result: `All checks passed!`.
- Command: `git diff --check`
- Result: passed with no output.
- Command: `uv run pre-commit run --files src/dbt_osmosis/cli/main.py src/dbt_osmosis/core/generators.py tests/core/test_cli_generate_group.py tests/core/test_generators.py`
- Result: all applicable hooks passed.

Implementation summary:

- Added a CLI-local generated YAML write helper that validates YAML output paths under the dbt project root, loads/generated data with the shared ruamel schema YAML instance, reads existing YAML with `_read_yaml()` before `_write_yaml()`, and writes/dry-runs through `_write_yaml()`.
- Added `--overwrite` to `generate model`, `generate sources`, `generate staging`, and deprecated `nl generate`; default non-dry-run behavior now refuses existing YAML targets and points users to `--overwrite`.
- Existing YAML overwrite with `--overwrite` preserves unmanaged top-level sections such as `semantic_models` through the schema reader/writer cache preservation path.
- Dry-run output now lists planned writes, including schema/source/staging YAML paths and generated SQL paths for commands that would write SQL.
- AI staging YAML generation now builds structured YAML data and serializes it with ruamel instead of f-string YAML, preserving descriptions containing colons, quotes, multiline text, and Jinja.

Residual risks:

- SQL file writes remain direct and do not share the YAML overwrite policy, matching packet scope but leaving existing SQL overwrite semantics unchanged.
- Dry-run intentionally reports planned writes and validates serialization/path safety without enforcing the non-dry-run existing-file refusal.
- Broader full-suite validation was not run; focused generate/generator tests, Ruff, diff whitespace, and targeted pre-commit passed locally.

Ticket recommendation:

- Move ticket:c10gen20 to parent review/critique routing for mandatory high-risk data-preservation critique; no further Ralph iteration appears necessary for this bounded slice unless parent wants full-suite or integration validation.

# Parent Merge Notes

Parent accepted the child output as in scope and complete, then hardened the implementation after mandatory critique found additional safety gaps:

- Re-ran child focused generate/generator checks: `36 passed`.
- Added parent-side writer no-clobber enforcement in `src/dbt_osmosis/core/schema/writer.py` and regression coverage in `tests/core/test_schema.py` after critique found preflight-only overwrite refusal.
- Added project-root validation for explicit SQL paths, default model-name-derived SQL paths, deprecated `nl generate`, and staging SQL-only paths after critique found incomplete ACC-004 coverage.
- Preserved `_write_yaml()` positional optional argument compatibility after critique found the first no-clobber signature change regressed existing callers.
- Re-ran expanded c10gen20 validation: `119 passed, 1 skipped`.
- Ran Ruff format/check, `git diff --check`, and targeted pre-commit successfully.
- Ran final mandatory read-only critique; no findings remained.
- Committed implementation as `750bff70765850751ea157e21ee4bedc0540d138`.
- Recorded evidence in `evidence:c10gen20-safe-generate-yaml-writes-validation`.
- Recorded final critique in `critique:c10gen20-safe-generate-yaml-writes-review`.
- Updated accepted explanation in `wiki:yaml-sync-safety`.

Outcome: consumed; ticket reconciliation completed in `ticket:c10gen20`.
