---
id: packet:ralph-ticket-gh329loss-20260505T062916Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:gh329loss
mode: execution
change_class: code-behavior
risk_class: high
style: reference-first
verification_posture: test-first
iteration: 1
created_at: 2026-05-05T06:29:16Z
updated_at: 2026-05-05T06:51:54Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - packet:ralph-ticket-gh329loss-20260505T062916Z
  paths:
    - src/dbt_osmosis/core/restructuring.py
    - tests/core/test_restructuring.py
parent_merge_scope:
  records:
    - ticket:gh329loss
  paths:
    - .loom/evidence/
    - .loom/critique/
    - .loom/tickets/20260505-gh329loss-preserve-unmanaged-restructure-files.md
    - .loom/packets/ralph/20260505T062916Z-ticket-gh329loss-iter-01.md
source_fingerprint:
  git_commit: db73d9e4a6fb2c28939d31aab611ca8d1f2f7021
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: db73d9e4a6fb2c28939d31aab611ca8d1f2f7021
  git_status_summary: clean
  git_status_detail: clean after triage-record commit
  compiled_from:
    - ticket:gh329loss
    - initiative:issue-pr-zero
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 6
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution: []
  initiative:
    - initiative:issue-pr-zero
  research: []
  spec: []
  plan: []
  ticket:
    - ticket:gh329loss
links: {}
---

# Mission

Fix the remaining `yaml refactor` restructure cleanup path that can delete a superseded schema YAML file even though it still contains unmanaged top-level content such as `semantic_models` or `databricks-tags`.

# Bound Context

This packet advances `ticket:gh329loss` only. Same-path schema read/write preservation is already covered elsewhere; this iteration must stay focused on superseded-file cleanup in restructure.

# Parent Launch Checklist

- Target ticket is ready and scoped to one data-loss fix.
- Source fingerprint matches `HEAD` and `origin/main` at packet compilation.
- Child write scope is limited to `src/dbt_osmosis/core/restructuring.py` and `tests/core/test_restructuring.py` plus this packet's child-output section if the worker updates it.
- Verification posture is test-first.
- Network and shared Git metadata mutations are forbidden for the child.

# Source Snapshot

- `src/dbt_osmosis/core/restructuring.py` superseded cleanup reads a filtered YAML document, removes managed entries, and deletes the old file if remaining filtered managed keys are empty.
- `tests/core/test_restructuring.py` already has deletion and partial-preservation behavior tests around superseded files.
- `tests/core/test_schema.py` already covers same-path preservation of unmanaged top-level sections, so this packet should not duplicate that whole suite unless needed for confidence.

# Change Class

`code-behavior`, high risk because it prevents silent YAML data loss. Mandatory critique follows this Ralph iteration before acceptance.

# Verification Targets

- ticket:gh329loss#ACC-001
- ticket:gh329loss#ACC-002
- ticket:gh329loss#ACC-003
- ticket:gh329loss#ACC-004
- ticket:gh329loss#ACC-005

# Task For This Iteration

Add the smallest correct implementation and regression tests so restructure does not delete a superseded schema file when unmanaged top-level content remains after managed entries are removed. Preserve truly empty superseded-file deletion behavior.

# Verification Posture

`test-first`: create a failing regression in `tests/core/test_restructuring.py` that demonstrates unmanaged root sections are lost/deleted by current superseded-file cleanup. Drive that focused test green with a minimal production change. Then run focused restructuring/schema tests relevant to superseded cleanup and unmanaged preservation.

# Stop Conditions

Stop if fixing this requires broad parser/writer redesign, changes to model-level custom key preservation, Databricks-specific generation, or any destructive Git operation. Stop if tests cannot reproduce the issue without excessive fixture setup.

# Output Contract

Return outcome, files changed, evidence gathered including red and green commands/output, blockers/risks, and ticket recommendation.

# Working Notes

Prefer keeping the old file with unmanaged sections rather than trying to migrate arbitrary unmanaged content unless the existing writer merge path makes a safer small change obvious.

# Child Output

Outcome: stop.

Files changed:

- `src/dbt_osmosis/core/restructuring.py`
- `tests/core/test_restructuring.py`

Evidence gathered:

- Red: unmanaged top-level superseded regression failed because a file containing `semantic_models` was deleted after managed model removal.
- Red: same-path superseded cleanup regression failed because cleanup removed the just-written managed section.
- Green: `uv run pytest tests/core/test_restructuring.py -q -k "superseded"` passed with `5 passed, 24 deselected`.
- Green: `git diff --check` passed.

Risks:

- Child initially ran in the default checkout and parent copied the patch into the hardening worktree, then cleaned the accidental default-checkout edits.
- Oracle critique found missing-original-cache and dry-run/cache coverage gaps, requiring iteration 2.

Ticket recommendation: continue to mandatory critique and a follow-up Ralph iteration for Oracle findings.

# Parent Merge Notes

Parent accepted the implementation as useful progress but not complete after Oracle critique. Iteration 2 superseded the remaining work for missing-original-cache and dry-run/cache coverage. This packet is consumed as the first implementation iteration.
