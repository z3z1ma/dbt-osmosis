---
id: "packet:ralph-ticket-c10ord18-20260504T122819Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10ord18"
mode: execution
change_class: "code-behavior"
risk_class: "medium"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T12:28:19Z"
updated_at: "2026-05-04T12:42:05Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T122819Z-ticket-c10ord18-iter-01.md"
  paths:
    - "src/dbt_osmosis/core/inheritance.py"
    - "src/dbt_osmosis/core/transforms.py"
    - "tests/core/test_inheritance_behavior.py"
    - "tests/test_yaml_inheritance.py"
parent_merge_scope:
  records:
    - "ticket:c10ord18"
  paths:
    - ".loom/tickets/20260503-c10ord18-fix-inheritance-ordering-determinism.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "4c4178b68d0724ce38cc23c263c18cb9613cc861"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "4c4178b68d0724ce38cc23c263c18cb9613cc861"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10ord18 packet compilation"
  compiled_from:
    - "ticket:c10ord18"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal local dependency/runtime behavior; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 6
  max_excerpt_lines_per_file: 260
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  evidence:
    - evidence:oracle-backlog-scan
  ticket:
    - "ticket:c10ord18"
links:
  tickets:
    - ticket:c10ord18
---

# Mission

Fix inheritance ancestor ordering, cycle detection, and deterministic tag merging without redesigning the inheritance algorithm.

# Bound Context

`ticket:c10ord18` is medium-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-003`. The ticket is ready and scoped to a small correctness/determinism fix.

Use local validation only for this ticket. Do not wait on GitHub Actions; final initiative validation will handle CI later.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/core/inheritance.py::_build_node_ancestor_tree()` initializes `visited = set(node.unique_id)`, which creates a set of characters rather than a set containing the root unique ID.
- `_build_node_ancestor_tree()` stores generation keys as strings such as `generation_2` and `generation_10`; downstream inheritance processing uses `reversed(sorted(tree.keys()))`, which sorts lexicographically.
- `_merge_graph_node_data()` merges top-level `tags` and nested `config.tags` using set union, which can reorder tags nondeterministically.
- `src/dbt_osmosis/core/transforms.py` semantic tag enhancement merges tags with `list(set(existing_tags + new_tags))`, also nondeterministic.
- Existing behavior-focused inheritance tests live in `tests/core/test_inheritance_behavior.py`; root inheritance tests live in `tests/test_yaml_inheritance.py`.

# Change Class

`code-behavior`, medium risk. The work can change inherited documentation/tag precedence and YAML output ordering.

# Verification Targets

- ticket:c10ord18#ACC-001
- ticket:c10ord18#ACC-002
- ticket:c10ord18#ACC-003
- ticket:c10ord18#ACC-004
- ticket:c10ord18#ACC-005
- initiative:dbt-110-111-hardening#OBJ-003

# Task For This Iteration

Implement the smallest fix that satisfies the ticket:

- Add failing tests first for root cycle detection with full unique IDs, a cycle like `A -> B -> A`, numeric generation processing where generation 2 precedes generation 10, deterministic tag merge order, and repeated tag output stability.
- Initialize the visited set as `{node.unique_id}`.
- Sort generation keys numerically wherever ancestor generations are processed.
- Replace set-based tag merges with order-preserving merges that keep existing/local order and append unseen inherited tags in deterministic upstream order.
- Apply the same deterministic tag merge principle to the semantic tag merge in `transforms.py` if covered by the ticket's cited line.
- Avoid broad graph abstractions, new dependencies, or semantic tag rule changes beyond deterministic ordering.

# Verification Posture

`test-first`:

- Red checks: write targeted tests first and show they fail because the current code treats the root visited value as characters, sorts generations lexicographically, or returns nondeterministic/set-ordered tags.
- Green focused checks: run the new/changed tests in `tests/core/test_inheritance_behavior.py` and any targeted root inheritance test touched.
- Broader local checks: run `tests/core/test_inheritance_behavior.py`, `tests/test_yaml_inheritance.py`, and related transform tests if `transforms.py` changes. Run Ruff format/check, `git diff --check`, and targeted pre-commit.

# Stop Conditions

- Stop and report `escalate` if correct ordering requires redesigning the inheritance graph rather than localized cycle/order/tag fixes.
- Stop if tests show current behavior depends on lexicographic generation ordering in a way that requires a spec decision.
- Stop if semantic tag merge behavior requires LLM integration changes outside deterministic list merging.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves cycle/order/tag nondeterminism
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including visited initialization, numeric generation ordering, top-level/config tag merge order, and semantic tag merge order
- residual risks
- ticket recommendation

# Working Notes

Prefer tiny helpers if needed, but do not introduce a new graph abstraction. The expected tag policy is order-preserving union: keep current/local tags in order, then append unseen incoming/inherited tags in their existing order.

# Child Output

Outcome: stop

Files changed:

- `src/dbt_osmosis/core/inheritance.py`
- `src/dbt_osmosis/core/transforms.py`
- `tests/core/test_inheritance_behavior.py`

Records changed:

- `.loom/packets/ralph/20260504T122819Z-ticket-c10ord18-iter-01.md`

Red evidence:

- Added focused tests first, then ran `uv run pytest tests/core/test_inheritance_behavior.py -k 'ancestor_tree_detects_cycle_back_to_root_unique_id or numeric_generations_farthest_to_closest or tag_merges_preserve_local_then_inherited_order or semantic_analysis_tag_merge_preserves_existing_then_suggested_order'`.
- Initial corrected red run failed 4 selected tests:
  - `test_ancestor_tree_detects_cycle_back_to_root_unique_id` showed `generation_2: ['model.pkg.a']`, proving the root unique ID was not fully present in `visited` and `A -> B -> A` re-entered the root.
  - `test_column_knowledge_processes_numeric_generations_farthest_to_closest` got `Raw source description` instead of `Closer staging description`, proving lexicographic `generation_2`/`generation_10` processing let farther generation 10 overwrite the closer generation 2 value.
  - `test_graph_node_tag_merges_preserve_local_then_inherited_order` got set-ordered top-level tags such as `['upstream_2', ...]` instead of `['local', 'shared', 'upstream', 'upstream_2']`.
  - `test_semantic_analysis_tag_merge_preserves_existing_then_suggested_order` got set-ordered semantic tags such as `['shared', 'existing', 'semantic', 'new']` instead of `['existing', 'shared', 'semantic', 'new']`.

Green evidence:

- Focused new tests: `uv run pytest tests/core/test_inheritance_behavior.py -k 'ancestor_tree_detects_cycle_back_to_root_unique_id or numeric_generations_farthest_to_closest or tag_merges_preserve_local_then_inherited_order or semantic_analysis_tag_merge_preserves_existing_then_suggested_order'` => 4 passed, 18 deselected.
- Broader local tests: `uv run pytest tests/core/test_inheritance_behavior.py tests/test_yaml_inheritance.py tests/core/test_transforms.py` => 53 passed.
- Formatting/lint: `uv run ruff format src/dbt_osmosis/core/inheritance.py src/dbt_osmosis/core/transforms.py tests/core/test_inheritance_behavior.py && uv run ruff check src/dbt_osmosis/core/inheritance.py src/dbt_osmosis/core/transforms.py tests/core/test_inheritance_behavior.py` => format completed and Ruff passed.
- Whitespace/pre-commit: `git diff --check && uv run pre-commit run --files src/dbt_osmosis/core/inheritance.py src/dbt_osmosis/core/transforms.py tests/core/test_inheritance_behavior.py` => passed all targeted hooks.

Implementation summary:

- Initialized `_build_node_ancestor_tree()` with `{node.unique_id}` so cycle detection tracks full unique IDs rather than characters.
- Added numeric generation key sorting and used it when processing ancestors farthest-to-closest, so `generation_10` sorts as depth 10 instead of between generation 1 and 2 lexicographically.
- Replaced set-union tag merging in `_merge_graph_node_data()` with an order-preserving union that keeps current/local top-level tags first, then appends unseen inherited tags in incoming order.
- Applied the same order-preserving union to nested `config.tags` merging.
- Replaced semantic analysis tag set merging in `transforms.py` with order-preserving merge of existing tags followed by unseen suggested tags.

Residual risks:

- The full repository test suite was not run; validation was limited to the packet-requested inheritance/YAML/transform tests and targeted hooks.
- Commands emitted the existing environment warning that `VIRTUAL_ENV=/Users/alexanderbutler/code_projects/personal/dbt-osmosis/.venv` did not match this worktree `.venv`; `uv` ignored it and executed with the project environment.
- A parent-owned ticket file was already modified before child execution for activation/packet linkage; it was inspected for scope relevance but not changed by this child.

Ticket recommendation:

- Parent should reconcile this iteration as complete and route `ticket:c10ord18` to review/acceptance disposition with the green evidence above; no additional implementation iteration appears necessary from this child pass.

# Parent Merge Notes

Parent accepted the child output as in scope and complete:

- Re-ran focused c10ord18 regressions: `4 passed, 18 deselected`.
- Re-ran related inheritance/transform/pipeline suites: `70 passed`.
- Ran Ruff format/check, `git diff --check`, and targeted pre-commit successfully.
- Ran read-only critique with no findings.
- Committed implementation as `96a43cbac9947e4a189163329bfd0febadf77ea1`.
- Recorded evidence in `evidence:c10ord18-inheritance-ordering-validation`.
- Recorded final critique in `critique:c10ord18-inheritance-ordering-review`.

Outcome: consumed; ticket reconciliation completed in `ticket:c10ord18`.
