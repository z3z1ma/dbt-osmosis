---
id: "packet:ralph-ticket-c10llm23-20260504T144951Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10llm23"
mode: execution
change_class: "code-behavior"
risk_class: "medium"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T14:49:51Z"
updated_at: "2026-05-04T15:16:22Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T144951Z-ticket-c10llm23-iter-01.md"
  paths:
    - "src/dbt_osmosis/cli/main.py"
    - "src/dbt_osmosis/core/llm.py"
    - "src/dbt_osmosis/core/test_suggestions.py"
    - "tests/core/test_cli.py"
    - "tests/core/test_llm.py"
    - "tests/core/test_test_suggestions.py"
    - "tests/test_package_metadata.py"
    - "docs/docs/reference/cli.md"
    - "docs/docs/tutorial-basics/installation.md"
parent_merge_scope:
  records:
    - "ticket:c10llm23"
  paths:
    - ".loom/tickets/20260503-c10llm23-align-llm-gates-azure-ai-defaults.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "7f1f153b7de7bf3af8fda69fc883a0b62b73d6f6"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "7f1f153b7de7bf3af8fda69fc883a0b62b73d6f6"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10llm23 packet compilation"
  compiled_from:
    - "ticket:c10llm23"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "no network calls in tests; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 10
  max_excerpt_lines_per_file: 340
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  evidence:
    - evidence:oracle-backlog-scan
  ticket:
    - "ticket:c10llm23"
links:
  tickets:
    - ticket:c10llm23
---

# Mission

Align `test-llm`, default LLM provider behavior, Azure OpenAI AD client wiring, optional extra messages, and AI test-suggestion fallback visibility so optional AI paths fail clearly and consistently.

# Bound Context

`ticket:c10llm23` is medium-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-006` and `#OBJ-007`. The work touches optional AI paths only; tests must mock clients and must not make network calls or expose secrets.

Use local validation only for this ticket. Do not wait on GitHub Actions per ticket; final initiative validation will handle CI later. Critique is recommended for this medium-risk optional auth/operator-clarity work; parent will run critique before acceptance unless it records a different disposition.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/cli/main.py::test_llm()` calls `get_llm_client()` and passes the tuple to `test_llm_connection()`, but `test_llm_connection()` ignores its parameter and requires `LLM_PROVIDER` even though `get_llm_client()` defaults to OpenAI.
- `src/dbt_osmosis/core/llm.py::get_llm_client()` defaults provider to `openai` and raises `ImportError` for missing OpenAI with a real `dbt-osmosis[openai]` hint.
- Azure API-key path uses `AzureOpenAI`, but Azure AD path acquires a token and constructs generic `OpenAI(base_url=..., api_key=token)` without API version/deployment-aware Azure client shape.
- The Azure identity missing message references `dbt-osmosis[azure]`, and package metadata tests show the `azure` extra exists.
- `test suggest --use-ai` defaults to true and core AI suggestion failures silently fall back to pattern suggestions in `src/dbt_osmosis/core/test_suggestions.py::_ai_suggest_tests()`.

# Change Class

`code-behavior`, medium risk. The work changes optional dependency/auth errors and user-visible CLI messaging.

# Verification Targets

- ticket:c10llm23#ACC-001
- ticket:c10llm23#ACC-002
- ticket:c10llm23#ACC-003
- ticket:c10llm23#ACC-004
- ticket:c10llm23#ACC-005
- ticket:c10llm23#ACC-006
- initiative:dbt-110-111-hardening#OBJ-006
- initiative:dbt-110-111-hardening#OBJ-007

# Task For This Iteration

Implement the smallest safe fix that satisfies the ticket:

- Add failing tests first for `test-llm` default OpenAI provider behavior with mocked client construction, missing OpenAI package/error wrapping, missing env vars, and invalid provider behavior.
- Make `test-llm` use exactly one client resolution path and raise friendly nonzero `ClickException` errors for missing packages/env/provider errors; do not require `LLM_PROVIDER` when default OpenAI should be used.
- Fix Azure OpenAI AD to use an Azure SDK client/auth shape, preferably `AzureOpenAI` with `azure_ad_token` or `azure_ad_token_provider` plus `api_version`, instead of generic `OpenAI(base_url=..., api_key=token)`. Preserve token-scope normalization and credential error redaction.
- Ensure install instructions reference only real extras/direct deps. The `azure` extra exists; keep messages consistent with package metadata.
- Keep `test suggest` AI default behavior stable unless absolutely necessary. Make the AI default/fallback behavior explicit and user-visible, for example by printing that AI is enabled by default and failures fall back to pattern-based suggestions, and by making `--pattern-only`/non-AI behavior explicit.
- Add/adjust tests for missing OpenAI package, missing provider env/default OpenAI, Azure identity absence, Azure AD client construction, and pattern-only behavior. Do not make real network calls.

# Verification Posture

`test-first`:

- Red checks: write targeted tests first and show they fail because `test-llm` requires `LLM_PROVIDER`, Azure AD uses generic OpenAI, or AI fallback behavior is not visible.
- Green focused checks: run `tests/core/test_llm.py`, `tests/core/test_cli.py` LLM cases, `tests/core/test_test_suggestions.py`, and package metadata tests if install hints/extras are touched.
- Broader local checks: run adjacent optional AI/generator tests if touched. Run Ruff format/check, `git diff --check`, and targeted pre-commit.

# Stop Conditions

- Stop and report `blocked` if changing `test suggest` from AI-by-default to opt-in appears necessary; that is a human decision trigger from the ticket.
- Stop and report `escalate` if OpenAI SDK version support for Azure AD cannot be made correct without changing dependency constraints.
- Stop if correct tests require real OpenAI/Azure network calls or secret values.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves current LLM behavior is wrong
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including default provider/test-llm behavior, optional dependency/env errors, Azure AD client shape, install hints/extras, and AI suggestion fallback visibility
- residual risks
- ticket recommendation

# Working Notes

Avoid logging or writing real secrets. Use obvious fake keys only in tests. Prefer `ClickException` boundaries in CLI and `LLMConfigurationError` in core. Keep OpenAI/Azure client objects mocked or inspected without calling APIs.

# Child Output

outcome: stop

files changed:

- `src/dbt_osmosis/cli/main.py`
- `src/dbt_osmosis/core/llm.py`
- `src/dbt_osmosis/core/test_suggestions.py`
- `tests/core/test_cli.py`
- `tests/core/test_llm.py`
- `tests/core/test_test_suggestions.py`
- `docs/docs/reference/cli.md`
- `docs/docs/tutorial-basics/installation.md`

records changed:

- `.loom/packets/ralph/20260504T144951Z-ticket-c10llm23-iter-01.md`

red evidence:

- Initial `uv run pytest ...` could not collect `tests/core/test_llm.py` because this worktree's uv environment lacks `openai`; no implementation claim was made from that run.
- Red run with the available local active env: `PYTHONPATH=src /Users/alexanderbutler/code_projects/personal/dbt-osmosis/.venv/bin/python -m pytest tests/core/test_llm.py::test_get_llm_client_defaults_to_openai_when_provider_unset tests/core/test_llm.py::test_get_llm_client_missing_openai_dependency_is_configuration_error tests/core/test_llm.py::test_get_llm_client_azure_ad_uses_azure_client_token_auth tests/core/test_cli.py::test_test_llm_defaults_to_openai_and_resolves_once tests/core/test_cli.py::test_test_llm_reports_friendly_click_errors tests/core/test_cli.py::test_test_suggest_pattern_only_reports_ai_disabled tests/core/test_test_suggestions.py::TestAITestSuggester::test_ai_suggest_fallback_logs_visible_warning` collected 9 tests and failed 8.
- The failures showed the intended pre-fix gaps: missing OpenAI raised raw `ImportError` instead of `LLMConfigurationError`; Azure AD never called `AzureOpenAI`; `test-llm` printed an `LLM_PROVIDER` missing message instead of defaulting to OpenAI; LLM config/provider/import errors surfaced as non-Click exceptions without `Error:` output; pattern-only mode had no explicit AI-disabled message; AI fallback produced no warning.

green evidence:

- New targeted tests after implementation: same `PYTHONPATH=src ... -m pytest` command passed `9 passed in 4.22s`.
- Focused/adjacent suite after formatting: `PYTHONPATH=src /Users/alexanderbutler/code_projects/personal/dbt-osmosis/.venv/bin/python -m pytest tests/core/test_llm.py tests/core/test_cli.py tests/core/test_test_suggestions.py tests/test_package_metadata.py` passed `110 passed, 5 skipped in 19.55s`; skips were existing Azure identity importskips because `azure.identity` is not installed in the local env.
- SDK shape check: local `openai.AzureOpenAI` signature includes `azure_ad_token`, `azure_ad_token_provider`, and `api_version` parameters.
- Formatting/lint/whitespace: `/Users/alexanderbutler/code_projects/personal/dbt-osmosis/.venv/bin/ruff format --check ... && .../ruff check ... && git diff --check` passed. Targeted pre-commit was not run because this repository's pre-commit config uses remote hooks and the packet forbids network calls.

implementation summary:

- `test-llm` now resolves the LLM client once, defaults the displayed provider to `openai` when `LLM_PROVIDER` is unset, and wraps `LLMConfigurationError`/`ImportError` as friendly nonzero `ClickException` messages.
- Core LLM client resolution now reports missing OpenAI SDK as `LLMConfigurationError` with the real `dbt-osmosis[openai]`/`openai` install hint.
- Azure OpenAI AD now keeps scope normalization and token acquisition/redaction, then constructs `AzureOpenAI(azure_endpoint=..., azure_ad_token=..., api_version=...)` using `AZURE_OPENAI_API_VERSION` or the existing default.
- `test suggest` keeps AI enabled by default, prints that default/fallback behavior, prints pattern-only AI-disabled behavior, and logs AI fallback warnings from core suggestion generation while falling back to pattern suggestions.
- Docs now describe OpenAI default-provider behavior, Azure AD requiring the real `azure` extra alongside `openai`, `test-llm` friendly errors, and AI suggestion fallback visibility.

residual risks:

- Local focused tests used the active sibling project virtualenv because this worktree's uv env lacks `openai`; the initial uv collection failure is recorded above.
- `azure.identity` is not installed locally, so existing non-mocked Azure identity tests skipped; Azure AD client construction was covered with mocks plus SDK signature inspection, not a real Azure credential/network flow.
- Targeted pre-commit was skipped to avoid network bootstrap of remote hooks; Ruff and `git diff --check` passed instead.

ticket recommendation:

- Move ticket `ticket:c10llm23` to review/critique. The bounded implementation is complete locally, with medium-risk critique still recommended before parent acceptance.

# Parent Merge Notes

- Parent accepted the bounded implementation with one test-hardening refinement: `tests/core/test_llm.py` now avoids import-time OpenAI dependencies and patches `dbt_osmosis.core.llm` clients directly so the worktree base `uv` environment can validate optional dependency behavior without installing `openai`.
- Implementation committed as `3fbb1e16acab450c7855e806a144e04613670985` (`fix: clarify LLM optional path failures`).
- Parent validation after commit: `uv run pytest tests/core/test_llm.py tests/core/test_cli.py tests/core/test_test_suggestions.py tests/test_package_metadata.py -q` passed `106 passed, 9 skipped`; `uv run ruff check ... && git diff --check` passed; targeted `uv run pre-commit run --files ...` passed; `uv run python -c "import importlib.util; print(importlib.util.find_spec('openai') is not None)"` printed `False`.
- Evidence recorded as `evidence:c10llm23-llm-gate-validation`.
- Critique recorded as `critique:c10llm23-llm-gate-review` with verdict `pass`, no findings, and recommendation `no-critique-blockers`.
