---
id: "packet:ralph-ticket-c10loss16-20260504T112322Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10loss16"
mode: execution
change_class: "code-behavior"
risk_class: "high"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T11:23:22Z"
updated_at: "2026-05-04T11:46:50Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T112322Z-ticket-c10loss16-iter-01.md"
  paths:
    - "src/dbt_osmosis/core/sync_operations.py"
    - "src/dbt_osmosis/core/schema/validation.py"
    - "tests/core/test_sync_operations.py"
    - "tests/core/test_validation.py"
parent_merge_scope:
  records:
    - "ticket:c10loss16"
  paths:
    - ".loom/tickets/20260503-c10loss16-stop-silent-yaml-data-loss.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "132f5eae04c016313f5ea9a909e7a592a157563e"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "132f5eae04c016313f5ea9a909e7a592a157563e"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10loss16 packet compilation"
  compiled_from:
    - "ticket:c10loss16"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
    - "wiki:versioned-model-yaml"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal local dependency/runtime behavior; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 8
  max_excerpt_lines_per_file: 220
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  evidence:
    - evidence:oracle-backlog-scan
  wiki:
    - wiki:versioned-model-yaml
  ticket:
    - "ticket:c10loss16"
links:
  tickets:
    - ticket:c10loss16
---

# Mission

Stop YAML sync from silently deleting duplicate model or version entries. Prefer a fail-closed policy with actionable errors unless a simple safe merge is demonstrably sufficient.

# Bound Context

`ticket:c10loss16` is high-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-003`. The current sync path mutates ruamel YAML documents and can remove user-authored duplicate model/version sections before writing. Silent deletion is the risk to eliminate.

Use local validation only for this ticket. Do not wait on GitHub Actions; final initiative validation will handle CI later.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/core/sync_operations.py::_deduplicate_model_entries()` collects same-name model entries, logs a warning, keeps the first entry, and `pop()`s later entries.
- `src/dbt_osmosis/core/sync_operations.py::_deduplicate_versions()` maps version entries by raw version key, replaces `doc_model["versions"]` with the mapped values, and silently drops duplicate entries.
- `src/dbt_osmosis/core/schema/validation.py::ModelValidator` now validates duplicate model version identity inside one model from `ticket:c10ver15`, but top-level duplicate `models[]` entries and sync-time duplicate deletion remain in scope here.
- `wiki:versioned-model-yaml` records accepted version identity and selector-preservation rules from `ticket:c10ver15`; reuse those rules rather than re-deriving them.

# Change Class

`code-behavior`, high risk. This ticket protects user-authored YAML content from silent deletion or overwrite.

# Verification Targets

- ticket:c10loss16#ACC-001
- ticket:c10loss16#ACC-002
- ticket:c10loss16#ACC-003
- ticket:c10loss16#ACC-004
- ticket:c10loss16#ACC-005
- ticket:c10loss16#ACC-006
- initiative:dbt-110-111-hardening#OBJ-003

# Task For This Iteration

Implement the smallest fail-closed duplicate handling that satisfies the ticket:

- Add focused failing tests first for duplicate model entries and duplicate version entries with distinct user-authored content.
- Replace silent duplicate deletion in sync with an actionable failure before document write. Prefer a clear exception type already used by the project if available; otherwise add the smallest local exception or validation issue that produces useful user-facing context.
- Coordinate validation so duplicate top-level model entries and duplicate version identity are caught before destructive sync when the validator is used.
- Preserve the `ticket:c10ver15` version identity semantics: exact raw first, restricted normalized fallback, distinct string versions preserved.
- Avoid designing a broad merge engine unless tests prove a simple safe merge is sufficient. The ticket's preferred policy is fail-closed.
- Keep generated/NL writer migration out of scope; `ticket:c10gen20` owns that broader migration.

# Verification Posture

`test-first`:

- Red checks: add duplicate model/version regression tests first and show they fail because current sync silently drops content or validation misses duplicates.
- Green focused checks: run the new/changed tests, likely targeted `tests/core/test_sync_operations.py` and `tests/core/test_validation.py` selections.
- Broader local checks: run related sync/validation/schema tests. Do not wait on GitHub Actions. Run targeted `pre-commit` and `git diff --check`.

# Stop Conditions

- Stop and report `escalate` if the implementation requires deciding between fail-closed and safe merge beyond the ticket's stated preference.
- Stop if duplicate handling requires a broad schema parser/writer rewrite.
- Stop if the duplicate checks conflict with dbt-valid versioned model YAML behavior accepted in `wiki:versioned-model-yaml`.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves silent data loss
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including duplicate model policy, duplicate version policy, validation behavior, and user-facing/actionable error behavior
- residual risks
- ticket recommendation

# Working Notes

Look first at `_deduplicate_model_entries()`, `_deduplicate_versions()`, `_get_or_create_model()`, `_get_or_create_version()`, `sync_node_to_yaml()`, and existing `ModelValidator` tests. Keep changes localized and fail closed before write.

# Child Output

Outcome: stop

Files changed:

- `src/dbt_osmosis/core/sync_operations.py`
- `src/dbt_osmosis/core/schema/validation.py`
- `tests/core/test_sync_operations.py`
- `tests/core/test_validation.py`

Records changed:

- `.loom/packets/ralph/20260504T112322Z-ticket-c10loss16-iter-01.md` child output section only

Red evidence:

- Command: `uv run pytest tests/core/test_sync_operations.py::test_get_or_create_model_rejects_duplicate_entries_without_deleting_user_content tests/core/test_sync_operations.py::test_get_or_create_version_rejects_duplicate_entries_without_deleting_user_content tests/core/test_validation.py::TestModelValidator::test_validate_duplicate_model_names`
- Result before implementation: 3 failed.
- Failure output summary:
  - `test_get_or_create_model_rejects_duplicate_entries_without_deleting_user_content`: `Failed: DID NOT RAISE <class 'dbt_osmosis.core.exceptions.YamlValidationError'>`; stderr logged `Found duplicate entries for model => customers`.
  - `test_get_or_create_version_rejects_duplicate_entries_without_deleting_user_content`: `Failed: DID NOT RAISE <class 'dbt_osmosis.core.exceptions.YamlValidationError'>`.
  - `test_validate_duplicate_model_names`: `assert not True` because `ValidationResult(issues=[], is_valid=True, fixes_applied=[])`.
- Why this proves silent data-loss risk: the sync duplicate tests expected fail-closed behavior while preserving two distinct user-authored entries; current sync accepted duplicates, with model sync only warning before its existing duplicate-removal path. Validation also missed duplicate top-level `models[]` entries entirely.

Green evidence:

- Command: `uv run pytest tests/core/test_sync_operations.py::test_get_or_create_model_rejects_duplicate_entries_without_deleting_user_content tests/core/test_sync_operations.py::test_get_or_create_version_rejects_duplicate_entries_without_deleting_user_content tests/core/test_validation.py::TestModelValidator::test_validate_duplicate_model_names tests/core/test_validation.py::TestModelValidator::test_validate_versioned_model_rejects_numeric_duplicate_versions tests/core/test_sync_operations.py::test_sync_node_to_yaml_versioned_preserves_column_selector`
  - Result: 5 passed in 4.26s.
- Command: `uv run pytest tests/core/test_sync_operations.py tests/core/test_validation.py`
  - Result: 76 passed in 16.09s after formatting.
- Command: `uv run pytest tests/core/test_schema.py`
  - Result: 13 passed in 0.54s.
- Command: `uv run pre-commit run --files src/dbt_osmosis/core/sync_operations.py src/dbt_osmosis/core/schema/validation.py tests/core/test_sync_operations.py tests/core/test_validation.py`
  - First run: `ruff-format` modified one file; all other hooks passed.
  - Rerun result: all applicable hooks passed (`check python ast`, EOF/whitespace, private key/debug checks, `ruff-format`, `ruff`, hardcoded secrets; JSON/YAML/TOML/GitHub Actions hooks skipped due no matching files).
- Command: `git diff --check`
  - Result: passed with no output.
- Skipped/blocked broader suites: full repository `uv run pytest` and `task test` were not run because the packet requested focused local validation and broader related sync/validation/schema checks; no local blocker observed.

Implementation summary:

- Duplicate model policy: `_deduplicate_model_entries()` no longer deletes same-name `models[]` entries. It raises `YamlValidationError` with model name, duplicate indexes, and an instruction to consolidate duplicate entries before syncing.
- Duplicate version policy: `_deduplicate_versions()` no longer rewrites `doc_model["versions"]`. It indexes versions only after checking dbt version identity with `_version_values_match()` and raises `YamlValidationError` on duplicates, including model name, duplicate version indexes, conflicting values, and consolidation guidance.
- Validation behavior: `ModelValidator` now emits `DUPLICATE_MODEL_NAME` for duplicate top-level model names with `model_name`, `model_index`, and `first_model_index` context. Existing duplicate version validation remains intact, including numeric identity semantics and distinct string-version preservation.
- User-facing/actionable errors: sync failures use the existing `YamlValidationError` hierarchy and explicitly say dbt-osmosis refuses to sync because choosing one entry would delete user-authored YAML content.

Residual risks:

- The fail-closed sync checks cover model and model-version duplicates in the localized sync path; generated/NL writer migration remains out of scope for `ticket:c10gen20`.
- Duplicate invalid version values are still primarily validator-owned; sync duplicate detection indexes valid dbt version identity values and preserves previous handling of malformed version entries.
- Full repository test suite was not run locally.

Ticket recommendation:

- Move `ticket:c10loss16` to `review_required` or `complete_pending_acceptance` after parent reconciliation records evidence. High-risk code-behavior work should receive critique before closure.

# Parent Merge Notes

Accepted with parent-side preflight refinement after mandatory critique. The child implementation replaced silent duplicate model/version deletion with fail-closed `YamlValidationError`s and validation coverage. Parent reconciliation added all-node preflight so one duplicate YAML document aborts the command before any target document is finalized or written.

Parent-side changes after child output:

- Added `_validate_no_duplicate_sync_entries()` and `_preflight_sync_group()` to scan grouped target YAML documents for duplicate `models[]`, `seeds[]`, and model `versions[]` entries before worker dispatch.
- Reused the child duplicate-version failure path during preflight so version identity follows the accepted `wiki:versioned-model-yaml` rules.
- Added regression coverage proving all-node sync raises before any `_finalize_synced_document(..., commit=True)` call when another target YAML has duplicate model entries.

Final parent validation:

- Focused duplicate/preflight/version-selector regressions: `6 passed`.
- Broader related sync/validation/schema suites: `90 passed`.
- Ruff format/check, `git diff --check`, and targeted pre-commit hooks passed.
- Final mandatory critique found no medium/high findings.

Ticket reconciliation should cite the final evidence and critique records and may close the ticket once the implementation and acceptance records are committed.
