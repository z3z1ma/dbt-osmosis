---
id: packet:ralph-ticket-c10lock07-20260503T234103Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:c10lock07
mode: execution
change_class: release-packaging
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-03T23:41:03Z
updated_at: 2026-05-03T23:50:14Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns output only; parent reconciles ticket, evidence, critique, and packet status.
  paths:
    - .github/workflows/tests.yml
    - .github/workflows/constraints.txt
    - Taskfile.yml
    - pyproject.toml
    - uv.lock
parent_merge_scope:
  records:
    - ticket:c10lock07
  paths:
    - .loom/tickets/20260503-c10lock07-lock-ci-dependency-resolution.md
    - .loom/evidence/
    - .loom/critique/
    - .loom/packets/ralph/20260503T234103Z-ticket-c10lock07-iter-01.md
source_fingerprint:
  git_commit: 9f743e4eb8da7b47cc0fed82e28e7b650666da6e
  integration_remote: origin
  integration_ref: refs/remotes/origin/main
  integration_commit: 9f743e4eb8da7b47cc0fed82e28e7b650666da6e
  git_status_summary: dirty_mixed
  git_status_detail: Parent-compiled packet and ticket activation are expected dirty state; child write-scope files were clean at compilation.
  compiled_from:
    - ticket:c10lock07
    - initiative:dbt-110-111-hardening
    - wiki:ci-compatibility-matrix
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed - dependency-resolution and install-smoke verification may need package index access; remote Git mutation remains forbidden.
context_budget:
  posture: normal
  max_source_files: 10
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  spec: []
  plan: []
  ticket:
    - ticket:c10lock07
  wiki:
    - wiki:ci-compatibility-matrix
links:
  tickets:
    - ticket:c10ci06
  evidence:
    - evidence:oracle-backlog-scan
    - evidence:c10ci06-main-ci-success
---

# Mission

Make CI and local compatibility dependency resolution reproducible enough that dbt/Python matrix evidence can be trusted. This iteration should implement the ticket's CI/Taskfile dependency-resolution hardening without broad package metadata cleanup.

# Bound Context

The initiative objective is dbt 1.10/1.11 compatibility hardening without breaking public CLI, YAML, or package behavior. `ticket:c10ci06` already promoted dbt 1.11 into the CI matrix and added `UV_NO_SYNC=1` after branch CI showed `uv run` could resync the matrix runtime back to the lockfile. `wiki:ci-compatibility-matrix` now records that accepted workflow and failure mode.

`ticket:c10lock07` owns the next step: make dependency resolution clean and reproducible per matrix entry rather than relying on a locked environment plus later dbt overlay. It does not own broad package extras cleanup; if a pip smoke reveals a broader metadata defect, fix only the minimum required for the smoke or return `blocked`/`escalate` with the exact finding for `ticket:c10pkg10`.

# Source Snapshot

Key ticket facts:

- Acceptance requires lock freshness checks, clean/deterministic matrix dbt installs, dependency consistency validation, exact version reporting, and one clean pip-based install smoke.
- Current noted implementation issue: `.github/workflows/tests.yml` syncs dev/openai extras and then overlays matrix dbt packages; `Taskfile.yml` has similar local mutation; `pyproject.toml` contains a uv-only dependency override that a pip smoke should not hide.
- Critique is mandatory after implementation because release-packaging and dependency resolution determine compatibility evidence trustworthiness.

Inspect at least these files before editing:

- `.github/workflows/tests.yml`
- `Taskfile.yml`
- `pyproject.toml`
- `.github/workflows/constraints.txt`

# Change Class

Declared as `release-packaging`, risk class `high`. Dependency-resolution changes can silently weaken compatibility evidence, so the child must produce before/after observations and leave enough command output for the parent to preserve evidence and request critique.

# Verification Targets

- `ticket:c10lock07#ACC-001`
- `ticket:c10lock07#ACC-002`
- `ticket:c10lock07#ACC-003`
- `ticket:c10lock07#ACC-004`
- `ticket:c10lock07#ACC-005`
- `initiative:dbt-110-111-hardening#OBJ-001`
- `initiative:dbt-110-111-hardening#OBJ-005`

# Task For This Iteration

Implement the smallest correct dependency-resolution hardening that satisfies the ticket:

1. Add a lock freshness check to CI, such as `uv lock --check` or the current uv equivalent, so CI fails when `pyproject.toml` and `uv.lock` are inconsistent.
2. Change matrix dbt jobs so each matrix entry uses a clean or deterministic environment and does not inherit incompatible locked dbt transitive state from a different dbt version.
3. Add dependency consistency validation (`pip check` or equivalent) to every matrix entry.
4. Preserve exact version reporting for Python, uv, dbt-core, dbt adapter, and dbt-osmosis.
5. Add at least one pip-based install smoke that exercises published package metadata outside uv resolution.
6. Mirror the accepted local workflow in `Taskfile.yml` enough that operators can reproduce the new CI posture locally.

Prefer minimal workflow changes over a broad CI rewrite. Do not change support policy, publish/release behavior, or broad extras/package metadata unless a direct install-smoke failure makes a small metadata fix necessary.

# Verification Posture

Declared as `observation-first`.

Before implementation, capture a short observation of the current state: which required signals are absent or weak in `.github/workflows/tests.yml` / `Taskfile.yml` (`uv lock --check`, clean matrix environment, `pip check`, pip smoke). After implementation, capture the relevant diff and command output.

Expected after-change checks:

- A lock freshness command succeeds locally, for example `uv lock --check` or the current uv equivalent discovered by the child.
- Workflow linting passes for `.github/workflows/tests.yml` through `pre-commit`/actionlint.
- A local pip-based smoke passes in a clean temporary environment, or the child reports the exact blocker if package metadata needs a separate ticket.
- Any changed Taskfile commands are syntactically coherent and, where feasible, one focused local command is run.

# Stop Conditions

- Stop and return `blocked` if `uv.lock` is already stale and cannot be refreshed without broad dependency churn outside this ticket.
- Stop and return `escalate` if satisfying the pip smoke requires broad extras/package metadata changes owned by `ticket:c10pkg10`.
- Stop before changing public CLI behavior, package support ranges, or release/tagging behavior.
- Stop if `Tests`/`lint` runs for the previous main push are failing for a cause unrelated to this ticket and could make new evidence ambiguous.
- Do not run destructive fixture cleanup or destructive Git commands. Do not mutate shared Git metadata.

# Output Contract

Return exactly:

- outcome: `continue`, `stop`, `blocked`, or `escalate`
- files changed
- records changed, if any
- before observations
- implementation summary
- verification commands and results, including exit codes where available
- self-review findings or concerns
- blockers or residual risks
- ticket recommendation, including whether mandatory critique is ready next

# Working Notes

The parent has not granted child authority to edit Loom records. Recommend evidence/ticket updates in output; the parent will reconcile records, critique, commits, pushes, issue updates, and closure.

# Child Output

Outcome: `stop`

Files changed:

- `.github/workflows/tests.yml`
- `Taskfile.yml`

Records changed: none.

Before observations:

- CI had no `uv lock --check` lock freshness gate.
- CI/Taskfile matrix flow used `uv sync` or locked/project-config state, then overlaid dbt versions.
- Matrix jobs lacked dependency consistency checks.
- Existing latest-core smoke used `uv pip`, not plain pip.
- Initial `uv pip check` with project config exposed protobuf incompatibilities for dbt 1.11 due the existing uv-only protobuf override.

Implementation summary:

- Added CI `lockfile` job with `uv lock --check`.
- Changed matrix jobs to create clean environments and install matrix dependencies with `uv --no-config pip install`.
- Added `uv --no-config pip check` to uv-managed matrix entries.
- Preserved version reporting for Python, uv, dbt-core, dbt-duckdb, and dbt-osmosis.
- Added plain pip install smoke job.
- Mirrored the local workflow in `Taskfile.yml` with `lock`, clean matrix env installs, dependency checks, and `pip-install-smoke`.

Verification reported by child:

- `uv lock --check` - passed, exit 0.
- `uvx --from uv==0.5.13 uv lock --check` - passed, exit 0.
- `task --list` - passed, exit 0.
- `task lock` - passed, exit 0.
- `uvx pre-commit run check-yaml --files .github/workflows/tests.yml Taskfile.yml` - passed, exit 0.
- `uvx pre-commit run actionlint --files .github/workflows/tests.yml` - passed, exit 0.
- Local plain pip smoke with `.[openai]`, `dbt-core~=1.11.0`, and `dbt-duckdb~=1.10.1` - passed, exit 0; `pip check` passed; installed dbt-core 1.11.8 and dbt-duckdb 1.10.1.
- `task pip-install-smoke` - passed, exit 0.
- Final uv matrix smoke with `uv --no-config pip install` and `uv --no-config pip check` - passed, exit 0.
- Same uv matrix smoke using constrained `uv==0.5.13` - passed, exit 0.

Residual risks reported by child:

- Existing `[tool.uv] override-dependencies = ["protobuf>=5.0,<6.0"]` remains; matrix installs intentionally bypass project uv config to avoid dbt 1.11 protobuf incompatibility. Broader cleanup still belongs to `ticket:c10pkg10`.
- Local verification used Python 3.12; CI pip smoke is configured for Python 3.13.
- Final confidence still needs GitHub Actions matrix evidence.

Ticket recommendation: move to mandatory critique after preserving evidence; do not close until critique and CI evidence are reconciled.

# Parent Merge Notes

Parent accepted the Ralph iteration as in-scope, recorded `evidence:c10lock07-local-dependency-resolution-verification`, and moved `ticket:c10lock07` through mandatory release-packaging/test-coverage critique. Initial critique found the matrix integration step still reached `uv run` through `demo_duckdb/integration_tests.sh`; parent fixed the workflow to run direct `dbt-osmosis` commands from the matrix environment, and follow-up critique passed. Final CI evidence remains pending after commit/push.
