---
id: packet:ralph-ticket-gh329loss-20260505T064213Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:gh329loss
mode: execution
change_class: code-behavior
risk_class: high
style: reference-first
verification_posture: test-first
iteration: 2
created_at: 2026-05-05T06:42:14Z
updated_at: 2026-05-05T06:51:54Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - packet:ralph-ticket-gh329loss-20260505T064213Z
  paths:
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/src/dbt_osmosis/core/restructuring.py
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/tests/core/test_restructuring.py
parent_merge_scope:
  records:
    - ticket:gh329loss
  paths:
    - .loom/evidence/
    - .loom/critique/
    - .loom/tickets/20260505-gh329loss-preserve-unmanaged-restructure-files.md
    - .loom/packets/ralph/20260505T062916Z-ticket-gh329loss-iter-01.md
    - .loom/packets/ralph/20260505T064213Z-ticket-gh329loss-iter-02.md
source_fingerprint:
  git_commit: db73d9e4a6fb2c28939d31aab611ca8d1f2f7021
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: db73d9e4a6fb2c28939d31aab611ca8d1f2f7021
  git_status_summary: dirty_tracked
  git_status_detail: gh329loss iteration 1 diff present in restructuring.py and tests/core/test_restructuring.py plus iter-01 packet
  compiled_from:
    - ticket:gh329loss
    - packet:ralph-ticket-gh329loss-20260505T062916Z
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 5
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution: []
  initiative:
    - initiative:issue-pr-zero
  research: []
  spec: []
  plan: []
  ticket:
    - ticket:gh329loss
links:
  packets:
    - packet:ralph-ticket-gh329loss-20260505T062916Z
---

# Mission

Address Oracle critique findings from iteration 1: fail closed when original YAML cache is missing and add dry-run/cache regression coverage for unmanaged superseded files.

# Bound Context

This packet continues `ticket:gh329loss` only. Preserve iteration 1's intended behavior: unmanaged root sections must not be lost, while truly empty superseded files still delete.

# Parent Launch Checklist

- Target ticket remains active for high-risk data-loss prevention.
- Iteration 1 diff is intentionally present and is the baseline for this packet.
- Child write scope uses absolute paths under the hardening worktree because the previous child accidentally edited the default checkout.
- Verification posture remains test-first.

# Source Snapshot

Oracle critique findings:

- Medium: deletion fails open if `_YAML_ORIGINAL_CACHE` is missing while filtered buffer still exists.
- Medium: ACC-005 dry-run/cache behavior lacks direct coverage for unmanaged superseded files.

# Change Class

`code-behavior`, high risk due data-loss prevention. Mandatory critique must run again after this iteration.

# Verification Targets

- ticket:gh329loss#ACC-001
- ticket:gh329loss#ACC-002
- ticket:gh329loss#ACC-003
- ticket:gh329loss#ACC-004
- ticket:gh329loss#ACC-005

# Task For This Iteration

Add the smallest changes so superseded cleanup preserves unmanaged content even if the original YAML cache entry is absent. Add a focused dry-run/cache test proving unmanaged superseded files are not deleted or stale-cached during dry-run.

# Verification Posture

`test-first`: add failing tests for missing-original-cache preservation and dry-run unmanaged superseded behavior before changing production code. Drive them green, then rerun focused superseded/schema tests and `git diff --check`.

# Stop Conditions

Stop if the fix requires broad cache redesign or parser/writer rewrites. Do not edit files outside the child write scope. Do not modify Git metadata.

# Output Contract

Return outcome, files changed, red evidence, green evidence, blockers/risks, and ticket recommendation.

# Working Notes

If fallback-loading unfiltered YAML from disk is needed, make sure the writer still has enough original content to merge preserved sections during the subsequent `_write_yaml()` call.

# Child Output

Outcome: stop.

Files changed:

- `src/dbt_osmosis/core/restructuring.py`
- `tests/core/test_restructuring.py`

Evidence gathered:

- Red: missing-original-cache unmanaged preservation regression failed before production fix because unmanaged content could still be deleted.
- Red: dry-run unmanaged superseded regression failed before production fix because the path was classified as fully superseded.
- Green: new regression tests passed with `2 passed`.
- Green: superseded tests passed with `7 passed`.
- Green: schema tests passed with `28 passed`.
- Green: `uv run ruff check` on touched files passed.
- Green: `git diff --check` passed.

Risks:

- Full suite not run.
- External `VIRTUAL_ENV` warning was observed but `uv` ignored it.

Ticket recommendation: mark implementation complete and route to mandatory critique.

# Parent Merge Notes

Parent reran focused pytest, Ruff, and `git diff --check`; Oracle critique returned `accept` with no findings. This packet is consumed as the final implementation iteration for `ticket:gh329loss`.
