---
id: packet:ralph-ticket-gh328scaf-20260505T065313Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:gh328scaf
mode: execution
change_class: code-behavior
risk_class: medium
style: reference-first
verification_posture: test-first
iteration: 1
created_at: 2026-05-05T06:53:13Z
updated_at: 2026-05-05T06:59:39Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - packet:ralph-ticket-gh328scaf-20260505T065313Z
  paths:
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/src/dbt_osmosis/core/sync_operations.py
    - /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening/tests/core/test_sync_operations.py
parent_merge_scope:
  records:
    - ticket:gh328scaf
  paths:
    - .loom/evidence/
    - .loom/critique/
    - .loom/tickets/20260505-gh328scaf-honor-scaffold-empty-configs-config.md
    - .loom/packets/ralph/20260505T065313Z-ticket-gh328scaf-iter-01.md
source_fingerprint:
  git_commit: db73d9e4a6fb2c28939d31aab611ca8d1f2f7021
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: db73d9e4a6fb2c28939d31aab611ca8d1f2f7021
  git_status_summary: dirty_mixed
  git_status_detail: gh329loss implementation and Loom records are present and out of this packet scope
  compiled_from:
    - ticket:gh328scaf
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: normal
  max_source_files: 5
  max_excerpt_lines_per_file: 120
  avoid_full_file_reads: true
sources:
  constitution: []
  initiative:
    - initiative:issue-pr-zero
  research: []
  spec: []
  plan: []
  ticket:
    - ticket:gh328scaf
links: {}
---

# Mission

Make `scaffold-empty-configs` configured through dbt project/node options affect YAML sync the same way the CLI flag does.

# Bound Context

This packet advances only `ticket:gh328scaf`. Do not touch the active `gh329loss` diff or records.

# Parent Launch Checklist

- Target ticket is ready and scoped to one settings-resolution bug.
- Child write scope is limited to `sync_operations.py` and `test_sync_operations.py` under the hardening worktree.
- Verification posture is test-first.

# Source Snapshot

- `src/dbt_osmosis/core/sync_operations.py` directly checks `context.settings.scaffold_empty_configs` when deciding whether to keep node descriptions and empty column fields.
- Existing sync tests use simple mocked context objects and already cover project vars for `output-to-lower`.

# Change Class

`code-behavior`, medium risk because it changes config resolution for YAML output but should remain narrow and opt-in.

# Verification Targets

- ticket:gh328scaf#ACC-001
- ticket:gh328scaf#ACC-002
- ticket:gh328scaf#ACC-003
- ticket:gh328scaf#ACC-004
- ticket:gh328scaf#ACC-005

# Task For This Iteration

Add focused failing tests proving project/node config can keep empty scaffold fields without the CLI flag, then update sync to use existing `resolve_setting()`/SettingsResolver pathways instead of direct global setting checks.

# Verification Posture

`test-first`: add failing tests first, observe red, implement minimal fix, then run focused sync tests and `git diff --check`.

# Stop Conditions

Stop if this requires config precedence redesign, new scaffold options, or changes outside sync operations/tests.

# Output Contract

Return outcome, files changed, red evidence, green evidence, risks/blockers, and ticket recommendation.

# Working Notes

Prefer helper-local resolution inside `_sync_doc_section()` if it keeps the code small. Preserve CLI fallback behavior with `fallback=context.settings.scaffold_empty_configs`.

# Child Output

Outcome: stop.

Files changed:

- `src/dbt_osmosis/core/sync_operations.py`
- `tests/core/test_sync_operations.py`

Evidence gathered:

- Red: `uv run pytest tests/core/test_sync_operations.py -k 'scaffold_empty_configs'` after tests-only change failed with `2 failed, 1 passed`; failures showed missing `description` for project/node config cases.
- Green: same focused test passed with `3 passed`.
- Green: `uv run pytest tests/core/test_sync_operations.py` passed with `34 passed`.
- Green: `git diff --check` passed.

Risks:

- Full repository suite not run.

Ticket recommendation: move to parent reconciliation and review.

# Parent Merge Notes

Parent reran focused tests, Ruff, and `git diff --check`; Oracle critique returned `accept` with no findings. This packet is consumed as the implementation iteration for `ticket:gh328scaf`.
