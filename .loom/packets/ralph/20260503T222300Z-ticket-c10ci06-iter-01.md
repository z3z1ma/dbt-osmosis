---
id: packet:ralph-ticket-c10ci06-20260503T222300Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:c10ci06
mode: execution
change_class: release-packaging
risk_class: high
style: reference-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-03T22:23:00Z
updated_at: 2026-05-03T22:30:07Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns implementation output; parent reconciles Loom records
  paths:
    - .github/workflows/tests.yml
    - Taskfile.yml
    - src/dbt_osmosis/core/logger.py
parent_merge_scope:
  records:
    - ticket:c10ci06
    - packet:ralph-ticket-c10ci06-20260503T222300Z
  paths:
    - .loom/packets/ralph/20260503T222300Z-ticket-c10ci06-iter-01.md
    - .loom/tickets/20260503-c10ci06-promote-dbt-111-ci-gate.md
source_fingerprint:
  git_commit: 063b0a03077841ec91765937fda9e360d8ae4473
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 0fd1a17d5e33c15805685d2d290fd531fba476a2
  git_status_summary: clean
  git_status_detail: clean before packet creation
  compiled_from:
    - ticket:c10ci06
    - evidence:oracle-backlog-scan
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: sibling_worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed - local install/smoke commands and GitHub Actions observation allowed; no remote mutation by child
context_budget:
  posture: normal
  max_source_files: 8
  max_excerpt_lines_per_file: 140
  avoid_full_file_reads: true
sources:
  constitution:
    - constitution:main
  initiative:
    - initiative:dbt-110-111-hardening
  research:
    - research:dbt-110-111-api-surfaces
  spec: []
  plan: []
  ticket:
    - ticket:c10ci06
links:
  github_issue: https://github.com/z3z1ma/dbt-osmosis/issues/357
  failing_run: https://github.com/z3z1ma/dbt-osmosis/actions/runs/25291931743
  evidence: evidence:c10ci06-ci-gate-local-verification
---

# Mission

Promote dbt 1.11.x from a narrow canary to an explicit compatibility gate where published DuckDB adapter support permits, add installed-version assertions, keep local Taskfile parity, and fix the current dbt 1.11 smoke import failure.

# Bound Context

GitHub Actions run `25291931743` failed in `Validate latest dbt-core compatibility` after installing `dbt-core==1.11.8` and `dbt-duckdb==1.10.1`; `dbt parse` succeeded, but `dbt-osmosis --help` failed with `AttributeError: module 'rich' has no attribute 'console'` from `src/dbt_osmosis/core/logger.py`.

# Parent Launch Checklist

- Target ticket is ready and high risk.
- Child write scope is limited to tests workflow, local Taskfile, and the logger import needed for current 1.11 smoke.
- Verification posture is observation-first: observe workflow shape and local smoke behavior before/after where feasible.

# Source Snapshot

Current workflow facts:

- Main CI matrix covers Python 3.10-3.13 and dbt 1.8/1.9/1.10, excluding Python 3.13 for dbt 1.8/1.9.
- Latest-core job is a single Python 3.13 smoke for `dbt-core~=1.11.0` plus `dbt-duckdb~=1.10.1`.
- That smoke currently fails at CLI import because `logger.py` imports `rich` and uses `rich.console.Console` without importing `rich.console`.
- Taskfile local test matrix covers 1.8/1.9 for Python 3.10-3.12 and 1.10 for Python 3.10-3.13, plus a latest-core smoke.

# Change Class

`release-packaging`, high risk. The workflow changes alter compatibility gates and support signaling.

# Verification Targets

- ticket:c10ci06#ACC-001
- ticket:c10ci06#ACC-002
- ticket:c10ci06#ACC-003
- ticket:c10ci06#ACC-004
- ticket:c10ci06#ACC-005

# Task For This Iteration

1. Update `.github/workflows/tests.yml` so dbt 1.11.x is an explicit gate in the main matrix where `dbt-duckdb~=1.10.1` is the published adapter support boundary.
2. Add installed version assertions/prints for Python, `dbt-core`, `dbt-duckdb`, and `dbt-osmosis` in matrix and latest-canary jobs.
3. Add a scheduled canary and make the latest-core compatibility job cover latest dbt 1.10.x and 1.11.x patch drift.
4. Update `Taskfile.yml` to mirror the same local support story, including dbt 1.11 with the 1.10.1 DuckDB adapter boundary.
5. Fix the `rich.console` import failure in `src/dbt_osmosis/core/logger.py` with the smallest correct import change.
6. Run focused local verification that does not require the full matrix: at minimum import/CLI smoke and YAML/workflow syntax sanity where feasible.

# Verification Posture

`observation-first`: before-state observation is the GitHub Actions failure above. After-state should include local `python -c "import dbt_osmosis; import dbt_osmosis.cli.main"`, `uv run dbt-osmosis --help`, and any feasible workflow/version assertion sanity checks.

# Stop Conditions

- Stop if adding dbt 1.11 to every Python row requires unavailable adapters beyond `dbt-duckdb~=1.10.1`.
- Stop if resolving dependency determinism requires lockfile or resolver policy work owned by ticket:c10lock07.
- Stop if fixing all failures uncovered by the matrix requires unrelated code changes beyond the current logger smoke failure.
- Stop if files outside child_write_scope need edits.

# Output Contract

Return outcome, files changed, records changed, before/after evidence, concerns, residual risks, and ticket recommendation.

# Working Notes

GitHub issue: https://github.com/z3z1ma/dbt-osmosis/issues/357

# Child Output

Outcome: stop.

Files changed:

- `.github/workflows/tests.yml`
- `Taskfile.yml`
- `src/dbt_osmosis/core/logger.py`

Records changed by child: None. Parent owns Loom reconciliation.

Before evidence:

- GitHub Actions run `25291931743` installed `dbt-core==1.11.8` and `dbt-duckdb==1.10.1`; `dbt parse` succeeded, then `dbt-osmosis --help` failed with `AttributeError: module 'rich' has no attribute 'console'`.

After evidence returned by child:

- `uv run python -c "import dbt_osmosis; import dbt_osmosis.cli.main; print('import smoke ok')"` completed and printed `import smoke ok`.
- `uv run dbt-osmosis --help` completed and displayed the CLI command tree.
- ruamel YAML workflow/Taskfile assertion script completed and printed `workflow/taskfile YAML and dbt matrix assertions ok`.
- `task --list` parsed the Taskfile and listed expected tasks.
- Isolated smoke env with `dbt-core~=1.11.0`, `dbt-duckdb~=1.10.1`, and local package install reported `dbt-core: 1.11.8`, `dbt-duckdb: 1.10.1`, `dbt-osmosis: 1.3.0`; `dbt-osmosis --help` succeeded.

Residual risks:

- Full CI matrix and full pytest under dbt 1.11 were not run by the child; the new gate is expected to expose any remaining failures for follow-up tickets.
- Dependency resolver determinism remains owned by `ticket:c10lock07`.

Ticket recommendation: route to mandatory critique, then update `ticket:c10ci06` with evidence and acceptance disposition.

# Parent Merge Notes

Parent accepted the child output as within packet scope. Parent reran focused checks and an isolated dbt 1.11 parse/import/CLI smoke, preserved the observation as `evidence:c10ci06-ci-gate-local-verification`, and moved this packet to `consumed`. Mandatory critique remains required before ticket acceptance can progress.
