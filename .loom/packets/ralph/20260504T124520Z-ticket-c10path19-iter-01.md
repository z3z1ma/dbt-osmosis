---
id: "packet:ralph-ticket-c10path19-20260504T124520Z"
kind: packet
packet_kind: ralph
status: consumed
target: "ticket:c10path19"
mode: execution
change_class: "code-behavior"
risk_class: "medium"
style: "snapshot-first"
verification_posture: "test-first"
iteration: "01"
created_at: "2026-05-04T12:45:20Z"
updated_at: "2026-05-04T12:55:31Z"
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - ".loom/packets/ralph/20260504T124520Z-ticket-c10path19-iter-01.md"
  paths:
    - "src/dbt_osmosis/core/path_management.py"
    - "src/dbt_osmosis/core/sync_operations.py"
    - "tests/core/test_path_management.py"
    - "tests/core/test_sync_operations.py"
    - "tests/core/test_security.py"
parent_merge_scope:
  records:
    - "ticket:c10path19"
  paths:
    - ".loom/tickets/20260503-c10path19-clear-path-source-errors.md"
    - ".loom/evidence/"
    - ".loom/critique/"
    - ".loom/wiki/"
source_fingerprint:
  git_commit: "e0c5d7a07b03da3f6fd06a78518129ae8cae5724"
  integration_remote: "origin"
  integration_ref: "origin/main"
  integration_commit: "e0c5d7a07b03da3f6fd06a78518129ae8cae5724"
  git_status_summary: "clean"
  git_status_detail: "No tracked or untracked worktree changes before c10path19 packet compilation"
  compiled_from:
    - "ticket:c10path19"
    - "initiative:dbt-110-111-hardening"
    - "evidence:oracle-backlog-scan"
execution_context:
  branch: "loom/dbt-110-111-hardening"
  push_remote: "origin"
  worktree: "/Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening"
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: "allowed only for normal local dependency/runtime behavior; do not wait on GitHub Actions for this ticket"
context_budget:
  posture: expansive
  max_source_files: 6
  max_excerpt_lines_per_file: 260
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:dbt-110-111-hardening
  evidence:
    - evidence:oracle-backlog-scan
  ticket:
    - "ticket:c10path19"
links:
  tickets:
    - ticket:c10path19
---

# Mission

Replace raw YAML path-template and source-table crashes with clear user-facing errors and safe defaults without broad path-template redesign.

# Bound Context

`ticket:c10path19` is medium-risk `code-behavior` work under `initiative:dbt-110-111-hardening#OBJ-003` and `#OBJ-006`. The work is ready and scoped to user-facing error clarity and safe source table list initialization.

Use local validation only for this ticket. Do not wait on GitHub Actions; final initiative validation will handle CI later.

# Source Snapshot

Current state at packet compilation:

- `src/dbt_osmosis/core/path_management.py::get_target_yaml_path()` builds a `format_dict` and directly calls `tpl.format(**format_dict)`. Invalid placeholders can surface raw `AttributeError` or `KeyError`.
- Existing project-root traversal validation happens after rendering through `resolved_path.is_relative_to(project_root_path)` and must remain intact.
- `src/dbt_osmosis/core/sync_operations.py::_get_or_create_source_table()` iterates `doc_source["tables"]`, so an existing source entry lacking `tables` crashes with raw `KeyError`.
- `_get_or_create_source()` currently scans `source.get("tables", [])`; malformed non-list `tables` can also produce poor behavior.
- Existing path tests live in `tests/core/test_path_management.py`, `tests/core/test_security.py`, and related error-handling tests. Existing source sync tests live in `tests/core/test_sync_operations.py`.

# Change Class

`code-behavior`, medium risk. The work changes error shape and safe YAML mutation behavior for user input.

# Verification Targets

- ticket:c10path19#ACC-001
- ticket:c10path19#ACC-002
- ticket:c10path19#ACC-003
- ticket:c10path19#ACC-004
- ticket:c10path19#ACC-005
- initiative:dbt-110-111-hardening#OBJ-003
- initiative:dbt-110-111-hardening#OBJ-006

# Task For This Iteration

Implement the smallest fix that satisfies the ticket:

- Add failing tests first for invalid YAML path template fields raising raw exceptions, source YAML with a missing `tables` key syncing safely, and source YAML with non-list `tables` failing clearly.
- Wrap only path template formatting failures caused by missing placeholders/attributes into `PathResolutionError` with the node unique ID, configured template, and missing key/attribute context.
- Preserve existing project-root path traversal validation and related security tests.
- Make `_get_or_create_source_table()` initialize missing `tables` with `setdefault("tables", [])` only when safe, and validate the result is a list before iterating/appending.
- Validate source entries used by `_get_or_create_source()` when scanning by table so malformed non-list `tables` fails clearly instead of being treated as an iterable of dict-like tables.
- Avoid auto-repairing malformed non-list `tables` values.

# Verification Posture

`test-first`:

- Red checks: write targeted tests first and show they fail because current behavior raises raw path/template errors or raw/misleading source `tables` errors.
- Green focused checks: run the new/changed path/source tests.
- Broader local checks: run `tests/core/test_path_management.py`, `tests/core/test_security.py`, and `tests/core/test_sync_operations.py`. Run Ruff format/check, `git diff --check`, and targeted pre-commit.

# Stop Conditions

- Stop and report `escalate` if clear path errors require a new path-template language or broad SettingsResolver changes.
- Stop if preserving traversal validation conflicts with wrapping format errors.
- Stop if source `tables` behavior requires a validation policy broader than missing key repair plus non-list fail-closed errors.
- Stop if governing records, integration ref, or child-write-scope files materially changed from the source fingerprint before launch.

# Output Contract

Return:

- outcome (`continue`, `stop`, `blocked`, or `escalate`)
- files changed
- records changed
- red evidence: tests, commands, failure output, and why failure proves raw path/source errors
- green evidence: focused and broader local tests, hooks/checks, and any skipped/blocked broader suites
- implementation summary, including path formatting error wrapping, traversal preservation, missing `tables` behavior, and non-list `tables` failure behavior
- residual risks
- ticket recommendation

# Working Notes

Keep path error wrapping narrow. Catch `KeyError` and `AttributeError` from `tpl.format(**format_dict)` only; do not swallow unrelated programmer errors outside formatting. Tests should assert exception type and useful message fragments, not exact full wording.

# Child Output

## Iteration 01 child output — 2026-05-04T12:48Z

- outcome: stop
- files changed:
  - `src/dbt_osmosis/core/path_management.py`
  - `src/dbt_osmosis/core/sync_operations.py`
  - `tests/core/test_path_management.py`
  - `tests/core/test_sync_operations.py`
  - `tests/core/test_security.py`
- records changed:
  - `.loom/packets/ralph/20260504T124520Z-ticket-c10path19-iter-01.md`
- red evidence:
  - Command: `uv run pytest tests/core/test_path_management.py::test_source_name_not_available_for_models tests/core/test_path_management.py::test_missing_path_template_placeholder_raises_path_resolution_error tests/core/test_sync_operations.py::test_get_or_create_source_table_initializes_missing_tables_list tests/core/test_sync_operations.py::test_get_or_create_source_table_rejects_non_list_tables tests/core/test_sync_operations.py::test_get_or_create_source_rejects_non_list_tables_when_matching_by_table`
  - Result before implementation: 5 failed.
  - Failure proof: path template tests leaked raw `AttributeError` for `{node.source_name}` and raw `KeyError` for `{missing_placeholder}`; source table tests leaked raw `KeyError: 'tables'` for missing `tables` and raw `AttributeError: 'str' object has no attribute 'get'` for non-list `tables` during table sync/source matching.
- green evidence:
  - New focused tests: same targeted pytest command passed (`5 passed in 4.93s`).
  - Broader scoped tests: `uv run pytest tests/core/test_path_management.py tests/core/test_security.py tests/core/test_sync_operations.py` passed after security expectation update (`55 passed, 1 skipped in 19.94s`; skipped source path-template test because fixture exposed no source node).
  - Formatting/checks: `uv run ruff format src/dbt_osmosis/core/path_management.py src/dbt_osmosis/core/sync_operations.py tests/core/test_path_management.py tests/core/test_sync_operations.py tests/core/test_security.py` completed (`1 file reformatted, 4 files left unchanged`); `uv run ruff check ...` passed; `git diff --check` passed with no output.
  - Targeted pre-commit: `pre-commit run --files src/dbt_osmosis/core/path_management.py src/dbt_osmosis/core/sync_operations.py tests/core/test_path_management.py tests/core/test_sync_operations.py tests/core/test_security.py` passed all applicable hooks.
- implementation summary:
  - Wrapped only `KeyError` and `AttributeError` raised by `tpl.format(**format_dict)` in `get_target_yaml_path()` into `PathResolutionError` that includes the node unique ID, configured template, and missing key/attribute context.
  - Left project-root traversal validation after template rendering intact.
  - Changed `_get_or_create_source_table()` to initialize missing `tables` with `setdefault("tables", [])` and validate the resulting value is a list before iterating/appending.
  - Changed `_get_or_create_source()` table matching to validate source `tables` is a list before scanning, failing with `YamlValidationError` for malformed non-list values without auto-repairing them.
- residual risks:
  - Validation is scoped to missing `tables` repair and non-list `tables` fail-closed behavior; broader YAML structural validation remains outside this iteration.
  - Existing list entries inside `tables` are still assumed to be table-like mappings; this iteration did not add per-entry validation.
  - Local environment emits a `VIRTUAL_ENV` mismatch warning under `uv`; commands still ran against the project environment and passed.
- ticket recommendation: move to review/acceptance disposition; no further Ralph implementation appears necessary for this bounded ticket slice.

# Parent Merge Notes

Parent accepted the child output as in scope and complete:

- Re-ran focused c10path19 regressions: `5 passed`.
- Re-ran related path/source/error suites: `72 passed, 1 skipped`.
- Ran Ruff format/check, `git diff --check`, and targeted pre-commit successfully.
- Ran read-only critique; one low adjacent risk was accepted as non-blocking and out of scope.
- Committed implementation as `b324adc0f1a7cca571504aa168b2e58010e5a81f`.
- Recorded evidence in `evidence:c10path19-path-source-error-validation`.
- Recorded final critique in `critique:c10path19-path-source-error-review`.

Outcome: consumed; ticket reconciliation completed in `ticket:c10path19`.
