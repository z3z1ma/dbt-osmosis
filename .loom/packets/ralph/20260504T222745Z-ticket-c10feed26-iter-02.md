---
id: packet:ralph-ticket-c10feed26-20260504T222745Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:c10feed26
mode: execution
change_class: security-sensitive
risk_class: medium
style: reference-first
verification_posture: test-first
iteration: 2
created_at: 2026-05-04T22:27:45Z
updated_at: 2026-05-04T22:31:06Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - "None - child returns output only; parent reconciles Loom records"
  paths:
    - src/dbt_osmosis/workbench/app.py
    - tests/core/test_workbench_app.py
parent_merge_scope:
  records:
    - ticket:c10feed26
    - packet:ralph-ticket-c10feed26-20260504T222745Z
  paths:
    - .loom/tickets/20260503-c10feed26-sandbox-workbench-rss-html.md
    - .loom/packets/ralph/20260504T222745Z-ticket-c10feed26-iter-02.md
    - .loom/evidence/*c10feed26*.md
    - .loom/critique/*c10feed26*.md
source_fingerprint:
  git_commit: 4bc9bdbc99563806709486b27f00e1b842a6d210
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 4bc9bdbc99563806709486b27f00e1b842a6d210
  git_status_summary: dirty_mixed
  git_status_detail: "iteration 01 source/test/docs changes plus parent-owned records and consumed critique packet are present"
  compiled_from:
    - ticket:c10feed26
    - packet:ralph-ticket-c10feed26-20260504T221607Z
    - critique:c10feed26-workbench-feed-hardening-review
    - evidence:c10feed26-workbench-feed-hardening-validation
execution_context:
  branch: loom/dbt-110-111-hardening
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/dbt-osmosis-dbt-110-111-hardening
  isolation: worktree
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: allowed
context_budget:
  posture: normal
  max_source_files: 4
  max_excerpt_lines_per_file: 100
  avoid_full_file_reads: true
sources:
  target_records:
    - ticket:c10feed26
    - critique:c10feed26-workbench-feed-hardening-review
  evidence:
    - evidence:c10feed26-workbench-feed-hardening-validation
  diffs_or_paths:
    - src/dbt_osmosis/workbench/app.py
    - tests/core/test_workbench_app.py
  prior_packets:
    - packet:ralph-ticket-c10feed26-20260504T221607Z
links:
  critique:
    - critique:c10feed26-workbench-feed-hardening-review
---

# Mission

Resolve the blocking c10feed26 critique findings without widening scope.

# Bound Context

This packet is iteration 02 for `ticket:c10feed26`. Iteration 01 made the feed disabled by default and opt-in, but mandatory critique found incomplete failure tolerance.

# Task For This Iteration

1. Add failing tests first for:
   - malformed HTTP(S)-looking feed-entry URLs that make `urllib.parse.urlparse()` raise, proving enabled feed rendering fails closed instead of raising;
   - RSS response size cap behavior in `_fetch_feed_bytes()` or equivalent code.
2. Implement the smallest fix:
   - `_is_http_url()`/URL validation fails closed on parser exceptions;
   - entry rendering cannot escape `build_feed_html()` failure handling;
   - `_fetch_feed_bytes()` caps response reads at a documented constant and raises/returns fallback when exceeded.
3. Preserve iteration 01 behavior and tests.

# Verification Posture

`test-first`.

Expected red:

- `uv run pytest tests/core/test_workbench_app.py -q` should fail after adding the new malformed URL / size cap tests and before implementation.

Expected green:

- `uv run pytest tests/core/test_workbench_app.py tests/core/test_cli.py -q`
- `uv run ruff check src/dbt_osmosis/workbench/app.py tests/core/test_workbench_app.py`
- If feasible, `uv run basedpyright src/dbt_osmosis/workbench/app.py`

# Stop Conditions

Stop and return `blocked` if resolving the findings requires edits outside `src/dbt_osmosis/workbench/app.py` and `tests/core/test_workbench_app.py`.

# Output Contract

Return:

- outcome (`continue|stop|blocked|escalate`)
- files changed
- records changed
- red command and failure summary
- green command(s) and result summary
- implementation summary
- self-review findings or concerns
- blockers or residual risks
- ticket recommendation

# Working Notes

FIND-001 is medium and blocks acceptance until resolved. FIND-002 is low but should be resolved in code if practical rather than accepted as risk.

# Child Output

Outcome: `stop`.

Files changed:

- `src/dbt_osmosis/workbench/app.py`
- `tests/core/test_workbench_app.py`

Records changed: None.

Red evidence reported by child:

- Command: `uv run pytest tests/core/test_workbench_app.py -q`.
- Result: expected red, 2 failures: malformed entry URL raised `ValueError`; oversized RSS response did not raise.

Green evidence reported by child:

- `uv run pytest tests/core/test_workbench_app.py tests/core/test_cli.py -q` -> `45 passed`.
- `uv run ruff check src/dbt_osmosis/workbench/app.py tests/core/test_workbench_app.py` -> passed.
- `uv run basedpyright src/dbt_osmosis/workbench/app.py` -> `0 errors`.

Implementation summary:

- `_is_http_url()` now fails closed on URL parser exceptions.
- Feed entry rendering is inside `build_feed_html()` failure handling.
- `_fetch_feed_bytes()` reads at most `FEED_RESPONSE_MAX_BYTES + 1` and rejects oversized responses.
- Added tests for malformed entry URL handling and RSS response size cap.

Blockers or residual risks: none identified by child.

Ticket recommendation: parent can reconcile this iteration as resolving FIND-001/FIND-002 and proceed to acceptance/required review disposition.

# Parent Merge Notes

Parent accepted iteration 02 on 2026-05-04T22:31:06Z after reviewing the focused diff and rerunning validation:

- `uv run pytest tests/core/test_workbench_app.py tests/core/test_cli.py -q` -> `45 passed`.
- `uv run ruff check src/dbt_osmosis/workbench/app.py tests/core/test_workbench_app.py src/dbt_osmosis/cli/main.py tests/core/test_cli.py` -> passed.
- `uv run basedpyright src/dbt_osmosis/workbench/app.py src/dbt_osmosis/cli/main.py` -> `0 errors, 354 warnings`.

Evidence updated in `evidence:c10feed26-workbench-feed-hardening-validation`. Ticket moved to `review_required` for final required critique verification.
