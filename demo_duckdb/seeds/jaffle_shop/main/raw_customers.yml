---
version: 2
seeds:
  - name : raw_customers
    meta :
      foo : bar
    columns :
      - {name : id, data_type : INTEGER}
      - {name : first_name, data_type : VARCHAR}
      - {name : last_name, data_type : VARCHAR}
