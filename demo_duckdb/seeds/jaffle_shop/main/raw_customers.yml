version: 2
seeds:
  - name: raw_customers
    columns:
      - name: id
        data_type: INTEGER
      - name: first_name
        data_type: VARCHAR
      - name: last_name
        data_type: VARCHAR
