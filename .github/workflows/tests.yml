name: Tests

on:
  push:
  pull_request:
  schedule:
    - cron: "17 8 * * 1"

jobs:
  tests:
    name: Run pytest
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
        dbt-version: ["1.8.0", "1.9.0", "1.10.0", "1.11.0"]
        include:
          - dbt-version: "1.8.0"
            dbt-duckdb-version: "1.8.0"
          - dbt-version: "1.9.0"
            dbt-duckdb-version: "1.9.0"
          - dbt-version: "1.10.0"
            dbt-duckdb-version: "1.10.0"
          - dbt-version: "1.11.0"
            dbt-duckdb-version: "1.10.1"
        exclude:
          - python-version: "3.13"
            dbt-version: "1.8.0"
          - python-version: "3.13"
            dbt-version: "1.9.0"

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4.1.1

      - name: Set up Python
        uses: actions/setup-python@v5.1.0
        with:
          python-version: "${{ matrix.python-version }}"

      - name: Upgrade pip
        run: |
          pip install --constraint=.github/workflows/constraints.txt pip
          pip --version

      - name: Install uv
        run: |
          pip install --constraint=.github/workflows/constraints.txt uv
          uv --version

      - name: Install required packages
        run: | # install dev and openai extras to be able to parse manifest and run all tests
          uv sync --extra dev --extra openai

      - name: Install dbt runtime
        run: |
          uv pip install "dbt-core~=${{ matrix.dbt-version }}" "dbt-duckdb~=${{ matrix.dbt-duckdb-version }}"

      - name: Assert installed versions
        run: |
          uv run python - <<'PY'
          import importlib.metadata as metadata
          import platform
          import sys

          expected_python = "${{ matrix.python-version }}"
          expected = {
              "dbt-core": "${{ matrix.dbt-version }}",
              "dbt-duckdb": "${{ matrix.dbt-duckdb-version }}",
          }

          print(f"python: {sys.version}")
          print(f"python executable: {sys.executable}")
          assert platform.python_version().startswith(expected_python + "."), platform.python_version()

          for package in ("dbt-core", "dbt-duckdb", "dbt-osmosis"):
              version = metadata.version(package)
              print(f"{package}: {version}")
              if package in expected:
                  expected_prefix = ".".join(expected[package].split(".")[:2]) + "."
                  assert version.startswith(expected_prefix), f"{package} {version} does not match {expected_prefix}x"
          PY

      - name: Run basedpyright
        run: |
          uv run basedpyright --outputjson > /tmp/dbt-osmosis-basedpyright.json || true
          python -c "import json, pathlib, sys; summary = json.loads(pathlib.Path('/tmp/dbt-osmosis-basedpyright.json').read_text())['summary']; print('basedpyright summary:', summary['errorCount'], 'errors,', summary['warningCount'], 'warnings'); sys.exit(1 if summary['errorCount'] else 0)"

      - name: Parse manifest
        run: |
          uv run dbt parse --project-dir demo_duckdb --profiles-dir demo_duckdb -t test

      - name: Run pytest
        run: |
          uv run pytest

      - name: Run integration tests
        working-directory: demo_duckdb
        run: |
          bash -x integration_tests.sh

  latest-core-compat:
    name: Validate latest dbt compatibility (${{ matrix.dbt-version }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        dbt-version: ["1.10.0", "1.11.0"]
        include:
          - dbt-version: "1.10.0"
            dbt-duckdb-version: "1.10.0"
          - dbt-version: "1.11.0"
            dbt-duckdb-version: "1.10.1"

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4.1.1

      - name: Set up Python
        uses: actions/setup-python@v5.1.0
        with:
          python-version: "3.13"

      - name: Upgrade pip
        run: |
          pip install --constraint=.github/workflows/constraints.txt pip
          pip --version

      - name: Install uv
        run: |
          pip install --constraint=.github/workflows/constraints.txt uv
          uv --version

      - name: Validate latest dbt-core compatibility with published DuckDB adapter
        run: |
          SMOKE_ENV=$(mktemp -d)/dbt-osmosis-smoke
          uv venv "$SMOKE_ENV"
          uv pip install --python "$SMOKE_ENV/bin/python" "dbt-core~=${{ matrix.dbt-version }}" "dbt-duckdb~=${{ matrix.dbt-duckdb-version }}" basedpyright pytest pytest-cov hypothesis openai .
          export VIRTUAL_ENV="$SMOKE_ENV"
          export PATH="$SMOKE_ENV/bin:$PATH"
          python - <<'PY'
          import importlib.metadata as metadata
          import platform
          import sys

          expected_python = "3.13"
          expected = {
              "dbt-core": "${{ matrix.dbt-version }}",
              "dbt-duckdb": "${{ matrix.dbt-duckdb-version }}",
          }

          print(f"python: {sys.version}")
          print(f"python executable: {sys.executable}")
          assert platform.python_version().startswith(expected_python + "."), platform.python_version()

          for package in ("dbt-core", "dbt-duckdb", "dbt-osmosis"):
              version = metadata.version(package)
              print(f"{package}: {version}")
              if package in expected:
                  expected_prefix = ".".join(expected[package].split(".")[:2]) + "."
                  assert version.startswith(expected_prefix), f"{package} {version} does not match {expected_prefix}x"
          PY
          basedpyright --outputjson > /tmp/dbt-osmosis-basedpyright.json || true
          python -c "import json, pathlib, sys; summary = json.loads(pathlib.Path('/tmp/dbt-osmosis-basedpyright.json').read_text())['summary']; print('basedpyright summary:', summary['errorCount'], 'errors,', summary['warningCount'], 'warnings'); sys.exit(1 if summary['errorCount'] else 0)"
          dbt --version
          dbt parse --project-dir demo_duckdb --profiles-dir demo_duckdb -t test
          dbt-osmosis --help
          python -c "import dbt_osmosis; import dbt_osmosis.cli.main"
          pytest -q
